---
phase: 04-browse-tab-port
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/PROJECT.md
autonomous: true
requirements:
  - BROWSE-V2-01   # this plan ALSO trims the "(or /?tab=browse)" alias from BROWSE-V2-01 wording
  - BROWSE-V2-04   # explicitly removed/scope-out by this plan per D-19..D-22
tags: [docs, scope, requirements, traceability]

must_haves:
  truths:
    - "REQUIREMENTS.md no longer lists BROWSE-V2-04 as Pending under Browse Tab (Port)"
    - "REQUIREMENTS.md Out of Scope section contains a row for v2.0 Excel/CSV export with the locked rationale"
    - "REQUIREMENTS.md totals reflect 45 v2.0 reqs and Phase 4 mapped count = 4"
    - "REQUIREMENTS.md BROWSE-V2-01 wording no longer mentions the '/?tab=browse' alias (primary path '/browse' covers it)"
    - "ROADMAP.md Phase 4 shows three success criteria (filter swap, caps, URL round-trip) — not four"
    - "ROADMAP.md Phase 4 Requirements line lists exactly BROWSE-V2-01, BROWSE-V2-02, BROWSE-V2-03, BROWSE-V2-05"
    - "PROJECT.md Browse carry-over (v2.0) Active section no longer includes 'Excel + CSV export'"
    - "PROJECT.md Out of Scope section contains the export-removal rationale"
    - "PROJECT.md Key Decisions table contains the 'Drop v2.0 Browse export' row"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "BROWSE-V2-04 marked Out of Scope; totals updated; BROWSE-V2-01 alias trimmed"
      contains: "Excel/CSV export under v2.0 shell"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 4 trimmed to 4 requirements + 3 success criteria"
      contains: "BROWSE-V2-01, BROWSE-V2-02, BROWSE-V2-03, BROWSE-V2-05"
    - path: ".planning/PROJECT.md"
      provides: "Export carry moved to Out of Scope; Key Decision row added"
      contains: "Drop v2.0 Browse export to keep the port view-only"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/ROADMAP.md"
      via: "Phase 4 requirement-ID set"
      pattern: "BROWSE-V2-01.*BROWSE-V2-02.*BROWSE-V2-03.*BROWSE-V2-05"
---

<objective>
Apply the upstream documentation edits demanded by CONTEXT.md decisions D-19..D-22 (export feature removed from Phase 4 scope) BEFORE any code is written for Phase 4. This plan touches NO code — only the three planning documents that record the requirement scope, success criteria, and project carry-over.

Purpose: After the user's locked decision to drop the v2.0 Browse export feature, REQUIREMENTS.md / ROADMAP.md / PROJECT.md still claim BROWSE-V2-04 is Pending under Phase 4 with success criterion #3 about Excel/CSV download. Without these edits, the traceability tables disagree with the actual scope, the planner's `requirements:` frontmatter cannot honor "every roadmap requirement ID MUST appear in at least one plan" without false claims, and the Phase 4 invariant tests (Plan 04-04) would catch the inconsistency at runtime.

This plan also trims the parenthetical "(or `/?tab=browse`)" alias from BROWSE-V2-01: no plan implements the alias as a 302 redirect, and CONTEXT.md never locks it as a separate decision. Trimming the alias keeps REQUIREMENTS.md honest about what Phase 4 actually ships (the primary `/browse` path covers BROWSE-V2-01).

Output: Three updated planning documents. Phase 4 truthfully scoped to BROWSE-V2-01 (without alias), BROWSE-V2-02, BROWSE-V2-03, BROWSE-V2-05 (4 requirements) with 3 success criteria.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-browse-tab-port/04-CONTEXT.md
@.planning/phases/04-browse-tab-port/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move BROWSE-V2-04 to Out of Scope in REQUIREMENTS.md, trim BROWSE-V2-01 alias, and update totals</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (full file, especially line 63 "BROWSE-V2-01" + lines 60-100 "Browse Tab (Port)" + lines 90+ "Out of Scope" + lines 100-160 "Traceability" + footer "Totals")
    - .planning/phases/04-browse-tab-port/04-CONTEXT.md (D-19, D-20, D-21, D-22 + "Required upstream edits BEFORE planning starts" §)
    - .planning/phases/04-browse-tab-port/04-RESEARCH.md ("Required Upstream Edits BEFORE Planning Starts" § — Edit 1)
  </read_first>
  <action>
    Edit `.planning/REQUIREMENTS.md` to apply ALL of the following changes in a single revision:

    1) **Trim the BROWSE-V2-01 alias** (currently line 63). Original line:
       `- [ ] **BROWSE-V2-01**: Browse tab at `/browse` (or `/?tab=browse`) re-implements v1.0's pivot grid (platform × parameter) under Bootstrap + HTMX. Filter selectors (platform multiselect, parameter multiselect, swap-axes toggle) are HTMX-swapped on change — no full page reload.`
       Edited line:
       `- [ ] **BROWSE-V2-01**: Browse tab at `/browse` re-implements v1.0's pivot grid (platform × parameter) under Bootstrap + HTMX. Filter selectors (platform multiselect, parameter multiselect, swap-axes toggle) are HTMX-swapped on change — no full page reload.`
       (Drop the parenthetical " (or `/?tab=browse`)" — preserve the rest verbatim. No plan in Phase 4 implements the alias as a 302 redirect, and CONTEXT.md never locks it; this trim keeps REQUIREMENTS honest about what's actually shipping.)

    2) **Remove the BROWSE-V2-04 line** from the "### Browse Tab (Port)" section (currently line 66):
       `- [ ] **BROWSE-V2-04**: Excel (openpyxl) and CSV (utf-8-sig, single BOM) export via dedicated endpoints (`/browse/export/xlsx`, `/browse/export/csv`). Export respects current filter state.`
       Delete this line entirely; do NOT replace with anything in that section.

    3) **Add a new entry under "## Out of Scope"** (currently line 90+) — insert as a new bullet (place it logically in the existing list, e.g., after the litellm/openai bullet):
       `- **Excel/CSV export under v2.0 shell** — v1.0 Streamlit Browse remains the export surface until v1.0 sunset; v2.0 Browse is view-only by design choice (2026-04-26). v1.0's app/components/export_dialog.py is the reference implementation if/when this returns.`

    4) **Remove the BROWSE-V2-04 row** from the "## Traceability" table (currently line 142): `| BROWSE-V2-04 | Phase 4 | Pending |` — delete this row entirely.

    5) **Update the "**Totals:**" footer block** (currently lines 153-156). Two specific edits:
       - Change `v2.0 Requirements: **46** (9 INFRA + 6 OVERVIEW + 4 FILTER + 8 CONTENT + 7 SUMMARY + 5 BROWSE-V2 + 8 ASK-V2)` → `v2.0 Requirements: **45** (9 INFRA + 6 OVERVIEW + 4 FILTER + 8 CONTENT + 7 SUMMARY + 4 BROWSE-V2 + 8 ASK-V2)`
       - Change `Mapped to phases: 46 / 46 (Phase 1: 9, Phase 2: 10, Phase 3: 15, Phase 4: 5, Phase 5: 8)` → `Mapped to phases: 45 / 45 (Phase 1: 9, Phase 2: 10, Phase 3: 15, Phase 4: 4, Phase 5: 8)`
       - Increment `Deferred: 7` to `Deferred: 7` (no change — this is the future requirements count, not Out-of-Scope).

    Do NOT touch any other line. Do NOT renumber BROWSE-V2-05 (the gap in the ID sequence is intentional — IDs are stable identifiers).

    Commit message for this edit (when handed to gsd-tools commit): `docs(req): trim BROWSE-V2-01 alias + move BROWSE-V2-04 export to Out of Scope (D-20, D-21)` — single commit covers both the alias trim AND the export trim.
  </action>
  <verify>
    <automated>! grep -F '/?tab=browse' .planning/REQUIREMENTS.md && ! grep -E "^\- \[ \] \*\*BROWSE-V2-04\*\*" .planning/REQUIREMENTS.md && grep -q "Excel/CSV export under v2.0 shell" .planning/REQUIREMENTS.md && ! grep -E "^\| BROWSE-V2-04 \| Phase 4" .planning/REQUIREMENTS.md && grep -q "v2.0 Requirements: \*\*45\*\*" .planning/REQUIREMENTS.md && grep -q "Phase 4: 4" .planning/REQUIREMENTS.md && grep -q "4 BROWSE-V2" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -F '/?tab=browse' .planning/REQUIREMENTS.md` succeeds (the alias literal `/?tab=browse` appears NOWHERE in the file after the edit — Issue 1 fix)
    - BROWSE-V2-01 line still exists and still mentions `/browse`: `grep -E '^\- \[ \] \*\*BROWSE-V2-01\*\*.*`/browse`' .planning/REQUIREMENTS.md` succeeds
    - `grep -c "BROWSE-V2-04" .planning/REQUIREMENTS.md` returns 0 (the ID appears nowhere in the file)
    - `grep -q "Excel/CSV export under v2.0 shell" .planning/REQUIREMENTS.md` succeeds (Out of Scope entry present)
    - `grep -q "v1.0 Streamlit Browse remains the export surface" .planning/REQUIREMENTS.md` succeeds (rationale present)
    - `grep -q "v2.0 Requirements: \*\*45\*\*" .planning/REQUIREMENTS.md` succeeds (totals updated)
    - `grep -q "(9 INFRA + 6 OVERVIEW + 4 FILTER + 8 CONTENT + 7 SUMMARY + 4 BROWSE-V2 + 8 ASK-V2)" .planning/REQUIREMENTS.md` succeeds (BROWSE-V2 count = 4)
    - `grep -q "Mapped to phases: 45 / 45" .planning/REQUIREMENTS.md` succeeds
    - `grep -q "Phase 4: 4" .planning/REQUIREMENTS.md` succeeds
    - BROWSE-V2-01, BROWSE-V2-02, BROWSE-V2-03, BROWSE-V2-05 all still present (verify with `for id in 01 02 03 05; do grep -q "BROWSE-V2-$id" .planning/REQUIREMENTS.md || exit 1; done`)
  </acceptance_criteria>
  <done>BROWSE-V2-01 wording trimmed to drop the `/?tab=browse` alias; BROWSE-V2-04 is removed from Pending and added to Out of Scope; traceability table no longer lists it; totals reflect 45 v2.0 requirements with Phase 4 mapped count = 4.</done>
</task>

<task type="auto">
  <name>Task 2: Trim Phase 4 in ROADMAP.md to 4 requirements + 3 success criteria</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (full file, especially lines 82-92 "### Phase 4: Browse Tab Port")
    - .planning/phases/04-browse-tab-port/04-CONTEXT.md (D-19, D-20, D-21 + "Required upstream edits" §)
    - .planning/phases/04-browse-tab-port/04-RESEARCH.md ("Required Upstream Edits" § — Edit 2)
  </read_first>
  <action>
    Edit `.planning/ROADMAP.md` Phase 4 section (currently lines 82-92) with two specific changes:

    1) **Update the Requirements line** (currently line 85):
       `**Requirements**: BROWSE-V2-01, BROWSE-V2-02, BROWSE-V2-03, BROWSE-V2-04, BROWSE-V2-05`
       →
       `**Requirements**: BROWSE-V2-01, BROWSE-V2-02, BROWSE-V2-03, BROWSE-V2-05`
       (drop BROWSE-V2-04; preserve the rest verbatim including the comma-space delimiter style)

    2) **Delete success criterion #3 entirely** (currently line 89):
       `  3. User can download the current pivot grid as Excel (.xlsx) or CSV; the export reflects the active filter state and respects the row/col caps`
       After deletion, **renumber the remaining items** so the list reads:
       ```
       **Success Criteria** (what must be TRUE):
         1. User can select platforms and parameters, and the pivot grid updates in the Browse tab without a full page reload; the sticky header remains visible while scrolling
         2. The 30-column cap warning and 200-row cap warning appear when the respective limits are reached — matching v1.0 behavior exactly
         3. A Browse URL with query params (e.g. `?platforms=...&params=...&swap=1`) renders the correct filtered pivot grid when opened directly — the link is shareable
       ```
       (The original #4 about URL round-trip becomes the new #3.)

    Do NOT touch the Goal, Depends on, Plans, or any other Phase 4 field. Do NOT touch any other phase. Do NOT touch the v1.0 archive `<details>` block.
  </action>
  <verify>
    <automated>grep -A1 "^### Phase 4" .planning/ROADMAP.md | grep -q "Browse Tab Port" && grep -E "^\*\*Requirements\*\*: BROWSE-V2-01, BROWSE-V2-02, BROWSE-V2-03, BROWSE-V2-05$" .planning/ROADMAP.md && ! grep -q "Excel (.xlsx) or CSV" .planning/ROADMAP.md && grep -q "shareable" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "BROWSE-V2-04" .planning/ROADMAP.md` returns 0
    - `grep -E "^\*\*Requirements\*\*: BROWSE-V2-01, BROWSE-V2-02, BROWSE-V2-03, BROWSE-V2-05$" .planning/ROADMAP.md` matches exactly one line under the Phase 4 section
    - `grep -c "Excel (.xlsx) or CSV" .planning/ROADMAP.md` returns 0
    - `grep -c "Excel/CSV" .planning/ROADMAP.md` returns 0
    - The Phase 4 section's Success Criteria block contains exactly three numbered items (verify by counting): `awk '/^### Phase 4/,/^### Phase 5/' .planning/ROADMAP.md | grep -cE "^  [1-9]\."` returns exactly 3
    - The numbering is sequential 1, 2, 3 (no gap from removal): `awk '/^### Phase 4/,/^### Phase 5/' .planning/ROADMAP.md | grep -cE "^  3\. A Browse URL"` returns 1
  </acceptance_criteria>
  <done>Phase 4 in ROADMAP.md lists exactly four requirements and three success criteria; no Excel/CSV references remain.</done>
</task>

<task type="auto">
  <name>Task 3: Update PROJECT.md to reflect export removal from v2.0 Browse</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - .planning/PROJECT.md (full file, especially the "Browse carry-over (v2.0)" Active section, the "Out of Scope" section, and the "Key Decisions" table)
    - .planning/phases/04-browse-tab-port/04-CONTEXT.md (D-19, D-20, D-22 + "Required upstream edits" § Edit 3)
    - .planning/phases/04-browse-tab-port/04-RESEARCH.md ("Required Upstream Edits" § — Edit 3)
  </read_first>
  <action>
    Edit `.planning/PROJECT.md` with these specific changes:

    1) **Locate the "Browse carry-over (v2.0)" Active section** (search for the heading; it currently contains a checklist that includes the line `- [ ] Excel + CSV export`). **Delete** that single line. Do not modify the other Browse carry-over items.

    2) **Locate the "Out of Scope" section** (search for the `## Out of Scope` heading or equivalent; line ~110-115). Add this new bullet (place it logically near other v2.0 scope-removal items):
       `- **v2.0 Browse Excel/CSV export** — v1.0 Streamlit Browse remains the export surface until v1.0 sunset; v2.0 Browse is view-only by design choice (2026-04-26 user decision). The v1.0 export_dialog component remains in place and is NOT touched, copied, or imported by app_v2/.`

    3) **Locate the "Key Decisions" table** (Markdown table with three columns: Decision | Rationale | Status/Note). Append this new row at the end of the table:
       `| Drop v2.0 Browse export to keep the port view-only | Simpler shell migration; v1.0 Streamlit Browse still serves the export workflow until v1.0 sunset | ⚠️ Revisit at v1.0 sunset planning |`

    Do NOT modify the "## Project" intro paragraph (which mentions "export" as part of the original product vision — that statement is still true; the v1.0 surface delivers it).
    Do NOT modify the v1.0 milestone description that lists "Excel (openpyxl) and CSV export" as a v1.0 deliverable.
  </action>
  <verify>
    <automated>! grep -qE "^\- \[ \] Excel \+ CSV export" .planning/PROJECT.md && grep -q "v2.0 Browse Excel/CSV export" .planning/PROJECT.md && grep -q "Drop v2.0 Browse export to keep the port view-only" .planning/PROJECT.md && grep -q "v1.0 Streamlit Browse still serves the export workflow" .planning/PROJECT.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "^\- \[ \] Excel \+ CSV export" .planning/PROJECT.md` returns nothing (line removed from Browse carry-over)
    - `grep -q "v2.0 Browse Excel/CSV export" .planning/PROJECT.md` succeeds (Out of Scope entry added)
    - `grep -q "Drop v2.0 Browse export to keep the port view-only" .planning/PROJECT.md` succeeds (Key Decisions row added)
    - `grep -q "Revisit at v1.0 sunset planning" .planning/PROJECT.md` succeeds (rationale + status note present)
    - The v1.0 milestone description still mentions Excel export as a v1.0 deliverable (verify v1.0 history preserved): `grep -q "Excel (openpyxl)" .planning/PROJECT.md` succeeds
  </acceptance_criteria>
  <done>PROJECT.md no longer claims v2.0 Browse will deliver export; Out of Scope section and Key Decisions table reflect the removal with the locked rationale.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This plan is documentation-only; no code, no inputs, no I/O. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Tampering (planning artifacts) | REQUIREMENTS.md / ROADMAP.md / PROJECT.md | accept | Documentation drift is detected by Plan 04-04's invariant guards (no `import openpyxl` / `import csv` in app_v2/ — if the docs are reverted but the code stays clean, the invariants still hold). Git history provides full audit trail; commits are signed via gsd-tools commit pipeline. |
| T-04-01-02 | Repudiation (who decided to drop export) | CONTEXT.md decision record | accept | CONTEXT.md D-19..D-22 captures the user decision date (2026-04-26) and rationale; this plan's edits cite the date in every prose block. |

**Severity:** All threats LOW. This is a doc-only plan; no input handling, no code paths, no security surface beyond planning hygiene.
</threat_model>

<verification>
After all three tasks complete, run the combined sanity check:

```bash
# Alias trimmed from BROWSE-V2-01 (Issue 1 fix):
grep -F '/?tab=browse' .planning/REQUIREMENTS.md && echo "FAIL: alias still present" && exit 1

# All three docs should be export-free for v2.0 Browse:
grep -l "BROWSE-V2-04" .planning/REQUIREMENTS.md .planning/ROADMAP.md && echo "FAIL: BROWSE-V2-04 still present" && exit 1
grep -l "Excel + CSV export" .planning/PROJECT.md && echo "FAIL: Browse carry-over still claims export" && exit 1

# Out of Scope entries present in all three docs:
grep -q "Excel/CSV export under v2.0 shell" .planning/REQUIREMENTS.md || (echo "FAIL: REQUIREMENTS missing Out of Scope entry" && exit 1)
grep -q "v2.0 Browse Excel/CSV export" .planning/PROJECT.md || (echo "FAIL: PROJECT missing Out of Scope entry" && exit 1)

echo "PASS: BROWSE-V2-01 alias trimmed; Phase 4 scope correctly trimmed across REQUIREMENTS, ROADMAP, PROJECT"
```
</verification>

<success_criteria>
- The literal `/?tab=browse` appears nowhere in REQUIREMENTS.md (Issue 1 fix)
- BROWSE-V2-04 appears nowhere in REQUIREMENTS.md or ROADMAP.md (grep -c returns 0 in both files)
- "Out of Scope" entry with the locked rationale present in REQUIREMENTS.md and PROJECT.md
- ROADMAP.md Phase 4 has exactly 3 success criteria (filter swap + sticky header, caps mirror v1.0, URL round-trip)
- ROADMAP.md Phase 4 Requirements line lists exactly: BROWSE-V2-01, BROWSE-V2-02, BROWSE-V2-03, BROWSE-V2-05
- REQUIREMENTS.md Totals: 45 v2.0 reqs, Phase 4 mapped = 4
- PROJECT.md Key Decisions table contains the export-drop row with "Revisit at v1.0 sunset planning"
- All commits authored via gsd-tools commit pipeline
</success_criteria>

<output>
After completion, create `.planning/phases/04-browse-tab-port/04-01-SUMMARY.md` documenting:
- The exact lines removed/added in each file (cite line numbers from the original files)
- Confirmation that downstream Plans 04-02..04-04 can now use `requirements: [BROWSE-V2-01, BROWSE-V2-02, BROWSE-V2-03, BROWSE-V2-05]` without conflict with REQUIREMENTS.md
- Confirmation that the `/?tab=browse` alias was trimmed from BROWSE-V2-01 (Issue 1 fix from plan-checker)
- Note that this plan's `requirements: [BROWSE-V2-01, BROWSE-V2-04]` is the unique exception — Plan 04-01 claims BROWSE-V2-01 because it edits the requirement's wording (alias trim) and BROWSE-V2-04 because it removes that requirement from scope (a legitimate "implementation" of D-20)
</output>
</output>
