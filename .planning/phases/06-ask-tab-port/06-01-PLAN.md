---
phase: 06-ask-tab-port
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - ASK-V2-03
  - ASK-V2-04
  - ASK-V2-05
must_haves:
  truths:
    - "REQUIREMENTS.md ASK-V2-03 no longer mentions a Regenerate button"
    - "REQUIREMENTS.md ASK-V2-04 is moved out of the active 'Ask Tab (Port)' section into 'Out of Scope' (or merged into ASK-V2-F01)"
    - "REQUIREMENTS.md ASK-V2-05 no longer mentions 'global sidebar' placement or the OpenAI dismissible alert banner"
    - "ROADMAP.md Phase 6 Success Criterion #3 no longer claims OpenAI banner appearance/dismissal/clearing"
    - "REQUIREMENTS.md Traceability table reflects ASK-V2-04 status accurately (not 'Phase 6 / Pending' anymore)"
    - "Active v2.0 requirement totals stay reconciled (49 mapped reqs after ASK-V2-04 leaves the active list)"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Post-deviation Phase 6 spec"
      contains: "ASK-V2-03"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 6 Goal + Success Criteria reflecting D-11/D-12/D-18"
      contains: "Phase 6: Ask Tab Port"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/ROADMAP.md"
      via: "Phase 6 success criterion #3 must agree with the post-deviation ASK-V2-05 wording"
      pattern: "Switching the backend selector"
---

<objective>
Apply the three Phase 6 spec deviations to `.planning/REQUIREMENTS.md` and reconcile `.planning/ROADMAP.md` Phase 6 Success Criterion #3 so downstream plans (06-02..06-06) build against the corrected spec — not the obsolete one. This is a docs-only plan; zero production code changes.

Purpose: User locked D-05 (no history), D-11 (no Regenerate), D-12 (Ask-page-only LLM selector), D-18 (no OpenAI banner). The requirements document still claims all four. Subsequent implementation plans must reference the post-deviation IDs, and verification (post-execute) must not flag the implementation against the obsolete spec.

Output:
- Updated `.planning/REQUIREMENTS.md` (ASK-V2-03 trimmed; ASK-V2-04 moved to Out of Scope; ASK-V2-05 trimmed; Totals + Traceability table reconciled)
- Updated `.planning/ROADMAP.md` (Phase 6 Success Criterion #3 rewritten to drop OpenAI-banner sub-claims)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-ask-tab-port/06-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply ASK-V2-03 / ASK-V2-04 / ASK-V2-05 deviations to REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (the file being edited — see lines 79-86 "Ask Tab (Port)", lines 88-96 "Future Requirements", lines 98-108 "Out of Scope", lines 158-165 traceability rows for ASK-V2-01..08, lines 167-170 Totals)
    - .planning/phases/06-ask-tab-port/06-CONTEXT.md ("Spec deviations" table at the bottom of `<decisions>`, plus D-05 / D-11 / D-12 / D-18 for the rationale)
  </read_first>
  <action>
Make exactly four edits to `.planning/REQUIREMENTS.md`:

(1) **Replace ASK-V2-03 bullet** (currently at line 81). Delete the trailing "Regenerate button above the summary re-invokes the agent with the same question + confirmed params via HTMX." sentence. New full bullet text (replace verbatim):

```
- [ ] **ASK-V2-03**: Answer panel renders: result table (via `<table>` with same TextColumn-only rule), plain-text LLM summary (escaped, displayed in a `<div>`), collapsed SQL expander using Bootstrap `<details>` with the validated/limited SQL inside a `<code>` block. (Phase 6 D-11: no Regenerate button — editing the textarea and clicking Run is the regeneration mechanism.)
```

(2) **Remove ASK-V2-04 from the active "Ask Tab (Port)" section** (currently at line 82). Delete the bullet entirely. Then add a new entry under "Future Requirements (Deferred to later milestones)" (after `ASK-V2-F01` at line 96) — actually since ASK-V2-F01 already exists for "Persistent NL history across sessions", merge ASK-V2-04 into ASK-V2-F01 by appending a parenthetical attribution to the existing ASK-V2-F01 bullet. Replace the existing ASK-V2-F01 bullet with this exact text:

```
- [ ] **ASK-V2-F01**: Persistent NL history across sessions — also subsumes the original ASK-V2-04 session-history-panel scope (max 50 entries LRU, signed cookie or app.state) which was removed from active Phase 6 scope per D-05 (Phase 6 ships zero history surface area; current scope: single-shot Q→A only).
```

(3) **Replace ASK-V2-05 bullet** (currently at line 83). Drop both the "global sidebar (top-right of the Bootstrap nav, a `<select>` with Ollama/OpenAI options)" placement clause AND the entire OpenAI dismissible-alert banner clause. New full bullet text (replace verbatim):

```
- [ ] **ASK-V2-05**: LLM backend selector lives in a small page-header strip at the top of the Ask page (`/ask`) ONLY — NOT in the global navbar (Phase 6 D-12). The selector is a Bootstrap dropdown labelled `"LLM: Ollama ▾"` / `"LLM: OpenAI ▾"` (active backend's display name baked into the visible label) with two `<button class="dropdown-item">` entries (one per configured backend). Backend preference is stored in a plain unsigned cookie `pbm2_llm` (`Path=/; SameSite=Lax; Max-Age=31536000; HttpOnly`; no `Secure` because intranet HTTP); validated on read against `settings.llms[].name`, falling back silently to `settings.app.default_llm` on any invalid value (Phase 6 D-15). Cookie effect is GLOBAL across the app — AI Summary on Overview also picks up the cookie via the shared `llm_resolver` (Phase 6 D-17). NO OpenAI sensitivity-warning banner anywhere (Phase 6 D-18 — the visible dropdown label IS the affordance).
```

(4) **Reconcile Totals + Traceability table.** Two sub-edits:

  (4a) Line 158 currently reads `| ASK-V2-04 | Phase 6 | Pending |` — change to `| ASK-V2-04 | (deferred — see ASK-V2-F01) | Out of Scope |` so the table doesn't claim ASK-V2-04 is in Phase 6.

  (4b) Lines 167-170 ("Totals") currently say `**51** (... + 8 ASK-V2)` and `Mapped to phases: 45 / 45`. Update to:

```
**Totals:**
- v2.0 Requirements: **50** (9 INFRA + 6 OVERVIEW + 4 FILTER + 8 CONTENT + 7 SUMMARY + 4 BROWSE-V2 + 6 OVERVIEW-V2 + 7 ASK-V2; ASK-V2-04 moved to Out of Scope per Phase 6 D-05)
- Deferred: 7 (ASK-V2-F01 now subsumes the former ASK-V2-04 scope)
- Mapped to phases: 44 / 44 (Phase 1: 9, Phase 2: 10, Phase 3: 15, Phase 4: 4, Phase 5: 6, Phase 6: 7)
```

Note: Phase 5 had `Mapped to phases: 45 / 45` with a "Phase 5: 8" claim that was actually 6 reqs (OVERVIEW-V2-01..06). The Totals re-derivation here just adds Phase 6 = 7 (ASK-V2-01, -02, -03, -05, -06, -07, -08; ASK-V2-04 removed) and corrects Phase 5 = 6.

Do not introduce any other edits. Do not touch any v1.0 archive references.
  </action>
  <verify>
    <automated>
grep -c "Regenerate button above the summary" .planning/REQUIREMENTS.md  # MUST be 0
grep -c "global sidebar (top-right of the Bootstrap nav" .planning/REQUIREMENTS.md  # MUST be 0
grep -c "dismissible Bootstrap alert banner" .planning/REQUIREMENTS.md  # MUST be 0
grep -cE "^- \[ \] \*\*ASK-V2-04\*\*: Session history panel" .planning/REQUIREMENTS.md  # MUST be 0 (removed from active section)
grep -cE "^- \[ \] \*\*ASK-V2-F01\*\*:.*subsumes the original ASK-V2-04" .planning/REQUIREMENTS.md  # MUST be 1
grep -cE "^- \[ \] \*\*ASK-V2-05\*\*: LLM backend selector lives in a small page-header strip" .planning/REQUIREMENTS.md  # MUST be 1
grep -cE "no Regenerate button" .planning/REQUIREMENTS.md  # MUST be >=1
grep -cE "ASK-V2-04 \| \(deferred" .planning/REQUIREMENTS.md  # MUST be 1
grep -cE "v2\.0 Requirements: \*\*50\*\*" .planning/REQUIREMENTS.md  # MUST be 1
    </automated>
  </verify>
  <done>
All 9 grep assertions above evaluate as documented. The Phase 6 active requirements list contains exactly 7 ASK-V2-* bullets (01, 02, 03, 05, 06, 07, 08); ASK-V2-04 is gone from the active list and folded into ASK-V2-F01.
  </done>
</task>

<task type="auto">
  <name>Task 2: Reconcile ROADMAP.md Phase 6 Success Criterion #3</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (the file being edited — Phase 6 block at lines 121-132; Success Criteria list at lines 125-130)
    - .planning/phases/06-ask-tab-port/06-CONTEXT.md (D-12 / D-18 — selector lives on Ask page only; no OpenAI banner anywhere)
  </read_first>
  <action>
Replace ROADMAP.md line 128 — the obsolete success criterion #3:

```
  3. Switching the backend selector to OpenAI shows the data-sensitivity alert banner; the selected backend persists across page refreshes (cookie); switching back to Ollama clears the banner
```

with the post-deviation criterion (verbatim replacement, including the leading "  3." indent that matches the surrounding numbered list):

```
  3. The Ask page header bar shows an "LLM: Ollama ▾" / "LLM: OpenAI ▾" dropdown that lets the user switch backends; the selected backend persists across page refreshes via the `pbm2_llm` cookie and the dropdown label reflects the active backend on every page render. (Phase 6 D-12 + D-18: selector is Ask-page-only; no OpenAI sensitivity-warning banner anywhere — the visible dropdown label is the affordance.)
```

Make no other ROADMAP.md edits in this task. (The Phase 6 Plans subsection currently says "**Plans**: TBD" — that gets updated at the END of phase planning by the planner's existing `update_roadmap` step, not here.)
  </action>
  <verify>
    <automated>
grep -c "shows the data-sensitivity alert banner" .planning/ROADMAP.md  # MUST be 0
grep -c "switching back to Ollama clears the banner" .planning/ROADMAP.md  # MUST be 0
grep -cE "Phase 6 D-12 \+ D-18" .planning/ROADMAP.md  # MUST be 1
grep -cE "LLM: Ollama ▾" .planning/ROADMAP.md  # MUST be 1
grep -c "Phase 6: Ask Tab Port" .planning/ROADMAP.md  # MUST be >=1 (intact)
    </automated>
  </verify>
  <done>
ROADMAP.md Phase 6 Success Criterion #3 reflects the post-deviation D-12 + D-18 reality (no OpenAI banner; Ask-page-only dropdown). All 5 grep assertions above evaluate as documented.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation-only plan — no code paths, no user inputs, no data flow | This plan ships zero new attack surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | Information Disclosure | REQUIREMENTS.md edits | accept | Spec edits do not expose secrets, PII, or sensitive infrastructure detail. Public-by-default planning artifact. |
| T-06-01-02 | Tampering | ROADMAP.md edits | accept | Single-developer + git history is the integrity control; no other contributor model in scope. |

No mitigations required. This is a docs-only plan.
</threat_model>

<verification>
- All grep assertions in both tasks evaluate as documented (zero false positives, zero false negatives).
- `git diff` shows changes ONLY in `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md`.
- No production code changed (`git diff app/ app_v2/ tests/ streamlit_app.py | wc -l` returns 0).
- Phase 6 active ASK-V2-* requirement count is exactly 7 (was 8); ASK-V2-04 appears as deferred reference inside ASK-V2-F01.
- ROADMAP Phase 6 still has 5 success criteria (count unchanged); criterion #3 is the rewritten one.
</verification>

<success_criteria>
1. Three deviations applied verbatim per CONTEXT.md spec-deviation table (ASK-V2-03 Regenerate clause dropped; ASK-V2-04 deferred via merge into ASK-V2-F01; ASK-V2-05 global-sidebar + banner clauses dropped).
2. ROADMAP Phase 6 SC#3 no longer claims OpenAI banner behavior.
3. REQUIREMENTS Totals reconciled to 50 active reqs (44 mapped) — math agrees with the per-phase breakdown.
4. Zero production-code changes.
5. Plans 06-02..06-06 can now reference post-deviation requirement IDs without spec mismatch.
</success_criteria>

<output>
After completion, create `.planning/phases/06-ask-tab-port/06-01-SUMMARY.md` summarizing:
- Each of the 4 sub-edits in REQUIREMENTS.md (line numbers before/after if helpful)
- The single ROADMAP edit (SC#3 rewrite)
- Confirmation that Plans 06-02..06-06 unblock with corrected spec
- Pointer to D-05 / D-11 / D-12 / D-18 in CONTEXT.md as the locked decisions backing each deviation
</output>
