---
phase: 01-overview-tab-auto-discover-platforms-from-html-files
plan: 05
type: execute
wave: 3
depends_on: ["01-02", "01-03"]
files_modified:
  - app_v2/templates/base.html
  - app_v2/templates/overview/index.html
  - app_v2/templates/overview/_grid.html
  - app_v2/templates/overview/_filter_bar.html
  - app_v2/templates/joint_validation/detail.html
autonomous: true
requirements: [D-JV-01, D-JV-05, D-JV-10, D-JV-11, D-JV-12, D-JV-13, D-JV-15, D-JV-17]
must_haves:
  truths:
    - "Top-nav label says 'Joint Validation' (not 'Overview'); URL stays /"
    - "templates/overview/index.html renders the three blocks (grid, count_oob, filter_badges_oob) with JV column shape"
    - "templates/overview/_grid.html renders 12 sortable column headers + Action column with two buttons per row"
    - "templates/overview/_filter_bar.html renders 6 picker_popover invocations (status, customer, ap_company, device, controller, application) with form_id='overview-filter-form'"
    - "Empty state renders the helper text 'No Joint Validations yet. Drop a Confluence export at content/joint_validation/<page_id>/index.html' when vm.rows is empty"
    - "Title cell is a link to /joint_validation/<id>; Action column has Report Link + AI Summary buttons"
    - "Report Link button is disabled when row.link is None; otherwise opens target=_blank rel=noopener noreferrer"
    - "AI Summary button hx-post to /joint_validation/<id>/summary; modal target #summary-modal-body"
    - "templates/joint_validation/detail.html renders properties table + iframe sandbox=allow-same-origin allow-popups allow-popups-to-escape-sandbox referencing /static/joint_validation/<id>/index.html"
    - "Missing fields render as blank '' (NO em-dash sentinel anywhere in JV templates)"
  artifacts:
    - path: "app_v2/templates/base.html"
      provides: "Top-nav with 'Joint Validation' label"
      contains: "Joint Validation"
    - path: "app_v2/templates/overview/index.html"
      provides: "Full-page Joint Validation listing with sortable_th macro inside grid block, 3 OOB blocks, AI Summary modal"
      contains: "block grid"
    - path: "app_v2/templates/overview/_grid.html"
      provides: "Row template with title link + Report Link button + AI Summary button"
      contains: "Report Link"
    - path: "app_v2/templates/overview/_filter_bar.html"
      provides: "6 picker_popover invocations with form_id"
      contains: "picker_popover"
    - path: "app_v2/templates/joint_validation/detail.html"
      provides: "Properties table + iframe sandbox detail page"
      contains: "sandbox=\"allow-same-origin allow-popups allow-popups-to-escape-sandbox\""
  key_links:
    - from: "app_v2/templates/overview/index.html"
      to: "app_v2/templates/overview/_grid.html"
      via: "Jinja include or block content"
      pattern: "_grid.html"
    - from: "app_v2/templates/overview/index.html"
      to: "app_v2/templates/overview/_filter_bar.html"
      via: "Jinja include"
      pattern: "_filter_bar.html"
    - from: "app_v2/templates/overview/_filter_bar.html"
      to: "app_v2/templates/browse/_picker_popover.html"
      via: "Jinja import + macro call"
      pattern: "browse/_picker_popover.html"
    - from: "app_v2/templates/overview/_grid.html"
      to: "/joint_validation/{confluence_page_id}/summary"
      via: "hx-post attribute on AI Summary button"
      pattern: "/joint_validation/.*/summary"
    - from: "app_v2/templates/joint_validation/detail.html"
      to: "/static/joint_validation/{confluence_page_id}/index.html"
      via: "iframe src attribute"
      pattern: "/static/joint_validation/"
---

<objective>
Replace the Phase 5 Platform-curated templates with Joint Validation templates. Full rewrite (NOT rename) of three `templates/overview/*.html` files; add new `templates/joint_validation/detail.html`; flip top-nav label in `templates/base.html` from "Overview" to "Joint Validation".

Purpose: Plan 04 (router rewrite) wired the routes; this plan delivers the actual HTML the routes render. Plans 04 + 05 together complete the wave-3 wiring; Plan 06 then deletes obsolete files and adds invariant tests.

Output: Five modified template files. Templates render the JV grid against the `vm: JointValidationGridViewModel` produced by Plan 02; the detail template renders against the `jv: JointValidationRow` produced by Plan 04 Task 3. After this plan, Plan 06 can run end-to-end TestClient assertions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/01-overview-tab-auto-discover-platforms-from-html-files/01-CONTEXT.md
@.planning/phases/01-overview-tab-auto-discover-platforms-from-html-files/01-RESEARCH.md
@./CLAUDE.md
@app_v2/templates/base.html
@app_v2/templates/overview/index.html
@app_v2/templates/overview/_grid.html
@app_v2/templates/overview/_filter_bar.html
@app_v2/templates/browse/_picker_popover.html
@app_v2/templates/platforms/detail.html
@app_v2/templates/summary/_success.html
@app_v2/templates/summary/_error.html
@app_v2/services/joint_validation_grid_service.py

<interfaces>
<!-- View-model contract from Plan 02 (templates render against this) -->

`vm: JointValidationGridViewModel` (passed to overview/index.html as `{{ vm }}`):
- `vm.rows: list[JointValidationRow]`  — each row has 14 fields (13 metadata + confluence_page_id)
- `vm.filter_options: dict[str, list[str]]` — keys: status, customer, ap_company, device, controller, application
- `vm.active_filter_counts: dict[str, int]` — same 6 keys
- `vm.sort_col: str` — one of "title","status","customer","model_name","ap_company","ap_model","device","controller","application","assignee","start","end" (12 sortable)
- `vm.sort_order: Literal["asc","desc"]`
- `vm.total_count: int`

`row: JointValidationRow` (each iteration of vm.rows):
- `row.confluence_page_id: str` (^\d+$)
- `row.title, row.status, row.customer, row.model_name, row.ap_company, row.ap_model, row.device, row.controller, row.application, row.assignee, row.start, row.end: str` (default "" per D-JV-05)
- `row.link: str | None` (sanitized; None → disabled Link button)

`jv: JointValidationRow` (passed to joint_validation/detail.html — same shape as row above)

`selected_filters: dict[str, list[str]]` (passed to overview/index.html and _filter_bar.html — keys = the 6 FILTERABLE_COLUMNS, values = currently-applied selections)

`active_tab: str` (passed to overview/index.html and joint_validation/detail.html — value "overview")

<!-- Picker popover macro signature (reused AS-IS from browse/_picker_popover.html) -->
- Macro `picker_popover(name, label, options, selected, form_id, hx_post, hx_target, disable_auto_commit, disabled)` — accepts kwargs
- Phase 1 calls with `name="status"`, `label="Status"`, `options=vm.filter_options.status`, `selected=selected_filters.status`, `form_id="overview-filter-form"`, `hx_post="/overview/grid"`, `hx_target="#overview-grid"`, `disable_auto_commit=False`, `disabled=(vm.total_count == 0)`

<!-- Summary partial contract (parameterized in Plan 01 Task 2) -->
- `summary/_success.html` and `summary/_error.html` accept `entity_id`, `summary_url`, `target_id`, `summary_html`, `backend_name`, `cached_age_s`/`reason`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip top-nav label in templates/base.html and rewrite templates/overview/_filter_bar.html</name>
  <files>app_v2/templates/base.html, app_v2/templates/overview/_filter_bar.html</files>
  <read_first>
    - app_v2/templates/base.html (lines 30-60 — current `Overview` nav-link)
    - app_v2/templates/overview/_filter_bar.html (current Phase 5 file — 6 picker_popover invocations + Clear-all link)
    - app_v2/templates/browse/_picker_popover.html (macro signature with all kwargs — DO NOT modify; reused AS-IS)
    - .planning/phases/01-overview-tab-auto-discover-platforms-from-html-files/01-CONTEXT.md (D-JV-01 — label "Joint Validation" verbatim; D-JV-11 — six filters)
  </read_first>
  <action>
**Step 1 — Modify `app_v2/templates/base.html` top-nav label (D-JV-01).**

Find the existing nav-link block (around line 38-43):

```
<a class="nav-link {% if active_tab == 'overview' %}active{% endif %}"
   {% if active_tab == 'overview' %}aria-current="page"{% endif %}
   href="/">
  <i class="bi bi-list-ul"></i> Overview
</a>
```

Replace ONLY the visible label text `Overview` with `Joint Validation`. Do NOT change the `href="/"`, the `active_tab == 'overview'` comparison, or the icon. Result:

```
<a class="nav-link {% if active_tab == 'overview' %}active{% endif %}"
   {% if active_tab == 'overview' %}aria-current="page"{% endif %}
   href="/">
  <i class="bi bi-list-ul"></i> Joint Validation
</a>
```

Do NOT touch the Browse or Ask nav entries. Do NOT change the navbar-brand or any other base.html structure.

**Step 2 — Rewrite `app_v2/templates/overview/_filter_bar.html`** (full rewrite per D-JV-06).

The file structure mirrors Phase 5 byte-for-byte except for the column-context: 6 picker_popover invocations inside a `.overview-filter-bar` panel + a Clear-all link. The macro is reused AS-IS from `browse/_picker_popover.html`. Use exactly this content:

```
{# Joint Validation filter bar — Phase 1 Plan 05.
   Six popover-checklist filters (D-JV-11) reusing the Phase 4 picker_popover
   macro AS-IS with form_id='overview-filter-form'. D-15b auto-commit + 250ms
   debounce + form-association + OOB filter-badge swap all carry forward. #}
{% from "browse/_picker_popover.html" import picker_popover %}

<div class="overview-filter-bar panel" id="overview-filter-bar">
  <form id="overview-filter-form"
        hx-post="/overview/grid"
        hx-target="#overview-grid"
        hx-swap="outerHTML"
        hx-trigger="change delay:250ms from:find input[type=checkbox]"
        hx-include="this">
    <input type="hidden" name="sort" value="{{ vm.sort_col | e }}">
    <input type="hidden" name="order" value="{{ vm.sort_order | e }}">

    {{ picker_popover(
        name="status",
        label="Status",
        options=vm.filter_options["status"],
        selected=selected_filters["status"],
        form_id="overview-filter-form",
        hx_post="/overview/grid",
        hx_target="#overview-grid",
        disable_auto_commit=False,
        disabled=(vm.total_count == 0 and (vm.filter_options["status"] | length) == 0),
    ) }}

    {{ picker_popover(
        name="customer",
        label="Customer",
        options=vm.filter_options["customer"],
        selected=selected_filters["customer"],
        form_id="overview-filter-form",
        hx_post="/overview/grid",
        hx_target="#overview-grid",
        disable_auto_commit=False,
        disabled=(vm.total_count == 0 and (vm.filter_options["customer"] | length) == 0),
    ) }}

    {{ picker_popover(
        name="ap_company",
        label="AP Company",
        options=vm.filter_options["ap_company"],
        selected=selected_filters["ap_company"],
        form_id="overview-filter-form",
        hx_post="/overview/grid",
        hx_target="#overview-grid",
        disable_auto_commit=False,
        disabled=(vm.total_count == 0 and (vm.filter_options["ap_company"] | length) == 0),
    ) }}

    {{ picker_popover(
        name="device",
        label="Device",
        options=vm.filter_options["device"],
        selected=selected_filters["device"],
        form_id="overview-filter-form",
        hx_post="/overview/grid",
        hx_target="#overview-grid",
        disable_auto_commit=False,
        disabled=(vm.total_count == 0 and (vm.filter_options["device"] | length) == 0),
    ) }}

    {{ picker_popover(
        name="controller",
        label="Controller",
        options=vm.filter_options["controller"],
        selected=selected_filters["controller"],
        form_id="overview-filter-form",
        hx_post="/overview/grid",
        hx_target="#overview-grid",
        disable_auto_commit=False,
        disabled=(vm.total_count == 0 and (vm.filter_options["controller"] | length) == 0),
    ) }}

    {{ picker_popover(
        name="application",
        label="Application",
        options=vm.filter_options["application"],
        selected=selected_filters["application"],
        form_id="overview-filter-form",
        hx_post="/overview/grid",
        hx_target="#overview-grid",
        disable_auto_commit=False,
        disabled=(vm.total_count == 0 and (vm.filter_options["application"] | length) == 0),
    ) }}

    <a href="/overview"
       class="btn btn-link btn-sm ms-2"
       hx-get="/overview"
       hx-target="body"
       hx-push-url="true">
      Clear all
    </a>
  </form>
</div>
```

The 6 macro invocation block IS the entire filter bar; no other surrounding chrome. This matches Phase 5 byte-for-byte except for the data-source comments. The `disabled` kwarg uses the Phase 5 D-JV-17 empty-state pattern: when there are no rows AND no filter options, the popover is disabled.

**Why exactly six picker_popover invocations:** D-JV-11 locks `status, customer, ap_company, device, controller, application` — same set as Phase 5. Other columns (title, model_name, ap_model, assignee) are sortable but not filterable per D-JV-11 + Deferred Ideas in CONTEXT.md.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import jinja2; e = jinja2.Environment(loader=jinja2.FileSystemLoader('app_v2/templates'), autoescape=True); e.get_template('overview/_filter_bar.html'); e.get_template('base.html'); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '> Joint Validation</a>' app_v2/templates/base.html` returns at least `1` OR `grep -F 'bi bi-list-ul"></i> Joint Validation' app_v2/templates/base.html` finds the new label
    - `grep -c '> Overview</a>' app_v2/templates/base.html` returns `0` (old label gone)
    - `grep -c "active_tab == 'overview'" app_v2/templates/base.html` is unchanged from before this task (URL/route logic preserved)
    - File `app_v2/templates/overview/_filter_bar.html` exists and is the rewritten content
    - `grep -c 'picker_popover(' app_v2/templates/overview/_filter_bar.html` returns at least `6`
    - `grep -F 'name="status"' app_v2/templates/overview/_filter_bar.html` finds occurrence
    - `grep -F 'name="customer"' app_v2/templates/overview/_filter_bar.html` finds occurrence
    - `grep -F 'name="ap_company"' app_v2/templates/overview/_filter_bar.html` finds occurrence
    - `grep -F 'name="device"' app_v2/templates/overview/_filter_bar.html` finds occurrence
    - `grep -F 'name="controller"' app_v2/templates/overview/_filter_bar.html` finds occurrence
    - `grep -F 'name="application"' app_v2/templates/overview/_filter_bar.html` finds occurrence
    - `grep -F 'form_id="overview-filter-form"' app_v2/templates/overview/_filter_bar.html` finds at least 6 occurrences (one per popover call)
    - `grep -F 'has_content' app_v2/templates/overview/_filter_bar.html` returns no matches (Platform-only filter removed)
    - `grep -F '"—"' app_v2/templates/overview/_filter_bar.html` returns no matches (D-JV-05)
    - Both templates compile under Jinja2 (verify command above)
  </acceptance_criteria>
  <done>Top-nav label flipped to "Joint Validation"; URL stays /; _filter_bar.html invokes the picker_popover macro six times with the correct keyword arguments; Jinja2 compile succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite templates/overview/index.html and templates/overview/_grid.html</name>
  <files>app_v2/templates/overview/index.html, app_v2/templates/overview/_grid.html</files>
  <read_first>
    - app_v2/templates/overview/index.html (current Phase 5 file — sortable_th macro at lines 103-123, three blocks `grid` / `count_oob` / `filter_badges_oob` at lines 100-225, AI Summary modal at lines 160-194; preserve overall structure, replace Platform-curated bits)
    - app_v2/templates/overview/_grid.html (current Phase 5 file — row iteration; Link button + AI Summary button shapes; em-dash maybe() macro to be removed per D-JV-05)
    - app_v2/services/joint_validation_grid_service.py (Plan 02 — JointValidationRow shape: 12 sortable fields + link)
    - .planning/phases/01-overview-tab-auto-discover-platforms-from-html-files/01-RESEARCH.md (Phase 5 Reuse Map for index.html / _grid.html; Pitfall 8 — sortable_th MUST be defined INSIDE the grid block for jinja2-fragments)
    - .planning/phases/01-overview-tab-auto-discover-platforms-from-html-files/01-CONTEXT.md (D-JV-05 blank not em-dash; D-JV-10 sort cycle asc→desc→asc; D-JV-15 two row buttons; D-JV-17 empty state copy verbatim)
  </read_first>
  <action>
**Step 1 — Rewrite `app_v2/templates/overview/index.html`** (full rewrite per D-JV-06).

Use this exact structure (mirror Phase 5 byte-stable except for: column headers, row template include, modal target ids, empty-state copy):

```
{% extends "base.html" %}
{% block content %}
<div class="shell">
  <div class="page-head mb-3">
    <h1 class="page-title">Joint Validation</h1>
  </div>

  {% include "overview/_filter_bar.html" %}

  <div id="overview-filter-badges-wrapper">
    {# OOB filter badges block — rendered both in full page and in HTMX swaps #}
    {% block filter_badges_oob %}
      <div id="overview-filter-badges" hx-swap-oob="true">
        {% for col, count in vm.active_filter_counts.items() %}
          {% if count > 0 %}
            <span class="badge bg-secondary me-1">
              {{ col | e }}: {{ count | e }}
            </span>
          {% endif %}
        {% endfor %}
      </div>
    {% endblock %}
  </div>

  <div class="panel">
    <div class="panel-body p-0">
      {# OOB count caption #}
      {% block count_oob %}
        <div id="overview-count" class="px-3 pt-2 small text-muted" hx-swap-oob="true">
          {{ vm.total_count | e }} {{ "entry" if vm.total_count == 1 else "entries" }}
        </div>
      {% endblock %}

      {# Main grid block — rendered both in full page and in HTMX fragment swaps.
         IMPORTANT (Pitfall 8): sortable_th macro MUST be defined INSIDE this
         block, not at the template top, otherwise jinja2-fragments
         block_names=['grid', ...] rendering loses the macro. #}
      {% block grid %}
        {% macro sortable_th(col, label) %}
          {%- set is_active = (vm.sort_col == col) -%}
          {%- set next_order = "desc" if (is_active and vm.sort_order == "asc") else "asc" -%}
          <th>
            <button type="button"
                    class="btn btn-link btn-sm p-0 text-decoration-none {% if is_active %}fw-bold{% endif %}"
                    hx-post="/overview/grid"
                    hx-include="#overview-filter-form"
                    hx-vals='{"sort": "{{ col | e }}", "order": "{{ next_order | e }}"}'
                    hx-target="#overview-grid"
                    hx-swap="outerHTML">
              {{ label | e }}
              {% if is_active %}
                <i class="bi bi-caret-{% if vm.sort_order == 'asc' %}up{% else %}down{% endif %}-fill"></i>
              {% endif %}
            </button>
          </th>
        {% endmacro %}

        <div id="overview-grid">
          {% include "overview/_grid.html" %}
        </div>
      {% endblock %}
    </div>
  </div>

  {# AI Summary modal (D-OV-15 → D-JV-15 inherits modal pattern). Body slot
     #summary-modal-body is the hx-target for AI Summary button POSTs. The
     show.bs.modal listener resets the slot to a placeholder spinner so each
     open starts fresh. #}
  <div class="modal fade" id="summary-modal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog modal-lg modal-dialog-scrollable">
      <div class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title">AI Summary</h5>
          <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
        </div>
        <div class="modal-body" id="summary-modal-body">
          <div class="text-center text-muted py-4">
            <div class="spinner-border" role="status"></div>
            <div class="mt-2">Generating…</div>
          </div>
        </div>
      </div>
    </div>
  </div>
  <script>
    document.getElementById('summary-modal').addEventListener('show.bs.modal', function () {
      var slot = document.getElementById('summary-modal-body');
      slot.innerHTML = '<div class="text-center text-muted py-4"><div class="spinner-border" role="status"></div><div class="mt-2">Generating…</div></div>';
    });
  </script>

</div>
{% endblock %}
```

Key invariants the executor must preserve:
- The `sortable_th` macro is defined INSIDE `{% block grid %}` (Pitfall 8 — block-local macro visibility for jinja2-fragments).
- The three OOB blocks (`filter_badges_oob`, `count_oob`, `grid`) are at the TOP level of the content block so `block_names=["grid", "count_oob", "filter_badges_oob"]` rendering picks them up cleanly.
- The `vm.active_filter_counts` iteration matches the Phase 5 OOB filter-badge swap pattern (only show badges where count > 0).
- The count caption uses `vm.total_count` (not `len(vm.rows)` — `total_count` is the explicit field on the view-model).
- The empty-state row count uses singular "entry" for 1, plural "entries" otherwise (D-JV-17 says "0 entries").
- Modal target slot id is `summary-modal-body` (Phase 5 D-OV-15 reuse).

**Step 2 — Rewrite `app_v2/templates/overview/_grid.html`** (full rewrite per D-JV-06).

Use this exact content:

```
{# Joint Validation grid — Phase 1 Plan 05.
   D-JV-05: missing fields render as blank "" (NOT em-dash sentinel).
   D-JV-15: two row buttons (Report Link + AI Summary).
   Title cell IS the link to /joint_validation/<id> (no separate View button).

   12 sortable column headers via sortable_th macro defined in index.html grid
   block. Action column is the 13th column and is NOT sortable. #}
<table class="table table-sm overview-grid sticky-top-table" id="overview-grid-table">
  <thead>
    <tr>
      {{ sortable_th("title", "Title") }}
      {{ sortable_th("status", "Status") }}
      {{ sortable_th("customer", "Customer") }}
      {{ sortable_th("model_name", "Model Name") }}
      {{ sortable_th("ap_company", "AP Company") }}
      {{ sortable_th("ap_model", "AP Model") }}
      {{ sortable_th("device", "Device") }}
      {{ sortable_th("controller", "Controller") }}
      {{ sortable_th("application", "Application") }}
      {{ sortable_th("assignee", "담당자") }}
      {{ sortable_th("start", "Start") }}
      {{ sortable_th("end", "End") }}
      <th class="text-end">Actions</th>
    </tr>
  </thead>
  <tbody>
    {% if vm.rows | length == 0 %}
      <tr>
        <td colspan="13" class="text-center text-muted py-4">
          <strong>No Joint Validations yet.</strong>
          Drop a Confluence export at <code>content/joint_validation/&lt;page_id&gt;/index.html</code>.
        </td>
      </tr>
    {% else %}
      {% for row in vm.rows %}
        <tr>
          <td>
            <a href="/joint_validation/{{ row.confluence_page_id | e }}"
               class="text-decoration-none">
              {{ row.title | e }}
            </a>
          </td>
          <td>{{ row.status | e }}</td>
          <td>{{ row.customer | e }}</td>
          <td>{{ row.model_name | e }}</td>
          <td>{{ row.ap_company | e }}</td>
          <td>{{ row.ap_model | e }}</td>
          <td>{{ row.device | e }}</td>
          <td>{{ row.controller | e }}</td>
          <td>{{ row.application | e }}</td>
          <td>{{ row.assignee | e }}</td>
          <td>{{ row.start | e }}</td>
          <td>{{ row.end | e }}</td>
          <td class="text-end">
            {# Report Link button (D-JV-15) — disabled when row.link is None
               (sanitizer rejected scheme OR raw value was empty). #}
            {% if row.link %}
              <a href="{{ row.link | e }}"
                 class="btn btn-sm btn-outline-secondary"
                 target="_blank" rel="noopener noreferrer"
                 aria-label="Open report link for {{ row.title | e }}">
                <i class="bi bi-link-45deg"></i> Link
              </a>
            {% else %}
              <button type="button"
                      class="btn btn-sm btn-outline-secondary"
                      disabled
                      aria-label="No report link available">
                <i class="bi bi-link-45deg"></i> Link
              </button>
            {% endif %}

            {# AI Summary button (D-JV-15) — opens modal; HTMX-fetches summary
               into #summary-modal-body. Reuses Phase 3's _success.html /
               _error.html partials (parameterized in Plan 01 Task 2). #}
            <button type="button"
                    class="btn btn-sm btn-outline-primary ms-1"
                    data-bs-toggle="modal"
                    data-bs-target="#summary-modal"
                    hx-post="/joint_validation/{{ row.confluence_page_id | e }}/summary"
                    hx-target="#summary-modal-body"
                    hx-swap="innerHTML"
                    hx-headers='{"HX-Target": "summary-modal-body"}'
                    aria-label="Generate AI summary for {{ row.title | e }}">
              <i class="bi bi-magic"></i> AI
            </button>
          </td>
        </tr>
      {% endfor %}
    {% endif %}
  </tbody>
</table>
```

Key invariants:
- 12 `sortable_th(...)` calls in the header — exactly the 12 fields in `SORTABLE_COLUMNS` (Plan 02). Action column header is plain `<th>`, not sortable.
- 13 `<td>` per data row to match the 13 columns.
- Empty-state `<tr>` has `colspan="13"` (matches column count).
- Empty-state copy is verbatim per D-JV-17: "**No Joint Validations yet.** Drop a Confluence export at `content/joint_validation/<page_id>/index.html`".
- NO em-dash sentinel anywhere (D-JV-05). Every `{{ row.<field> | e }}` renders blank `""` if the field is empty.
- Title cell is `<a href="/joint_validation/{id}">` (D-JV-15 — title cell IS the detail-page link; NO separate View button).
- Report Link button uses `<a target="_blank" rel="noopener noreferrer">` when `row.link` is truthy; falls back to a `<button disabled>` placeholder otherwise.
- AI Summary button: `hx-post="/joint_validation/{id}/summary"` → `#summary-modal-body`. Bootstrap modal trigger via `data-bs-toggle="modal"`.
- Every dynamic value is `| e` explicit-escape (defense-in-depth on top of Jinja2 autoescape).
  </action>
  <verify>
    <automated>.venv/bin/python -c "import jinja2; e = jinja2.Environment(loader=jinja2.FileSystemLoader('app_v2/templates'), autoescape=True); e.get_template('overview/index.html'); e.get_template('overview/_grid.html'); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File `app_v2/templates/overview/index.html` exists; compiles under Jinja2
    - File `app_v2/templates/overview/_grid.html` exists; compiles under Jinja2
    - `grep -c '{% block grid %}' app_v2/templates/overview/index.html` returns at least `1`
    - `grep -c '{% block count_oob %}' app_v2/templates/overview/index.html` returns at least `1`
    - `grep -c '{% block filter_badges_oob %}' app_v2/templates/overview/index.html` returns at least `1`
    - `grep -c '{% macro sortable_th' app_v2/templates/overview/index.html` returns exactly `1`
    - The macro definition appears INSIDE the grid block: verify with `awk '/{% block grid %}/,/{% endblock %}/' app_v2/templates/overview/index.html | grep -c 'macro sortable_th'` returns `1`
    - `grep -c 'sortable_th(' app_v2/templates/overview/_grid.html` returns at least `12` (one per sortable column)
    - `grep -F '"title", "Title"' app_v2/templates/overview/_grid.html` finds the title sortable_th call
    - `grep -F '"담당자"' app_v2/templates/overview/_grid.html` finds the Korean assignee column header
    - `grep -F 'colspan="13"' app_v2/templates/overview/_grid.html` finds the empty-state row span
    - `grep -F 'No Joint Validations yet.' app_v2/templates/overview/_grid.html` finds the verbatim empty-state copy
    - `grep -F 'content/joint_validation/&lt;page_id&gt;/index.html' app_v2/templates/overview/_grid.html` finds the helper-text path
    - `grep -F '/joint_validation/{{ row.confluence_page_id' app_v2/templates/overview/_grid.html` finds the title-link template variable
    - `grep -F 'target="_blank"' app_v2/templates/overview/_grid.html` finds the Report Link target attr
    - `grep -F 'rel="noopener noreferrer"' app_v2/templates/overview/_grid.html` finds the security relationship
    - `grep -F 'hx-post="/joint_validation/' app_v2/templates/overview/_grid.html` finds the AI Summary button hx-post
    - `grep -F 'hx-target="#summary-modal-body"' app_v2/templates/overview/_grid.html` finds the modal target
    - `grep -F 'data-bs-toggle="modal"' app_v2/templates/overview/_grid.html` finds the Bootstrap modal trigger
    - `grep -F '"—"' app_v2/templates/overview/_grid.html` returns no matches (D-JV-05 — no em-dash)
    - `grep -F 'has_content' app_v2/templates/overview/_grid.html` returns no matches (Platform-only field gone)
    - `grep -F 'platform_id' app_v2/templates/overview/_grid.html` returns no matches (Platform terminology gone)
    - `grep -c '| e' app_v2/templates/overview/_grid.html` returns at least `13` (every dynamic value escaped)
    - `grep -c '| safe' app_v2/templates/overview/_grid.html` returns `0` (no unsafe rendering in JV templates)
    - `grep -c '| safe' app_v2/templates/overview/index.html` returns `0`
  </acceptance_criteria>
  <done>overview/index.html renders 3 OOB blocks with sortable_th macro inside grid block; overview/_grid.html renders 12 sortable column headers + Action column with two buttons per row; empty state matches D-JV-17 copy verbatim; no em-dash; no | safe; Jinja2 compile succeeds.</done>
</task>

<task type="auto">
  <name>Task 3: Create templates/joint_validation/detail.html</name>
  <files>app_v2/templates/joint_validation/detail.html</files>
  <read_first>
    - app_v2/templates/platforms/detail.html (Phase 3 / Phase 5 reference for properties-table-on-top pattern; do NOT modify)
    - app_v2/templates/overview/index.html (just rewritten in Task 2 — pattern reference for the panel + page-head shell)
    - .planning/phases/01-overview-tab-auto-discover-platforms-from-html-files/01-RESEARCH.md (iframe Sandbox section — exact attribute string `allow-same-origin allow-popups allow-popups-to-escape-sandbox`; Code Example 4 — full template skeleton)
    - .planning/phases/01-overview-tab-auto-discover-platforms-from-html-files/01-CONTEXT.md (D-JV-12 — properties table + iframe; D-JV-13 — static mount path; D-JV-15 — Report Link button shape)
  </read_first>
  <action>
**Create `app_v2/templates/joint_validation/detail.html`** with this exact content. The router (Plan 04 Task 3) passes `jv: JointValidationRow` and `active_tab: "overview"` in the context dict.

```
{% extends "base.html" %}
{% block content %}
<div class="shell">
  <div class="page-head mb-3">
    <h1 class="page-title">{{ jv.title | e }}</h1>
    <small class="text-muted">Confluence Page ID: <code>{{ jv.confluence_page_id | e }}</code></small>
  </div>

  {# Properties table (D-JV-12) — same Obsidian-style row shape as
     templates/platforms/detail.html. Missing fields render blank per D-JV-05. #}
  <div class="panel mb-3">
    <div class="panel-body">
      <table class="table table-sm jv-properties">
        <tbody>
          <tr><th style="width: 16ch;">Status</th><td>{{ jv.status | e }}</td></tr>
          <tr><th>Customer</th><td>{{ jv.customer | e }}</td></tr>
          <tr><th>Model Name</th><td>{{ jv.model_name | e }}</td></tr>
          <tr><th>AP Company</th><td>{{ jv.ap_company | e }}</td></tr>
          <tr><th>AP Model</th><td>{{ jv.ap_model | e }}</td></tr>
          <tr><th>Device</th><td>{{ jv.device | e }}</td></tr>
          <tr><th>Controller</th><td>{{ jv.controller | e }}</td></tr>
          <tr><th>Application</th><td>{{ jv.application | e }}</td></tr>
          <tr><th>담당자</th><td>{{ jv.assignee | e }}</td></tr>
          <tr><th>Start</th><td>{{ jv.start | e }}</td></tr>
          <tr><th>End</th><td>{{ jv.end | e }}</td></tr>
          <tr>
            <th>Report Link</th>
            <td>
              {% if jv.link %}
                <a href="{{ jv.link | e }}"
                   target="_blank" rel="noopener noreferrer"
                   class="text-decoration-none">
                  <i class="bi bi-link-45deg"></i> {{ jv.link | e }}
                </a>
              {% endif %}
            </td>
          </tr>
        </tbody>
      </table>
    </div>
  </div>

  {# Iframe sandbox (D-JV-12 + D-JV-13).
     sandbox attribute set per RESEARCH.md "iframe Sandbox" section:
       - allow-same-origin: lets parent + iframe share origin (CSS renders;
         scripts in iframe still BLOCKED because allow-scripts is absent)
       - allow-popups + allow-popups-to-escape-sandbox: external links
         (target="_blank") in the Confluence body open in a non-sandboxed tab
     CRITICAL: do NOT add allow-scripts — combining allow-scripts with
     allow-same-origin lets the framed document remove the sandbox attr
     (MDN warns explicitly against this).
     The iframe src points at the static mount (D-JV-13). #}
  <div class="panel">
    <div class="panel-body p-0">
      <iframe
        src="/static/joint_validation/{{ jv.confluence_page_id | e }}/index.html"
        sandbox="allow-same-origin allow-popups allow-popups-to-escape-sandbox"
        style="width: 100%; height: 80vh; border: 0;"
        loading="lazy"
        title="Confluence export — {{ jv.title | e }}"></iframe>
    </div>
  </div>
</div>
{% endblock %}
```

Key invariants:
- The literal sandbox attribute value is exactly `"allow-same-origin allow-popups allow-popups-to-escape-sandbox"` — three flags, space-separated, in this order.
- NO `allow-scripts` flag (Pitfall — combining with `allow-same-origin` breaks the sandbox).
- NO `allow-top-navigation` flag (would let a malicious export navigate the parent away).
- NO `allow-forms` flag (Confluence forms not in scope this phase).
- The iframe `src` is `/static/joint_validation/{id}/index.html` — must match the StaticFiles mount path in Plan 04 Task 1.
- Every dynamic value is `| e` explicit-escape — including the `iframe src` value (defense-in-depth even though `confluence_page_id` is regex-pinned at the route layer).
- Page heading uses `{{ jv.title | e }}` directly (no `or jv.confluence_page_id` fallback in template — the router constructs `JointValidationRow` with `title=parsed.title or confluence_page_id` so the fallback already happened service-side).
- Properties table uses 12 rows for the 12 metadata fields (status through Report Link). The `confluence_page_id` is in the page-head, not the properties table.
- Korean `담당자` label rendered verbatim (UTF-8 preserved through Jinja2 autoescape — `| e` escapes HTML entities, not Unicode codepoints).

**Why no script:** The iframe-loaded HTML may contain `<script>` tags (Confluence syntax-highlighter snippets). With `sandbox="allow-same-origin"` and NO `allow-scripts`, those scripts do not execute. This is acceptable per D-JV-09 ("the iframe shows the page as exported"); users who need full interactivity click the Report Link to open the live Confluence page. RESEARCH.md §"iframe Sandbox" + Pitfall 4 confirm.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import jinja2; e = jinja2.Environment(loader=jinja2.FileSystemLoader('app_v2/templates'), autoescape=True); e.get_template('joint_validation/detail.html'); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File `app_v2/templates/joint_validation/detail.html` exists; compiles under Jinja2
    - `grep -F 'sandbox="allow-same-origin allow-popups allow-popups-to-escape-sandbox"' app_v2/templates/joint_validation/detail.html` finds exactly one occurrence (literal byte-stable attribute value)
    - `grep -F 'allow-scripts' app_v2/templates/joint_validation/detail.html` returns no matches (CRITICAL — combining with allow-same-origin breaks sandbox)
    - `grep -F 'allow-top-navigation' app_v2/templates/joint_validation/detail.html` returns no matches
    - `grep -F 'allow-forms' app_v2/templates/joint_validation/detail.html` returns no matches
    - `grep -F '/static/joint_validation/{{ jv.confluence_page_id' app_v2/templates/joint_validation/detail.html` finds the iframe src path
    - `grep -F '/index.html' app_v2/templates/joint_validation/detail.html` finds at least one occurrence (the iframe-src tail)
    - `grep -F '<iframe' app_v2/templates/joint_validation/detail.html` finds exactly one occurrence
    - `grep -F 'target="_blank"' app_v2/templates/joint_validation/detail.html` finds the Report Link target
    - `grep -F 'rel="noopener noreferrer"' app_v2/templates/joint_validation/detail.html` finds the Report Link rel
    - `grep -F '<th>담당자</th>' app_v2/templates/joint_validation/detail.html` finds the Korean assignee row
    - `grep -F '"—"' app_v2/templates/joint_validation/detail.html` returns no matches (D-JV-05)
    - `grep -c '| safe' app_v2/templates/joint_validation/detail.html` returns `0` (no unsafe rendering)
    - `grep -c '| e' app_v2/templates/joint_validation/detail.html` returns at least `13` (12 fields + page heading)
  </acceptance_criteria>
  <done>joint_validation/detail.html renders properties table + iframe with the locked sandbox attribute string; iframe src references /static/joint_validation/{id}/index.html; Korean 담당자 row present; no em-dash; no | safe; no allow-scripts; Jinja2 compile succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `vm.rows[].field` (parsed Confluence content) → Jinja2 template render → user browser | Stored XSS via parsed metadata cells |
| `jv.link` (parsed `<a href>` value) → `<a href>` attribute in browser | URL injection (already sanitized in service layer; defense-in-depth at template layer via `| e`) |
| Confluence-exported HTML → `<iframe>` element → user browser | Same-origin embedded content; XSS / nav-hijack vectors |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | Stored XSS via parsed `row.title`, `row.customer`, etc. rendered in `_grid.html` and `detail.html` | mitigate | Every `{{ row.<field> }}` and `{{ jv.<field> }}` reference uses explicit `| e` filter. Jinja2 autoescape is also ON for `.html` templates by Jinja2 environment default — belt-and-suspenders. Acceptance grep enforces `| e` count + zero `| safe`. |
| T-05-02 | Information Disclosure | Stored XSS via `row.link` href (e.g., `javascript:` scheme) | mitigate | (1) Service-layer `_sanitize_link` (Plan 02) drops `javascript:`, `data:`, `vbscript:`, `file:`, `about:` schemes BEFORE the value reaches the template. (2) Template applies `| e` to the URL value as defense-in-depth (escapes `"` and `>` so the attribute cannot be broken out of). (3) `target="_blank" rel="noopener noreferrer"` prevents the new tab from accessing `window.opener`. |
| T-05-03 | Information Disclosure / Tampering | Embedded HTML in the iframe contains scripts that exfiltrate data or hijack the parent | mitigate | `sandbox="allow-same-origin allow-popups allow-popups-to-escape-sandbox"`. Without `allow-scripts`, scripts in the iframe document do not execute. Without `allow-top-navigation`, the iframe cannot navigate the parent. The acceptance grep enforces zero occurrences of `allow-scripts`, `allow-top-navigation`, `allow-forms` in the detail template. |
| T-05-04 | Spoofing / Click-jacking | Iframe content overlays the parent page UI to trick users | accept | The iframe is contained inside a Bootstrap panel with `border: 0; height: 80vh; width: 100%;` — visually framed. Sandboxed scripts cannot break out. The deployment is intranet-only with trusted authors of `content/joint_validation/`. Out of scope to add CSP `frame-ancestors`. |
| T-05-05 | Information Disclosure | Iframe scripts read parent's cookies via `same-origin` | mitigate | `allow-same-origin` does grant same-origin treatment, but WITHOUT `allow-scripts`, no JS can run inside the iframe to make requests. The combination is the deliberate trade-off recommended by MDN/web.dev for embedding trusted same-origin static HTML. |
| T-05-06 | Tampering | Iframe `src` attribute injection via `confluence_page_id` route arg | mitigate | `confluence_page_id` is regex-pinned to `^\d+$` at the route layer (Plan 04 Task 3). Even if the regex were bypassed, the `| e` filter on `{{ jv.confluence_page_id | e }}` would HTML-encode any `"` or `>` byte, preventing attribute breakout. |
| T-05-07 | DoS via huge iframe content | User clicks detail link for a 50MB Confluence export; browser stalls | accept | Browser-side problem; the StaticFiles mount supports HTTP Range requests. Out of scope to add server-side size caps. |
</threat_model>

<verification>
After all 3 tasks:
- All 4 modified templates compile under Jinja2: `python -c "import jinja2; e = jinja2.Environment(loader=jinja2.FileSystemLoader('app_v2/templates'), autoescape=True); [e.get_template(t) for t in ['base.html', 'overview/index.html', 'overview/_grid.html', 'overview/_filter_bar.html', 'joint_validation/detail.html']]; print('OK')"` exits 0
- TestClient end-to-end smoke check (with Plan 02 + 04 in place): `GET /` returns 200 with `<a href="/joint_validation/...` somewhere in the body when at least one JV folder exists; `GET /joint_validation/<id>` returns 200 with `<iframe ... sandbox="allow-same-origin allow-popups allow-popups-to-escape-sandbox"`. (Full E2E tests live in Plan 06.)
</verification>

<success_criteria>
- Top-nav label is "Joint Validation" (verbatim per D-JV-01); URL `/` unchanged
- All Phase 5 Platform-curated template artifacts gone from JV templates (no `platform_id`, no `OverviewEntity`, no `has_content`, no em-dash sentinel)
- Empty state copy verbatim per D-JV-17
- Iframe sandbox attribute exactly `"allow-same-origin allow-popups allow-popups-to-escape-sandbox"`; NO `allow-scripts`
- All dynamic values `| e` explicit-escaped; zero `| safe` filters in JV templates
- Plans 04 + 06 can render `GET /overview` and `GET /joint_validation/{id}` and run TestClient assertions
</success_criteria>

<output>
After completion, create `.planning/phases/01-overview-tab-auto-discover-platforms-from-html-files/01-05-SUMMARY.md` documenting:
- 5 template files modified (1 nav flip, 3 full rewrites, 1 new)
- LOC delta per file
- Confirmation that `| safe` count is 0 across all JV templates
- Confirmation that iframe sandbox attribute is exactly the locked 3-flag string
</output>
