---
phase: 01-overview-tab-auto-discover-platforms-from-html-files
plan: 04
type: execute
wave: 3
depends_on: ["01-02", "01-03"]
files_modified:
  - app_v2/main.py
  - app_v2/routers/overview.py
  - app_v2/routers/joint_validation.py
autonomous: true
requirements: [D-JV-01, D-JV-07, D-JV-12, D-JV-13, D-JV-14, D-JV-15, D-JV-16]
must_haves:
  truths:
    - "GET /overview returns 200 and the rendered HTML contains the JV grid (no Platform-curated artifacts visible)"
    - "GET / returns 200 and renders the JV grid (root alias preserved)"
    - "POST /overview/grid returns three OOB blocks (grid, count_oob, filter_badges_oob) and sets HX-Push-Url to canonical /overview?... URL"
    - "POST /overview/add no longer exists (returns 405 or 404)"
    - "GET /joint_validation/<numeric_id> returns 200 with properties table + iframe sandbox referencing /static/joint_validation/<id>/index.html"
    - "GET /joint_validation/<non-numeric> returns 404 (path-arg regex constraint)"
    - "POST /joint_validation/<id>/summary returns 200 (always-200 contract); rendered fragment contains hx-post=/joint_validation/<id>/summary for retry/regenerate"
    - "GET /static/joint_validation/<id>/index.html serves the file content"
    - "GET /static/joint_validation/../etc/passwd returns 404 (Starlette path normalization)"
    - "URL state shape preserved: GET /overview?status=A&customer=X&sort=start&order=desc renders filtered+sorted grid"
  artifacts:
    - path: "app_v2/main.py"
      provides: "StaticFiles mount for /static/joint_validation; lifespan mkdir for content/joint_validation; joint_validation router registered"
      contains: "joint_validation_static"
    - path: "app_v2/routers/overview.py"
      provides: "Rewritten listing routes (GET /, GET /overview, POST /overview/grid) for Joint Validation; deleted POST /overview/add and add_platform/_resolve_curated_pids/_entity_dict/_build_overview_context"
      exports: ["router"]
    - path: "app_v2/routers/joint_validation.py"
      provides: "GET /joint_validation/<id> detail route + POST /joint_validation/<id>/summary AI Summary route"
      exports: ["router"]
  key_links:
    - from: "app_v2/main.py"
      to: "/static/joint_validation"
      via: "app.mount(...) StaticFiles call"
      pattern: 'app.mount..../static/joint_validation'
    - from: "app_v2/routers/overview.py"
      to: "app_v2.services.joint_validation_grid_service"
      via: "import + call build_joint_validation_grid_view_model"
      pattern: "from app_v2.services.joint_validation_grid_service import"
    - from: "app_v2/routers/joint_validation.py"
      to: "app_v2.services.joint_validation_summary"
      via: "import + call get_or_generate_jv_summary"
      pattern: "from app_v2.services.joint_validation_summary import"
    - from: "app_v2/routers/joint_validation.py"
      to: "app_v2/templates/joint_validation/detail.html"
      via: "templates.TemplateResponse"
      pattern: "joint_validation/detail.html"
---

<objective>
Wire the data layer (Plans 02 + 03) to HTTP routes. Rewrite `routers/overview.py` for Joint Validation listing (D-JV-01, D-JV-12, D-JV-14, deleted POST /overview/add per D-JV-07). Add new `routers/joint_validation.py` for the detail page (D-JV-12) and AI Summary route (D-JV-16). Add the StaticFiles mount and lifespan mkdir in `main.py` (D-JV-13).

Purpose: This plan makes the listing render JV rows and adds the detail/summary routes. Plan 05 (templates) renders the actual HTML; this plan ensures the route bodies exist with the correct context dicts.

Output: Three modified files (`main.py`, `routers/overview.py`, `routers/joint_validation.py` — new); the legacy curated-Platform routes are gone; the JV listing/detail/summary routes are wired; static mount serves `/static/joint_validation/<id>/...`. Plan 05 templates will be rendered against this; Plan 06 tests will exercise these endpoints.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/01-overview-tab-auto-discover-platforms-from-html-files/01-CONTEXT.md
@.planning/phases/01-overview-tab-auto-discover-platforms-from-html-files/01-RESEARCH.md
@./CLAUDE.md
@app_v2/main.py
@app_v2/routers/overview.py
@app_v2/routers/summary.py
@app_v2/routers/platforms.py
@app_v2/services/joint_validation_grid_service.py
@app_v2/services/joint_validation_store.py
@app_v2/services/joint_validation_summary.py
@app_v2/services/llm_resolver.py

<interfaces>
<!-- Phase 5 router contract (lines reused; lines deleted; new routes added). -->

From `app_v2/routers/overview.py` (current Phase 5 file — referenced for surgical changes):
- `_parse_filter_dict(...)` lines 131-152 — REUSED VERBATIM
- `_build_overview_url(filters, sort_col, sort_order)` lines 155-181 — REUSED VERBATIM (uses `urllib.parse.urlencode(pairs, quote_via=urllib.parse.quote)`; same 6 query keys)

From `app_v2/services/joint_validation_grid_service.py` (Plan 02):

```
def build_joint_validation_grid_view_model(
    jv_root: Path,
    filters: dict[str, list[str]] | None = None,
    sort_col: str | None = None,
    sort_order: Literal["asc", "desc"] | None = None,
) -> JointValidationGridViewModel: ...
```

From `app_v2/services/joint_validation_store.py` (Plan 02):
- `JV_ROOT: Path = Path("content/joint_validation")`
- `PAGE_ID_PATTERN: re.Pattern = re.compile(r"^\d+$")`
- `discover_joint_validations(jv_root) -> Iterator[tuple[str, Path]]`
- `get_parsed_jv(page_id, index_html) -> ParsedJV`

From `app_v2/services/joint_validation_summary.py` (Plan 03):

```
def get_or_generate_jv_summary(
    confluence_page_id: str,
    cfg: LLMConfig,
    jv_root: Path,
    *,
    regenerate: bool = False,
) -> SummaryResult: ...
```

From `app_v2/services/llm_resolver.py` (Phase 6 — reused unchanged): `resolve_llm_config(request) -> LLMConfig` reads the `pbm2_llm` cookie + falls back to `settings.llms[0]`.

From `app_v2/routers/summary.py` (Phase 3): `_resolve_target_id(hx_target, entity_id)` returns the existing target id from `HX-Target` header or falls back to `summary-{entity_id}`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add StaticFiles mount + lifespan mkdir + register joint_validation router in main.py</name>
  <files>app_v2/main.py</files>
  <read_first>
    - app_v2/main.py (current lifespan body lines 41-96 — `content_dir = Path("content/platforms")` mkdir at line 73-77; mount call at line 108; router import block at lines 168-182)
    - .planning/phases/01-overview-tab-auto-discover-platforms-from-html-files/01-RESEARCH.md (Code Example 5 — exact mount snippet; FastAPI StaticFiles Path-Traversal Defense section; Pitfall 10 — mount ordering)
    - .planning/phases/01-overview-tab-auto-discover-platforms-from-html-files/01-CONTEXT.md (D-JV-13 — exact mount call signature)
  </read_first>
  <action>
**Modify `app_v2/main.py`** with three insertions:

**Step 1 — Add lifespan mkdir for `content/joint_validation/`** (inside the existing `lifespan(app)` async function, immediately after the `content_dir` mkdir block around line 77):

```
    # Joint Validation drop-folder root (D-JV-13). Phase 1 Plan 04.
    jv_dir = Path("content/joint_validation")
    try:
        jv_dir.mkdir(parents=True, exist_ok=True)
    except OSError as exc:
        _log.warning("Failed to create content/joint_validation/: %s", exc)
```

**Step 2 — Add the StaticFiles mount BEFORE the existing `/static` mount (Pitfall 10 — Starlette dispatches mounts by registration order, longest-prefix-first is NOT automatic).** Find the line `app.mount("/static", StaticFiles(directory=str(STATIC_DIR)), name="static")` (currently at line 108) and insert ABOVE it:

```
# Joint Validation static mount (D-JV-13). Registered BEFORE /static so that
# requests to /static/joint_validation/... match this mount instead of the
# parent /static mount. See RESEARCH.md Pitfall 10.
app.mount(
    "/static/joint_validation",
    StaticFiles(
        directory="content/joint_validation",
        html=False,            # Do NOT auto-serve index.html for bare folder URLs
        follow_symlink=False,  # Default; explicit for documentation
    ),
    name="joint_validation_static",
)
```

The existing `app.mount("/static", ...)` line stays unchanged at its position immediately below the new block.

**Step 3 — Register the new joint_validation router** in the router-import block (currently lines 168-182). Insert the import AFTER `summary` and the include_router call AFTER `app.include_router(summary.router)`:

```
from app_v2.routers import overview  # noqa: E402
from app_v2.routers import platforms  # noqa: E402
from app_v2.routers import summary  # noqa: E402
from app_v2.routers import joint_validation  # noqa: E402  Phase 1 Plan 04
from app_v2.routers import browse  # noqa: E402
from app_v2.routers import ask  # noqa: E402
from app_v2.routers import settings as settings_router  # noqa: E402
from app_v2.routers import root  # noqa: E402

app.include_router(overview.router)
app.include_router(platforms.router)
app.include_router(summary.router)
app.include_router(joint_validation.router)
app.include_router(browse.router)
app.include_router(ask.router)
app.include_router(settings_router.router)
app.include_router(root.router)
```

The exact existing import order may differ slightly; preserve it. The only change is inserting `joint_validation` between `summary` and `browse` in BOTH the import block AND the include_router calls.

**Do NOT change** the existing lifespan body for `content_dir`, the `/static` mount target, the FastAPI app instantiation, the exception handlers, the `_is_htmx_request` helper, or any other routers' import lines.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/v2/test_main.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F 'app.mount("/static/joint_validation"' app_v2/main.py` finds exactly one mount call
    - `grep -F 'name="joint_validation_static"' app_v2/main.py` finds the mount name
    - `grep -F 'directory="content/joint_validation"' app_v2/main.py` finds the directory arg
    - `grep -F 'html=False' app_v2/main.py` finds the explicit html=False kwarg in the JV mount block
    - `grep -F 'follow_symlink=False' app_v2/main.py` finds the explicit follow_symlink kwarg
    - `grep -E '^from app_v2.routers import joint_validation' app_v2/main.py` finds exactly one import line
    - `grep -F 'app.include_router(joint_validation.router)' app_v2/main.py` finds exactly one include line
    - `grep -F 'jv_dir = Path("content/joint_validation")' app_v2/main.py` finds the lifespan mkdir
    - The JV mount line appears BEFORE the `/static` mount line in `main.py` — verify with: `grep -n 'app.mount' app_v2/main.py` shows the JV line number is smaller than the `/static` line number
    - Server imports cleanly: `.venv/bin/python -c "from app_v2.main import app; print('OK')"` exits 0
    - `pytest tests/v2/test_main.py -q` exits 0 (existing main tests still pass; the JV mount addition is non-breaking)
  </acceptance_criteria>
  <done>StaticFiles mount registered before /static; lifespan creates content/joint_validation/ on cold start; joint_validation router registered between summary and browse.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite routers/overview.py listing for Joint Validation; delete add_platform + curated-Platform helpers</name>
  <files>app_v2/routers/overview.py</files>
  <read_first>
    - app_v2/routers/overview.py (current Phase 5 file — preserve `_parse_filter_dict` lines 131-152, `_build_overview_url` lines 155-181; modify GET routes lines 184-253; modify POST /overview/grid lines 306-359; DELETE add_platform route lines 256-303 and `_resolve_curated_pids` / `_entity_dict` / `_build_overview_context` helpers lines 56-127)
    - app_v2/services/joint_validation_grid_service.py (Plan 02 — provides `build_joint_validation_grid_view_model`, `FILTERABLE_COLUMNS`, `JointValidationGridViewModel`)
    - app_v2/services/joint_validation_store.py (Plan 02 — provides `JV_ROOT`)
    - .planning/phases/01-overview-tab-auto-discover-platforms-from-html-files/01-RESEARCH.md (Phase 5 Reuse Map "GET / + GET /overview route" and "POST /overview/grid route" rows)
    - .planning/phases/01-overview-tab-auto-discover-platforms-from-html-files/01-CONTEXT.md (D-JV-12 + D-JV-14 — exact URL shape)
  </read_first>
  <action>
**Modify `app_v2/routers/overview.py`** as a structured rewrite:

**Step 1 — Replace imports.** Remove imports of `OverviewEntity`, `DuplicateEntityError`, `OverviewGridViewModel`, `build_overview_grid_view_model`, `read_yaml_entities`, `add_entity_to_yaml`, `OVERVIEW_YAML_PATH`, `read_frontmatter`, `CONTENT_DIR_DEFAULT`, and any `overview_filter` symbols. Add:

```
from app_v2.services.joint_validation_grid_service import (
    FILTERABLE_COLUMNS,
    JointValidationGridViewModel,
    build_joint_validation_grid_view_model,
)
from app_v2.services.joint_validation_store import JV_ROOT
```

**Step 2 — Delete legacy helpers.** Delete the following entire functions (Phase 5 file, current line ranges):
- `_resolve_curated_pids` (~lines 56-90)
- `_entity_dict` (~lines 90-110)
- `_build_overview_context` (~lines 110-127)

KEEP `_parse_filter_dict` (lines 131-152) and `_build_overview_url` (lines 155-181) VERBATIM — these are JV-agnostic with the same 6 query keys.

**Step 3 — Rewrite `GET /` and `GET /overview` route bodies.** The route signatures stay the same (FastAPI Query params unchanged). Replace the body with:

```
@router.get("/", response_class=HTMLResponse)
@router.get("/overview", response_class=HTMLResponse)
def get_overview(
    request: Request,
    status: Annotated[list[str], Query(default_factory=list)] = [],
    customer: Annotated[list[str], Query(default_factory=list)] = [],
    ap_company: Annotated[list[str], Query(default_factory=list)] = [],
    device: Annotated[list[str], Query(default_factory=list)] = [],
    controller: Annotated[list[str], Query(default_factory=list)] = [],
    application: Annotated[list[str], Query(default_factory=list)] = [],
    sort: Annotated[str | None, Query()] = None,
    order: Annotated[str | None, Query()] = None,
):
    """Render the Joint Validation listing (D-JV-01, D-JV-12, D-JV-14)."""
    filters = _parse_filter_dict(status, customer, ap_company, device, controller, application)
    vm: JointValidationGridViewModel = build_joint_validation_grid_view_model(
        JV_ROOT,
        filters=filters,
        sort_col=sort,
        sort_order=order,  # type: ignore[arg-type]
    )
    ctx = {
        "vm": vm,
        "selected_filters": filters,
        "active_tab": "overview",
    }
    return templates.TemplateResponse(request, "overview/index.html", ctx)
```

**Step 4 — DELETE the `POST /overview/add` route** (current Phase 5 file lines ~256-303). The function `add_platform`, the route decorator, and any imports it depends on (e.g., `add_entity_to_yaml`, `DuplicateEntityError`) are removed. D-JV-07 explicitly forbids this route.

**Step 5 — Rewrite `POST /overview/grid` route body** (current lines 306-359):

```
@router.post("/overview/grid", response_class=HTMLResponse)
def post_overview_grid(
    request: Request,
    response: Response,
    status: Annotated[list[str], Form(default_factory=list)] = [],
    customer: Annotated[list[str], Form(default_factory=list)] = [],
    ap_company: Annotated[list[str], Form(default_factory=list)] = [],
    device: Annotated[list[str], Form(default_factory=list)] = [],
    controller: Annotated[list[str], Form(default_factory=list)] = [],
    application: Annotated[list[str], Form(default_factory=list)] = [],
    sort: Annotated[str | None, Form()] = None,
    order: Annotated[str | None, Form()] = None,
):
    """HTMX fragment swap. Returns three OOB blocks; pushes canonical URL."""
    filters = _parse_filter_dict(status, customer, ap_company, device, controller, application)
    vm: JointValidationGridViewModel = build_joint_validation_grid_view_model(
        JV_ROOT,
        filters=filters,
        sort_col=sort,
        sort_order=order,  # type: ignore[arg-type]
    )
    ctx = {
        "vm": vm,
        "selected_filters": filters,
        "active_tab": "overview",
    }
    response.headers["HX-Push-Url"] = _build_overview_url(
        filters, vm.sort_col, vm.sort_order
    )
    return templates.TemplateResponse(
        request,
        "overview/index.html",
        ctx,
        block_names=["grid", "count_oob", "filter_badges_oob"],
    )
```

The `block_names=["grid", "count_oob", "filter_badges_oob"]` list is byte-identical to Phase 5 (D-JV-12 contract).

**Step 6 — Update the `__all__` export list** if present: remove `OverviewEntity`, `DuplicateEntityError`, `_entity_dict`, `_resolve_curated_pids`, `_build_overview_context`, `add_platform`. Keep `router`, `_parse_filter_dict`, `_build_overview_url`, `FILTERABLE_COLUMNS`.

**Why this exact rewrite (per CONTEXT.md researcher_open_questions resolution #3 + RESEARCH.md Phase 5 Reuse Map):** "Recommendation: NEW file `routers/joint_validation.py` for the GET /joint_validation/<id> detail route + AI Summary route; keep listing routes (GET /overview, POST /overview/grid) in routers/overview.py since they are tab-scoped not entity-scoped." The listing URL `/overview` stays per D-JV-12. The detail route lives in Task 3.

**Do NOT delete:** the file `app_v2/routers/overview.py` itself; the `templates` Jinja2 environment instance it imports; the `router = APIRouter(...)` declaration. The legacy `OverviewEntity` Pydantic class deletion happens in Plan 06 — but only after this plan stops importing it.

**Sync `def` discipline:** Per CLAUDE.md INFRA-05, both routes are sync `def` (not `async def`). FastAPI dispatches to threadpool. BS4 parsing inside `build_joint_validation_grid_view_model` is CPU-bound — sync `def` keeps it off the event loop without extra ceremony.
  </action>
  <verify>
    <automated>.venv/bin/python -c "from app_v2.main import app; from fastapi.testclient import TestClient; c = TestClient(app); r = c.get('/overview'); assert r.status_code == 200, r.status_code; r2 = c.post('/overview/add', data={'platform_id': 'x'}); assert r2.status_code in (404, 405), r2.status_code; print('OK', r.status_code, r2.status_code)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F 'from app_v2.services.joint_validation_grid_service import' app_v2/routers/overview.py` finds the import
    - `grep -F 'from app_v2.services.joint_validation_store import JV_ROOT' app_v2/routers/overview.py` finds the import
    - `grep -F 'build_joint_validation_grid_view_model' app_v2/routers/overview.py` finds at least 2 occurrences (one in GET, one in POST)
    - `grep -F 'block_names=["grid", "count_oob", "filter_badges_oob"]' app_v2/routers/overview.py` finds exactly one occurrence
    - `grep -c 'def add_platform' app_v2/routers/overview.py` returns `0` (route removed)
    - `grep -c '_resolve_curated_pids' app_v2/routers/overview.py` returns `0`
    - `grep -c '_entity_dict' app_v2/routers/overview.py` returns `0`
    - `grep -c '_build_overview_context' app_v2/routers/overview.py` returns `0`
    - `grep -c 'OverviewEntity' app_v2/routers/overview.py` returns `0`
    - `grep -c 'build_overview_grid_view_model' app_v2/routers/overview.py` returns `0` (old service import removed)
    - `grep -c 'async def' app_v2/routers/overview.py` returns `0` (sync def discipline preserved)
    - `grep -F '_build_overview_url' app_v2/routers/overview.py` finds at least 1 occurrence (helper preserved)
    - `grep -F '_parse_filter_dict' app_v2/routers/overview.py` finds at least 2 occurrences (helper + 2 call sites)
    - Live route check: TestClient `GET /overview` returns 200; `POST /overview/add` returns 404 or 405
    - `pytest tests/v2/test_main.py -q` exits 0 (no main-level regression)
  </acceptance_criteria>
  <done>routers/overview.py imports JV grid service; GET / and GET /overview render the JV listing; POST /overview/grid returns the three OOB blocks with HX-Push-Url; POST /overview/add deleted; legacy curated-Platform helpers deleted; sync def preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Create routers/joint_validation.py — detail route + AI Summary route</name>
  <files>app_v2/routers/joint_validation.py</files>
  <read_first>
    - app_v2/routers/summary.py (Phase 3 always-200 contract — `get_summary_route` lines 113-180; `_render_error` helper lines 95-110; `_resolve_target_id` line 75)
    - app_v2/routers/platforms.py (sibling pattern — `GET /platforms/{platform_id}` detail route; how `Annotated[str, Path(pattern=..., min_length=..., max_length=...)]` is used)
    - app_v2/services/joint_validation_summary.py (Plan 03 — `get_or_generate_jv_summary`)
    - app_v2/services/joint_validation_store.py (Plan 02 — `JV_ROOT`, `discover_joint_validations`, `get_parsed_jv`)
    - app_v2/services/joint_validation_grid_service.py (Plan 02 — `JointValidationRow`, `_sanitize_link`)
    - app_v2/services/llm_resolver.py (Phase 6 — `resolve_llm_config(request) -> LLMConfig`)
    - .planning/phases/01-overview-tab-auto-discover-platforms-from-html-files/01-RESEARCH.md (AI Summary Route Shape section — exact route signature; iframe Sandbox section — sandbox attribute set)
    - .planning/phases/01-overview-tab-auto-discover-platforms-from-html-files/01-CONTEXT.md (D-JV-12 — detail route shape; D-JV-15 — Report Link sanitization; D-JV-16 — pre-processing pipeline)
  </read_first>
  <action>
**Create `app_v2/routers/joint_validation.py`** with this exact module body. The detail route renders the properties table + iframe; the summary route mirrors `routers/summary.py:113` byte-stable except for the service call:

```
"""Joint Validation routes (Phase 1 Plan 04).

Two routes:
- GET /joint_validation/{confluence_page_id} — properties table + iframe sandbox
- POST /joint_validation/{confluence_page_id}/summary — AI Summary modal target

Both routes path-validate confluence_page_id with regex ^\\d+$ at the FastAPI
layer (path-traversal defense + non-numeric folders 404 per D-JV-03).

Sync def per INFRA-05 (BS4 + summary_service are CPU/IO-bound but synchronous;
FastAPI dispatches to threadpool).
"""
from __future__ import annotations

import logging
from typing import Annotated

from fastapi import APIRouter, Header, HTTPException, Path, Request
from fastapi.responses import HTMLResponse
from fastapi.templating import Jinja2Templates

from app_v2.services.joint_validation_grid_service import (
    JointValidationRow,
    _sanitize_link,
)
from app_v2.services.joint_validation_store import (
    JV_ROOT,
    PAGE_ID_PATTERN,
    discover_joint_validations,
    get_parsed_jv,
)
from app_v2.services.joint_validation_summary import get_or_generate_jv_summary
from app_v2.services.llm_resolver import resolve_llm_config


_log = logging.getLogger(__name__)
router = APIRouter(prefix="/joint_validation", tags=["joint_validation"])

# Reuse the same Jinja2Templates instance the rest of app_v2 uses. The exact
# import path is whatever the existing app_v2.routers.* files use — match the
# convention in app_v2/routers/overview.py and app_v2/routers/platforms.py.
# Typically:  from app_v2.templates import templates
# Or:         templates = Jinja2Templates(directory="app_v2/templates")
# Mirror whichever pattern exists in routers/overview.py.
from app_v2.routers.overview import templates  # noqa: E402


# Path constraint: numeric only, 1-32 chars (Confluence page IDs fit comfortably)
_CONFLUENCE_PAGE_ID = Path(pattern=r"^\d+$", min_length=1, max_length=32)


@router.get("/{confluence_page_id}", response_class=HTMLResponse)
def get_joint_validation_detail(
    request: Request,
    confluence_page_id: Annotated[str, _CONFLUENCE_PAGE_ID],
):
    """Detail page: properties table + iframe sandbox of the Confluence export.

    D-JV-12: properties table on top, <iframe sandbox="..."> below pointing at
             /static/joint_validation/<id>/index.html.
    D-JV-15: link sanitized with the same _sanitize_link as the listing.
    """
    index_html = JV_ROOT / confluence_page_id / "index.html"
    if not index_html.is_file():
        raise HTTPException(status_code=404, detail=f"No Joint Validation {confluence_page_id}")
    parsed = get_parsed_jv(confluence_page_id, index_html)
    # Build a JointValidationRow so the template uses the same field set as the listing
    row = JointValidationRow(
        confluence_page_id=confluence_page_id,
        title=parsed.title or confluence_page_id,
        status=parsed.status,
        customer=parsed.customer,
        model_name=parsed.model_name,
        ap_company=parsed.ap_company,
        ap_model=parsed.ap_model,
        device=parsed.device,
        controller=parsed.controller,
        application=parsed.application,
        assignee=parsed.assignee,
        start=parsed.start,
        end=parsed.end,
        link=_sanitize_link(parsed.link or None),
    )
    ctx = {
        "jv": row,
        "active_tab": "overview",
    }
    return templates.TemplateResponse(request, "joint_validation/detail.html", ctx)


@router.post("/{confluence_page_id}/summary", response_class=HTMLResponse)
def get_joint_validation_summary(
    request: Request,
    confluence_page_id: Annotated[str, _CONFLUENCE_PAGE_ID],
    x_regenerate: Annotated[str | None, Header()] = None,
    hx_target: Annotated[str | None, Header()] = None,
):
    """AI Summary route — always-200 contract, mirrors routers/summary.py:113.

    Reuses summary/_success.html and summary/_error.html (parameterized for
    entity_id + summary_url in Plan 01 Task 2).
    """
    cfg = resolve_llm_config(request)
    target_id = hx_target.lstrip("#") if hx_target else f"summary-{confluence_page_id}"
    summary_url = f"/joint_validation/{confluence_page_id}/summary"
    regenerate = x_regenerate is not None
    try:
        result = get_or_generate_jv_summary(
            confluence_page_id, cfg, JV_ROOT, regenerate=regenerate
        )
    except FileNotFoundError:
        return templates.TemplateResponse(
            request,
            "summary/_error.html",
            {
                "platform_id": confluence_page_id,   # backward-compat alias
                "entity_id": confluence_page_id,
                "summary_url": summary_url,
                "target_id": target_id,
                "reason": "missing-content",
                "backend_name": cfg.name,
            },
        )
    except Exception as exc:  # noqa: BLE001 — always-200 contract; see Phase 3
        _log.exception("JV summary generation failed for %s", confluence_page_id)
        from app_v2.services.summary_service import _classify_error
        return templates.TemplateResponse(
            request,
            "summary/_error.html",
            {
                "platform_id": confluence_page_id,
                "entity_id": confluence_page_id,
                "summary_url": summary_url,
                "target_id": target_id,
                "reason": _classify_error(exc, cfg.name),
                "backend_name": cfg.name,
            },
        )

    return templates.TemplateResponse(
        request,
        "summary/_success.html",
        {
            "platform_id": confluence_page_id,
            "entity_id": confluence_page_id,
            "summary_url": summary_url,
            "target_id": target_id,
            "summary_html": result.summary_html,
            "backend_name": result.backend_name,
            "cached_age_s": result.cached_age_s,
        },
    )
```

The template path resolution (`from app_v2.routers.overview import templates`) is the simplest reuse of the existing Jinja2Templates instance. If `app_v2/routers/overview.py` defines `templates` differently (e.g., imports from `app_v2.templates`), match its convention exactly — read `routers/overview.py` to confirm the canonical import path.

**Sync `def` discipline:** Both routes are sync `def`. BS4 parsing (called transitively via `get_parsed_jv` and `get_or_generate_jv_summary`) is CPU-bound; FastAPI dispatches sync routes to a threadpool, so this does not block the event loop.

**Path constraint defense-in-depth:** Even though the StaticFiles mount has its own path normalization, FastAPI's `Path(pattern=r"^\d+$")` rejects non-numeric IDs at the route layer — non-numeric `/joint_validation/foo` returns 422, mapped to a 404 by FastAPI's default validation error handler. Confluence page IDs in Atlassian Cloud are typically 8-12 digits; `max_length=32` is generous.

**Always-200 contract:** Every error path returns 200 with `summary/_error.html`. The route NEVER raises. This mirrors Phase 3's `routers/summary.py:113` contract.
  </action>
  <verify>
    <automated>.venv/bin/python -c "from app_v2.routers.joint_validation import router; print(sorted(r.path for r in router.routes))"</automated>
  </verify>
  <acceptance_criteria>
    - File `app_v2/routers/joint_validation.py` exists
    - `grep -E 'router = APIRouter\(prefix="/joint_validation"' app_v2/routers/joint_validation.py` finds the router declaration
    - `grep -F '@router.get("/{confluence_page_id}"' app_v2/routers/joint_validation.py` finds the detail route
    - `grep -F '@router.post("/{confluence_page_id}/summary"' app_v2/routers/joint_validation.py` finds the summary route
    - `grep -F 'pattern=r"^\\d+$"' app_v2/routers/joint_validation.py` finds the path-arg regex
    - `grep -F 'joint_validation/detail.html' app_v2/routers/joint_validation.py` finds the template reference
    - `grep -F 'summary/_success.html' app_v2/routers/joint_validation.py` finds the success-template reference
    - `grep -F 'summary/_error.html' app_v2/routers/joint_validation.py` finds the error-template reference (used in BOTH FileNotFoundError branch AND general exception branch)
    - `grep -c 'def get_joint_validation_detail' app_v2/routers/joint_validation.py` returns `1`
    - `grep -c 'def get_joint_validation_summary' app_v2/routers/joint_validation.py` returns `1`
    - `grep -c 'async def' app_v2/routers/joint_validation.py` returns `0` (sync def discipline)
    - `grep -F 'get_or_generate_jv_summary' app_v2/routers/joint_validation.py` finds the call
    - `grep -F 'get_parsed_jv' app_v2/routers/joint_validation.py` finds the detail-page parser call
    - `grep -F 'resolve_llm_config' app_v2/routers/joint_validation.py` finds the LLM resolution call
    - `grep -F '"summary_url":' app_v2/routers/joint_validation.py` finds the summary_url context var (passed to both success + error templates)
    - `grep -F '"entity_id":' app_v2/routers/joint_validation.py` finds the entity_id context var
    - Module imports cleanly: `.venv/bin/python -c "from app_v2.routers.joint_validation import router; print('OK')"` exits 0
    - Router exposes 2 routes: `.venv/bin/python -c "from app_v2.routers.joint_validation import router; assert len(router.routes) == 2"` exits 0
  </acceptance_criteria>
  <done>routers/joint_validation.py exposes GET /joint_validation/{id} (detail) + POST /joint_validation/{id}/summary (AI Summary always-200); both routes path-validate ^\d+$; both reuse existing summary partials with entity_id + summary_url; sync def preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP request `confluence_page_id` path arg → filesystem | Untrusted input crosses; could be path traversal, injection |
| HTTP request → `/static/joint_validation/<path>` mount | Untrusted path crosses Starlette StaticFiles boundary |
| `pbm2_llm` cookie → `resolve_llm_config` → LLM client | Untrusted cookie selects backend; closed-set defense via existing `llm_resolver.py` |
| Iframe-loaded HTML (Confluence export) → user browser | Same-origin embedded content; XSS / nav hijack vectors |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering / Spoofing | Path traversal via `confluence_page_id` route arg | mitigate | Annotated `Path(pattern=r"^\d+$", min_length=1, max_length=32)` rejects ANY non-digit input at the FastAPI parameter layer (returns 422 → 404). Defense-in-depth: `joint_validation_store.discover_joint_validations` enforces the same regex (Plan 02). The detail route's `JV_ROOT / confluence_page_id / "index.html"` path-join cannot escape `JV_ROOT` because the regex forbids `..`, `/`, and any non-digit byte. |
| T-04-02 | Tampering | Path traversal via `/static/joint_validation/...` URL | mitigate | Starlette 1.0.0 StaticFiles uses `os.path.realpath(joined_path)` + `os.path.commonpath([full_path, directory]) != str(directory)` reject (post-CVE-2023-29159). `html=False` and `follow_symlink=False` set explicitly. Test `client.get('/static/joint_validation/../etc/passwd')` returns 404 (Plan 06 invariant test covers this). |
| T-04-03 | Information Disclosure | Static mount surfaces non-numeric folders (e.g., `_drafts/sensitive.html`) if anyone drops them in `content/joint_validation/` | mitigate | Application-layer `^\d+$` regex in `discover_joint_validations` excludes non-numeric folders from the listing. The static mount itself does NOT enforce the regex — but the threat surface is "what users can navigate to via the UI", and the UI never links to non-numeric folders. Plan 06 invariant test asserts a `_drafts/` folder dropped into `content/joint_validation/` is NOT surfaced in the rendered listing HTML. |
| T-04-04 | Information Disclosure | XSS via Confluence-exported HTML rendered in iframe | mitigate | Plan 05 sets `sandbox="allow-same-origin allow-popups allow-popups-to-escape-sandbox"` on the iframe (NO `allow-scripts`). Even with `allow-same-origin`, scripts in the iframed HTML do NOT execute (sandbox blocks scripting unless `allow-scripts` is added). MDN warns explicitly against combining `allow-scripts` with `allow-same-origin` — Plan 05 must NOT add `allow-scripts`. |
| T-04-05 | Information Disclosure | XSS via parsed metadata rendered into the grid (stored XSS) | mitigate | Jinja2 autoescape ON (project default for `.html` templates); Plan 05 adds explicit `\| e` to every `{{ row.title }}`, `{{ row.customer }}`, etc. as defense-in-depth. The router itself does not render HTML — Jinja2 does. |
| T-04-06 | Information Disclosure | XSS via parsed `link` href → Report Link button | mitigate | `_sanitize_link` (Plan 02) drops `javascript:`, `data:`, `vbscript:`, `file:`, `about:` schemes BEFORE the value reaches the route's `JointValidationRow.link` field. The detail route construction in Task 3 calls `_sanitize_link(parsed.link or None)` explicitly. |
| T-04-07 | Elevation of Privilege | LLM prompt injection via Confluence content | mitigate | Inherits Plan 03 mitigation: D-JV-16 pre-processing decomposes `<script>`, `<style>`, `<img>`; JV_USER_PROMPT_TEMPLATE wraps content in `<jv_page>...</jv_page>`; JV_SYSTEM_PROMPT instructs LLM to treat wrapped content as data. The route does not introduce new injection vectors. |
| T-04-08 | Spoofing | Cache poisoning via mtime collision (different content, same mtime due to NFS / FAT seconds resolution) | accept | Per RESEARCH.md §"Mtime Cache" pitfall (a): the project deploys on Linux ext4 native filesystem (nanosecond mtime resolution). Risk only materializes on NFS / FAT / WSL2 host-volume mounts, none of which are in scope for this internal-tool deployment. |
| T-04-09 | DoS | LLM call hangs without timeout, blocks the threadpool worker | mitigate | `cfg.timeout_s` (default 30s from `AgentConfig`) is passed through `_call_llm_with_text` to `client.chat.completions.create(..., timeout=cfg.timeout_s)`. On timeout, the exception falls through to `_classify_error("timeout", ...)` → `summary/_error.html` with reason="timeout". |
| T-04-10 | DoS | Iframe loads massive Confluence export, blocks browser | accept | The exporter (Confluence) bounds export size; if a user drops a 100MB index.html, the iframe will be slow but not server-crashing. The static mount streams the file with Range support (Starlette StaticFiles default). Out of scope to add server-side size limits. |
</threat_model>

<verification>
After all 3 tasks:
- `pytest tests/v2/test_main.py -q` exits 0 (existing main tests still green)
- `python -c "from app_v2.main import app; print(sorted(r.path for r in app.routes if hasattr(r, 'path')))"` exits 0 and shows `/`, `/overview`, `/overview/grid`, `/joint_validation/{confluence_page_id}`, `/joint_validation/{confluence_page_id}/summary` in the route list
- `python -c "from fastapi.testclient import TestClient; from app_v2.main import app; c = TestClient(app); assert c.get('/').status_code == 200; assert c.get('/overview').status_code == 200; assert c.post('/overview/add', data={'p':'x'}).status_code in (404, 405); assert c.get('/joint_validation/abc').status_code == 422 or c.get('/joint_validation/abc').status_code == 404; print('OK')"` exits 0 (Plan 05 templates may not exist yet — this verification step covers only the route wiring; templated rendering tests live in Plan 06)
- All Phase 5 tests still green: `pytest tests/v2/ -q --ignore=tests/v2/test_overview_routes.py --ignore=tests/v2/test_overview_store.py --ignore=tests/v2/test_overview_grid_service.py --ignore=tests/v2/test_overview_filter.py --ignore=tests/v2/test_phase05_invariants.py` exits 0 (these 5 files still reference deleted modules — Plan 06 deletes them; this plan tolerates them being broken or skipped)
</verification>

<success_criteria>
- `app_v2/main.py` mounts `/static/joint_validation` BEFORE `/static`; lifespan creates `content/joint_validation/`; `joint_validation` router registered
- `app_v2/routers/overview.py` listing routes use the JV grid service; `POST /overview/add` is gone; sync def preserved
- `app_v2/routers/joint_validation.py` exposes `GET /joint_validation/{id}` (detail) + `POST /joint_validation/{id}/summary` (AI Summary always-200); both regex-pinned to `^\d+$`
- All routes import cleanly; TestClient smoke checks pass
- Plan 05 (templates) can render `overview/index.html` and `joint_validation/detail.html` against the context dicts this plan produces
</success_criteria>

<output>
After completion, create `.planning/phases/01-overview-tab-auto-discover-platforms-from-html-files/01-04-SUMMARY.md` documenting:
- main.py modifications (mount + lifespan + router register)
- routers/overview.py LOC delta (Phase 5 had ~370 lines; this plan should reduce to ~200 lines after curated-Platform helpers + add_platform are deleted)
- routers/joint_validation.py LOC (new, ~120 lines)
- Route-list snapshot showing JV routes + deleted /overview/add
</output>
