---
phase: 01-foundation-browsing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - .streamlit/config.toml
  - requirements.txt
  - app/adapters/db/mysql.py
  - streamlit_app.py
autonomous: true
requirements:
  - FOUND-01
  - FOUND-02
  - FOUND-03
  - FOUND-04
  - FOUND-05
  - FOUND-06
  - FOUND-08
  - SAFE-01
must_haves:
  truths:
    - "config/auth.yaml is not tracked by git (FOUND-02)"
    - "config/settings.yaml and .env are not tracked by git"
    - "requirements.txt pins sqlalchemy>=2.0,<2.1 and includes pydantic-ai>=1.0,<2.0 (FOUND-08)"
    - ".streamlit/config.toml exists with the UI-SPEC theme tokens"
    - "MySQLAdapter creates an engine with pool_pre_ping=True AND pool_recycle=3600 (FOUND-06)"
    - "MySQLAdapter.run_query uses pd.read_sql_query (not deprecated pd.read_sql)"
    - "MySQLAdapter.run_query attempts SET SESSION TRANSACTION READ ONLY when readonly=true (SAFE-01)"
    - "streamlit_app.py loads .env, builds st.navigation with Browse (default) and Settings pages, and renders the shared sidebar"
    - "Sidebar renders DB selector (or caption if exactly 1 DB), LLM selector (inert — writes st.session_state['active_llm']), and connection health indicator per D-03"
    - "streamlit run streamlit_app.py starts without ImportError and shows the Browse page by default"
  artifacts:
    - path: ".gitignore"
      provides: "git exclusions for secrets and runtime artifacts"
      contains: "config/auth.yaml"
    - path: ".streamlit/config.toml"
      provides: "Streamlit theme tokens per UI-SPEC"
      contains: 'primaryColor = "#1f77b4"'
    - path: "requirements.txt"
      provides: "pinned dependency list"
      contains: "pydantic-ai"
    - path: "app/adapters/db/mysql.py"
      provides: "MySQLAdapter with corrected pool_recycle and read_sql_query"
      contains: "pool_recycle=3600"
    - path: "streamlit_app.py"
      provides: "Streamlit entrypoint with navigation + shared sidebar"
      contains: "st.navigation"
  key_links:
    - from: "streamlit_app.py"
      to: "app/core/config.load_settings"
      via: "import at module top; settings instance built before sidebar renders"
      pattern: "from app.core.config import .*load_settings"
    - from: "streamlit_app.py"
      to: "st.session_state['active_db'], st.session_state['active_llm']"
      via: "sidebar selectbox widgets write to session_state keys"
      pattern: 'key="active_(db|llm)"'
    - from: "app/adapters/db/mysql.py"
      to: "sqlalchemy.create_engine"
      via: "engine singleton in _get_engine"
      pattern: "pool_pre_ping=True"
---

<objective>
Establish Phase 1 foundation: fix scaffolding defects, create the Streamlit entrypoint with shared sidebar, and close the remaining FOUND-* requirements that do not depend on the service layer.

Purpose: Every downstream plan (normalizer, service, Settings, Browse) depends on a working entrypoint, corrected MySQLAdapter, pinned requirements, and a gitignore that prevents demo credentials from being committed. These are the P0 scaffolding items the research identified.
Output: A runnable `streamlit run streamlit_app.py` that shows the Browse and Settings pages (empty bodies in this plan — they are implemented in later plans), correct dependency pins, and a fixed MySQLAdapter.

**Decision coverage:**
- D-01: Two `st.Page` surfaces (Browse default + Settings) — this plan wires navigation.
- D-03: Sidebar (DB selector, LLM selector inert, health indicator; no freshness) — this plan wires sidebar.
- D-04: Auth deferred. No streamlit-authenticator import in Phase 1 code. auth.yaml still gitignored. FOUND-01 and FOUND-03 are listed in `requirements` above because the phase includes them; their Phase 1 status is "deferred per D-04" and no code is produced for them.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-browsing/01-CONTEXT.md
@.planning/phases/01-foundation-browsing/01-RESEARCH.md
@.planning/phases/01-foundation-browsing/01-UI-SPEC.md
@app/core/config.py
@app/adapters/db/mysql.py
@app/adapters/db/registry.py
@requirements.txt
@.env.example

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from existing codebase. -->
<!-- Use these directly — no codebase exploration needed. -->

From app/core/config.py (already exists, use as-is):
```python
class DatabaseConfig(BaseModel):
    name: str
    type: Literal["mysql", "postgres", "mssql", "bigquery", "snowflake"] = "mysql"
    host: str; port: int = 3306; database: str = ""
    user: str = ""; password: str = ""; readonly: bool = True

class LLMConfig(BaseModel):
    name: str
    type: Literal["openai", "anthropic", "ollama", "vllm", "custom"] = "openai"
    model: str; endpoint: str; api_key: str; temperature: float; max_tokens: int
    headers: dict[str, str]

class Settings(BaseModel):
    databases: list[DatabaseConfig]
    llms: list[LLMConfig]
    app: AppConfig

def load_settings() -> Settings
def save_settings(settings: Settings) -> None
def find_database(settings: Settings, name: str) -> DatabaseConfig | None
def find_llm(settings: Settings, name: str) -> LLMConfig | None
```

From app/adapters/db/registry.py:
```python
def build_adapter(config: DatabaseConfig) -> DBAdapter  # dispatches to MySQLAdapter
```

From app/adapters/db/base.py:
```python
class DBAdapter(ABC):
    def test_connection(self) -> tuple[bool, str]
    def list_tables(self) -> list[str]
    def get_schema(self, tables) -> dict
    def run_query(self, sql: str) -> pd.DataFrame
    def dispose(self) -> None
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create .gitignore and .streamlit/config.toml</name>
  <files>.gitignore, .streamlit/config.toml</files>
  <read_first>
    - .planning/phases/01-foundation-browsing/01-UI-SPEC.md §Streamlit Theme Configuration
    - .planning/phases/01-foundation-browsing/01-RESEARCH.md §Pattern ".gitignore content"
    - config/ directory listing to confirm auth.yaml and settings.example.yaml filenames
    - .env.example to confirm .env variable names (do not commit .env)
  </read_first>
  <action>
    Create `.gitignore` at repo root with the following exact content (addresses FOUND-02 plus general hygiene):

    ```gitignore
    # Config files containing secrets (per D-11: settings.yaml holds plaintext passwords/api_keys)
    config/settings.yaml
    config/auth.yaml
    .env

    # Python
    __pycache__/
    *.pyc
    *.pyo
    *.pyd
    .venv/
    venv/
    *.egg-info/
    .pytest_cache/
    .mypy_cache/
    .ruff_cache/

    # Logs
    logs/

    # OS
    .DS_Store
    Thumbs.db

    # Editor
    .vscode/
    .idea/
    ```

    Create `.streamlit/config.toml` (mkdir `.streamlit/` first) with theme tokens from UI-SPEC (verbatim):

    ```toml
    [theme]
    base = "light"
    primaryColor = "#1f77b4"
    backgroundColor = "#ffffff"
    secondaryBackgroundColor = "#f0f2f6"
    textColor = "#262730"
    font = "sans serif"
    ```

    Do NOT add any other `[server]` or `[browser]` sections — UI-SPEC specifies theme only.
  </action>
  <verify>
    <automated>test -f .gitignore && test -f .streamlit/config.toml && grep -q "config/auth.yaml" .gitignore && grep -q "config/settings.yaml" .gitignore && grep -q '^primaryColor = "#1f77b4"$' .streamlit/config.toml && grep -q '^secondaryBackgroundColor = "#f0f2f6"$' .streamlit/config.toml</automated>
  </verify>
  <done>
    - `.gitignore` exists with `config/auth.yaml`, `config/settings.yaml`, `.env`, `__pycache__/`, `.venv/`, `logs/` entries
    - `.streamlit/config.toml` exists with exactly six `[theme]` keys matching UI-SPEC values
    - `git status` after this task does NOT list `config/auth.yaml` as tracked (run `git rm --cached config/auth.yaml` if it was already tracked)
  </done>
  <acceptance_criteria>
    - `grep -c "^config/auth.yaml$" .gitignore` outputs `1`
    - `grep -c "^config/settings.yaml$" .gitignore` outputs `1`
    - `grep -c "^.env$" .gitignore` outputs `1`
    - `grep -c '^primaryColor = "#1f77b4"$' .streamlit/config.toml` outputs `1`
    - `grep -c '^base = "light"$' .streamlit/config.toml` outputs `1`
    - `test -d .streamlit` succeeds
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Tighten requirements.txt pins (FOUND-08) and fix MySQLAdapter (FOUND-06, SAFE-01)</name>
  <files>requirements.txt, app/adapters/db/mysql.py</files>
  <read_first>
    - requirements.txt current pins
    - app/adapters/db/mysql.py (all 80 lines)
    - .planning/phases/01-foundation-browsing/01-RESEARCH.md §Scaffolding Audit → "What Needs Fixing"
    - CLAUDE.md §Technology Stack → "Version Compatibility Notes"
  </read_first>
  <action>
    **requirements.txt** — Edit in place. Target state of the file (replace existing pins):
    ```
    streamlit>=1.40,<2.0
    streamlit-authenticator>=0.3.3,<1.0
    sqlalchemy>=2.0,<2.1
    pymysql>=1.1
    pandas>=3.0
    openpyxl>=3.1
    pyyaml>=6.0
    pydantic>=2.7
    pydantic-ai>=1.0,<2.0
    sqlparse>=0.5
    openai>=1.50
    httpx>=0.27
    requests>=2.32
    plotly>=5.22
    altair>=5.3
    python-dotenv>=1.0
    bcrypt>=4.2
    ```
    Key changes vs current file: `streamlit` gets `<2.0` upper bound, `streamlit-authenticator` gets `<1.0` upper bound, `sqlalchemy` gets `<2.1` upper bound, `pandas` bumped from `>=2.2` to `>=3.0`, and `pydantic-ai>=1.0,<2.0` is added (placed after `pydantic>=2.7`).

    Even though `streamlit-authenticator` is NOT imported in Phase 1 per D-04, keep it pinned so when auth is re-enabled the pin is already correct.

    **app/adapters/mysql.py** — Apply exactly two edits in `_get_engine`:
    1. Change `pool_recycle=1800` → `pool_recycle=3600` (FOUND-06 requires 3600).
    2. Confirm `pool_pre_ping=True` is present (already is per current file; do NOT remove it).

    Apply one edit in `run_query`:
    3. Change `pd.read_sql(text(sql), conn)` → `pd.read_sql_query(text(sql), conn)` (pandas 3.x idiom; avoids FutureWarning in logs). Update the import line to ensure `pd` is used the same way (already `import pandas as pd` — no change needed).

    Do NOT change the `SET SESSION TRANSACTION READ ONLY` logic — it is correct per SAFE-01. Do NOT change the Korean docstrings.
  </action>
  <verify>
    <automated>grep -q "sqlalchemy>=2.0,<2.1" requirements.txt && grep -q "pydantic-ai>=1.0,<2.0" requirements.txt && grep -q "pandas>=3.0" requirements.txt && grep -q "pool_recycle=3600" app/adapters/db/mysql.py && grep -q "pool_pre_ping=True" app/adapters/db/mysql.py && grep -q "pd.read_sql_query" app/adapters/db/mysql.py && ! grep -qE "pd\.read_sql\(text" app/adapters/db/mysql.py && python -c "import ast; ast.parse(open('app/adapters/db/mysql.py').read())"</automated>
  </verify>
  <done>
    - requirements.txt has all 17 pins listed above with exact version constraints
    - mysql.py uses `pool_recycle=3600` and `pool_pre_ping=True` in `_get_engine`
    - mysql.py's `run_query` uses `pd.read_sql_query`, not bare `pd.read_sql`
    - mysql.py parses as valid Python (syntax check passes)
    - No removal of existing `SET SESSION TRANSACTION READ ONLY` call (SAFE-01 preserved)
  </done>
  <acceptance_criteria>
    - `grep -c "^sqlalchemy>=2.0,<2.1$" requirements.txt` outputs `1`
    - `grep -c "^pydantic-ai>=1.0,<2.0$" requirements.txt` outputs `1`
    - `grep -c "^pandas>=3.0$" requirements.txt` outputs `1`
    - `grep -c "^pymysql>=1.1$" requirements.txt` outputs `1`
    - `grep -c "pool_recycle=3600" app/adapters/db/mysql.py` outputs `1`
    - `grep -c "pool_pre_ping=True" app/adapters/db/mysql.py` outputs `1`
    - `grep -c "pd.read_sql_query" app/adapters/db/mysql.py` is at least `1`
    - `grep -c "SET SESSION TRANSACTION READ ONLY" app/adapters/db/mysql.py` is at least `1`
    - `python -c "import ast; ast.parse(open('app/adapters/db/mysql.py').read())"` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Create streamlit_app.py entrypoint with navigation + shared sidebar (FOUND-04, FOUND-05, D-01, D-03)</name>
  <files>streamlit_app.py, app/pages/__init__.py, app/pages/browse.py, app/pages/settings.py</files>
  <read_first>
    - .planning/phases/01-foundation-browsing/01-UI-SPEC.md §Navigation §Sidebar Widget Order §Copywriting Contract (for LLM selector Phase 2 hint)
    - .planning/phases/01-foundation-browsing/01-CONTEXT.md §Decisions D-01 through D-05
    - .planning/phases/01-foundation-browsing/01-RESEARCH.md §Pattern 1 (Entrypoint + st.navigation) §Pitfall 7 (sidebar widgets re-render on tab switch)
    - app/core/config.py for load_settings signature
    - .env.example for OPENAI_API_KEY, SETTINGS_PATH variable names
  </read_first>
  <action>
    Create **`streamlit_app.py`** at repo root. It MUST:
    - Call `load_dotenv()` before any Streamlit imports that depend on env vars.
    - Call `st.set_page_config(page_title="PBM2", page_icon=":material/table_chart:", layout="wide")` before any other `st.*` call.
    - Build an `@st.cache_resource` decorated factory `get_db_adapter(db_name: str) -> DBAdapter` that loads settings, finds the named DB, and returns `build_adapter(cfg)`. The factory must accept `db_name` as a hashable string argument so the cache keys correctly per DB.
    - Render the shared sidebar in a `render_sidebar(settings: Settings)` function that writes ONLY the entrypoint-level widgets per D-05 (app title, DB selector, LLM selector, health indicator, divider). Do NOT render platform/parameter multiselects here — those belong inside `browse.py` per Pitfall 7.
    - DB selector logic (D-03):
      - If `len(settings.databases) > 1`: `st.sidebar.selectbox("Database", options=[d.name for d in settings.databases], key="active_db")`.
      - If `len(settings.databases) == 1`: `st.session_state["active_db"] = settings.databases[0].name; st.sidebar.caption(f"DB: {settings.databases[0].name}")`.
      - If `len(settings.databases) == 0`: `st.sidebar.warning("No database configured. Add one in Settings.")` and set `st.session_state["active_db"] = ""`.
    - LLM selector logic (inert, D-03):
      - If `settings.llms` non-empty: `st.sidebar.selectbox("LLM Backend", options=[l.name for l in settings.llms], key="active_llm")` followed by `st.sidebar.caption("LLM backend selection takes effect in Phase 2 (Ask page).")` (exact copy from UI-SPEC).
      - If empty: render nothing for the LLM selector; do not crash.
    - Health indicator (D-03): After the LLM selector, compute pass/fail via `get_db_adapter(st.session_state.get("active_db", "")).test_connection()` inside a `try/except` guarded block. Store the result in `st.session_state["_db_health"]` with TTL via a plain timestamp check (re-test at most once per 60s). Render as `st.sidebar.markdown("● {name}", unsafe_allow_html=True)` where the dot is colored via inline `<span style="color:#2ca02c|#d62728|#aaaaaa">●</span>` per UI-SPEC. Acceptable simpler alternative: `st.sidebar.success("● Connected")` / `st.sidebar.error("● Disconnected")` / `st.sidebar.caption("● Untested")`.
    - Sidebar divider: `st.sidebar.divider()` after the health indicator so the Browse page can append its own widgets below.
    - Navigation (D-01): `st.navigation([st.Page("app/pages/browse.py", title="Browse", icon=":material/table_chart:", default=True), st.Page("app/pages/settings.py", title="Settings", icon=":material/settings:")])` and then `pg.run()`. Include the Phase 2 "Ask" page entry as a commented-out line verbatim: `# st.Page("app/pages/nl_agent.py", title="Ask", icon=":material/chat:"),  # Phase 2 — uncomment when NL agent is wired`.
    - Do NOT import `streamlit-authenticator` anywhere (D-04). Do NOT gate the page list by auth.

    Create **`app/pages/__init__.py`** as an empty file (or with just `"""Streamlit pages."""` docstring).

    Create **`app/pages/browse.py`** as a minimal placeholder:
    ```python
    """Browse page — Pivot / Detail / Chart tabs. Implemented in Plan 05 and Plan 06."""
    import streamlit as st
    st.title("Browse")
    st.info("Browse UI will be implemented in Plans 05 and 06.")
    ```

    Create **`app/pages/settings.py`** as a minimal placeholder:
    ```python
    """Settings page — DB/LLM CRUD. Implemented in Plan 04."""
    import streamlit as st
    st.title("Settings")
    st.info("Settings UI will be implemented in Plan 04.")
    ```

    These placeholder page files are replaced by the real implementations in Plans 04, 05, 06 — they exist now only so `st.navigation` has valid targets and `streamlit run streamlit_app.py` starts cleanly.

    **Do not surface FOUND-01 (login page) or FOUND-03 (cookie-key guard) in code.** Per D-04 they are explicitly deferred. If you feel tempted to add a demo check, stop — the deferral is intentional.
  </action>
  <verify>
    <automated>test -f streamlit_app.py && test -f app/pages/__init__.py && test -f app/pages/browse.py && test -f app/pages/settings.py && python -c "import ast; [ast.parse(open(p).read()) for p in ['streamlit_app.py', 'app/pages/browse.py', 'app/pages/settings.py']]" && grep -q "load_dotenv" streamlit_app.py && grep -q "st.navigation" streamlit_app.py && grep -q "st.Page" streamlit_app.py && grep -q 'default=True' streamlit_app.py && grep -q "active_llm" streamlit_app.py && grep -q "active_db" streamlit_app.py && grep -q "@st.cache_resource" streamlit_app.py && ! grep -q "streamlit_authenticator" streamlit_app.py && ! grep -q "st.multiselect" streamlit_app.py</automated>
  </verify>
  <done>
    - `streamlit_app.py` exists at repo root
    - `load_dotenv()` is called before any `load_settings()` call
    - `st.set_page_config(...)` is the first Streamlit call (before sidebar rendering)
    - `st.navigation([...])` has exactly two active `st.Page` entries: Browse (default=True) and Settings
    - Sidebar has: app title, DB selector (or caption if single DB), LLM selector (with Phase 2 hint caption), health indicator, `st.sidebar.divider()`
    - `@st.cache_resource` decorates `get_db_adapter`
    - `streamlit-authenticator` is NOT imported (D-04)
    - Platform/parameter multiselect widgets are NOT in `streamlit_app.py` (Pitfall 7)
    - `streamlit run streamlit_app.py` starts (verified via `python -c "import streamlit_app"` — not an error)
  </done>
  <acceptance_criteria>
    - `grep -c "load_dotenv" streamlit_app.py` is at least `1`
    - `grep -c "st.set_page_config" streamlit_app.py` is at least `1`
    - `grep -c "st.navigation" streamlit_app.py` is `1`
    - `grep -cE 'st\.Page\("app/pages/(browse|settings)\.py"' streamlit_app.py` is `2`
    - `grep -c 'default=True' streamlit_app.py` is at least `1`
    - `grep -c '@st.cache_resource' streamlit_app.py` is at least `1`
    - `grep -c 'key="active_db"' streamlit_app.py` is at least `1`
    - `grep -c 'key="active_llm"' streamlit_app.py` is at least `1`
    - `grep -c "LLM backend selection takes effect in Phase 2" streamlit_app.py` is `1`
    - `grep -cE "^import streamlit_authenticator|^from streamlit_authenticator" streamlit_app.py` is `0`
    - `grep -c "st.multiselect" streamlit_app.py` is `0`
    - `python -c "import ast; ast.parse(open('streamlit_app.py').read())"` exits 0
    - `python -c "import ast; ast.parse(open('app/pages/browse.py').read())"` exits 0
    - `python -c "import ast; ast.parse(open('app/pages/settings.py').read())"` exits 0
    - `test -f app/pages/__init__.py` succeeds
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → git | config/auth.yaml contains demo credentials; MUST NOT be committed |
| filesystem → app | config/settings.yaml contains plaintext passwords per D-11; MUST NOT be logged or echoed |
| app → MySQL | DB connection uses pooled engine; SET SESSION TRANSACTION READ ONLY is best-effort |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | `config/auth.yaml` (demo admin/admin1234) | mitigate | Add `config/auth.yaml` to `.gitignore` in Task 1; if already tracked, user runs `git rm --cached config/auth.yaml` (noted in `<done>`) |
| T-01-02 | Information Disclosure | `config/settings.yaml` (plaintext passwords per D-11) | mitigate | Add `config/settings.yaml` to `.gitignore` in Task 1 |
| T-01-03 | Information Disclosure | `.env` (OPENAI_API_KEY) | mitigate | Add `.env` to `.gitignore` in Task 1 |
| T-01-04 | Tampering | MySQL data from app | mitigate | Readonly DB user (per PROJECT.md); MySQLAdapter preserves `SET SESSION TRANSACTION READ ONLY` in run_query (SAFE-01); not removed by this plan |
| T-01-05 | Denial of Service | Stale MySQL connections | mitigate | `pool_pre_ping=True` + `pool_recycle=3600` in `_get_engine` (FOUND-06 via Task 2) |
| T-01-06 | Elevation of Privilege | Missing auth in Phase 1 | accept | Intranet-only deployment per PROJECT.md Constraints; D-04 explicitly accepts no auth in Phase 1 |
| T-01-07 | Information Disclosure | Secrets in Streamlit logs | mitigate | streamlit_app.py must NOT log settings or env values; error handling in the sidebar health check wraps adapter calls in try/except and only surfaces the adapter's curated error message, never the config |
</threat_model>

<verification>
Run after all three tasks complete:
1. `python -c "import ast; [ast.parse(open(p).read()) for p in ['streamlit_app.py','app/pages/browse.py','app/pages/settings.py','app/adapters/db/mysql.py']]"` — all files parse.
2. `streamlit run streamlit_app.py --server.headless=true` starts without crashing within 10 seconds (test by backgrounding for 5s, then killing). Acceptable outcome: Streamlit reports "You can now view your Streamlit app in your browser" without Python exceptions in stderr.
3. `git check-ignore config/auth.yaml config/settings.yaml .env` — each prints its argument (meaning they are ignored).
</verification>

<success_criteria>
- [ ] `.gitignore` excludes `config/auth.yaml`, `config/settings.yaml`, `.env`
- [ ] `.streamlit/config.toml` has the UI-SPEC theme tokens verbatim
- [ ] `requirements.txt` pins `sqlalchemy>=2.0,<2.1`, adds `pydantic-ai>=1.0,<2.0`, bumps `pandas>=3.0`
- [ ] `MySQLAdapter._get_engine` uses `pool_pre_ping=True` and `pool_recycle=3600`
- [ ] `MySQLAdapter.run_query` uses `pd.read_sql_query`
- [ ] `MySQLAdapter.run_query` still calls `SET SESSION TRANSACTION READ ONLY` (SAFE-01 preserved)
- [ ] `streamlit_app.py` boots, builds `st.navigation` with Browse(default) + Settings, renders shared sidebar with DB/LLM/health per D-03
- [ ] No `streamlit-authenticator` import anywhere in Phase 1 code (D-04)
- [ ] `app/pages/browse.py` and `app/pages/settings.py` exist as placeholders
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-browsing/01-01-SUMMARY.md` following the standard summary template. Note: FOUND-01 and FOUND-03 are listed in this plan's requirements frontmatter as "deferred per D-04 — no code produced in Phase 1"; the SUMMARY should record this deferral explicitly so the traceability table in `.planning/REQUIREMENTS.md` can be updated accordingly at phase close.
</output>
