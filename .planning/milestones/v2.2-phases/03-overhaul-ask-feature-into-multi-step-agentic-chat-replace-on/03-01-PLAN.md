---
phase: 03-overhaul-ask-feature-into-multi-step-agentic-chat-replace-on
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - app/core/agent/config.py
  - config/settings.example.yaml
  - app_v2/templates/base.html
  - app_v2/static/vendor/htmx/htmx-ext-sse.js
  - app_v2/static/vendor/htmx/VERSIONS.txt
  - app_v2/static/vendor/plotly/plotly.min.js
  - app_v2/static/vendor/plotly/VERSIONS.txt
autonomous: true
requirements:
  - D-CHAT-03
must_haves:
  truths:
    - "requirements.txt declares sse-starlette as an explicit pinned dep (not just transitively present)"
    - "AgentConfig.chat_max_steps field exists with default=12, ge=1, le=50"
    - "config/settings.example.yaml documents the new chat_max_steps key"
    - "base.html declares an {% block extra_head %}{% endblock %} placeholder inside <head>"
    - "htmx-ext-sse@2.2.4 and Plotly 2.x bundles are vendored under app_v2/static/vendor/"
    - "VERSIONS.txt manifests record version + source URL + download date for each vendored asset"
  artifacts:
    - path: "requirements.txt"
      provides: "Pinned sse-starlette dependency"
      contains: "sse-starlette"
    - path: "app/core/agent/config.py"
      provides: "chat_max_steps field on AgentConfig"
      contains: "chat_max_steps"
    - path: "config/settings.example.yaml"
      provides: "Documented chat_max_steps default"
      contains: "chat_max_steps"
    - path: "app_v2/templates/base.html"
      provides: "extra_head Jinja block for per-page asset injection"
      contains: "block extra_head"
    - path: "app_v2/static/vendor/htmx/htmx-ext-sse.js"
      provides: "Vendored HTMX SSE extension JS bundle"
    - path: "app_v2/static/vendor/plotly/plotly.min.js"
      provides: "Vendored Plotly 2.x bundle"
  key_links:
    - from: "AgentConfig.chat_max_steps"
      to: "stream_chat_turn UsageLimits(tool_calls_limit=chat_max_steps)"
      via: "settings.app.agent.chat_max_steps Pydantic field reach"
      pattern: "chat_max_steps"
    - from: "templates/ask/index.html (rewritten in plan 04)"
      to: "{% block extra_head %} in base.html"
      via: "Jinja inheritance"
      pattern: "extra_head"
---

<objective>
Foundation plan: install the dependencies, configuration knob, vendored browser assets, and the extra_head template block that downstream plans assume. This plan creates no Python logic and no runtime behavior change on its own — it pre-positions every primitive needed by plans 02-05.

Purpose: Without this plan, plan 03 (`stream_chat_turn`) cannot import `chat_max_steps`, plan 04 (`templates/ask/index.html`) cannot extend an extra_head block, and the rewritten Ask page cannot load Plotly or the HTMX SSE extension at runtime.

Output: Pinned `sse-starlette` in requirements.txt; new `AgentConfig.chat_max_steps` field; documented setting in `settings.example.yaml`; `extra_head` block in `base.html`; vendored `htmx-ext-sse@2.2.4` and `plotly-2.x.min.js` bundles with VERSIONS.txt manifests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-overhaul-ask-feature-into-multi-step-agentic-chat-replace-on/03-CONTEXT.md
@.planning/phases/03-overhaul-ask-feature-into-multi-step-agentic-chat-replace-on/03-RESEARCH.md
@.planning/phases/03-overhaul-ask-feature-into-multi-step-agentic-chat-replace-on/03-UI-SPEC.md
@app/core/agent/config.py
@app_v2/templates/base.html
@app_v2/static/vendor/htmx/VERSIONS.txt

<interfaces>
<!-- Existing AgentConfig shape (verified in app/core/agent/config.py) -->
```python
class AgentConfig(BaseModel):
    model: str = ""
    max_steps: int = Field(default=5, ge=1, le=20)
    row_cap: int = Field(default=200, ge=1, le=10000)
    timeout_s: int = Field(default=30, ge=5, le=300)
    allowed_tables: list[str] = Field(default_factory=lambda: ["ufs_data"])
    max_context_tokens: int = Field(default=30_000, ge=1000, le=1_000_000)
```

<!-- Existing VERSIONS.txt format (from app_v2/static/vendor/htmx/VERSIONS.txt) -->
```
htmx: 2.0.10
source: https://cdn.jsdelivr.net/npm/htmx.org@2.0.10/dist/htmx.min.js
downloaded: 2026-04-24
pinned-reason: HTMX 4.0 is alpha (2026-04-09); 2.x is stable and maintained indefinitely.
files:
  - htmx.min.js
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add sse-starlette pin + AgentConfig.chat_max_steps + settings.example.yaml documentation</name>
  <files>requirements.txt, app/core/agent/config.py, config/settings.example.yaml</files>
  <read_first>
    - .planning/phases/03-overhaul-ask-feature-into-multi-step-agentic-chat-replace-on/03-CONTEXT.md (D-CHAT-03 locked text)
    - .planning/phases/03-overhaul-ask-feature-into-multi-step-agentic-chat-replace-on/03-RESEARCH.md ("Gap 12: D-CHAT-03 config knob shape")
    - app/core/agent/config.py (the file being modified)
    - requirements.txt (the file being modified)
    - config/settings.example.yaml (the file being modified)
  </read_first>
  <action>
**Step 1 — requirements.txt**: Add `sse-starlette>=3.3,<4.0` as an explicit pinned dependency. RESEARCH.md confirmed sse-starlette 3.3.4 is already present transitively in `.venv` but it is not pinned in requirements.txt. Add the line in alphabetical order alongside the other runtime deps (the file groups runtime deps in one block; insert after `pydantic-ai` entry — researcher confirmed format).

**Step 2 — AgentConfig.chat_max_steps (D-CHAT-03)**: Open `app/core/agent/config.py`. Inside the existing `AgentConfig(BaseModel)` class (currently 6 fields ending with `max_context_tokens`), add this field literally:

```python
chat_max_steps: int = Field(
    default=12, ge=1, le=50,
    description=(
        "Per-turn step budget for the multi-step chat agent loop (Phase 3 D-CHAT-03). "
        "Independent from `max_steps`, which governs the legacy single-turn agent. "
        "Multi-step chat needs more headroom; default 12. "
        "Counts ALL tool calls including the terminal `present_result` call."
    ),
)
```

Place it as the LAST field in `AgentConfig` (after `max_context_tokens`), preserving the existing field order so `tests/agent/test_nl_agent.py` and `tests/agent/test_pydantic_model.py` regression tests still pass field-by-field.

Default = 12 per D-CHAT-03 ("default ≥ 12"). `ge=1, le=50` upper bound mirrors the headroom rationale in RESEARCH.md Gap 12 + Open Question 4 ("If users hit the limit in practice, raise default to 16").

**Step 3 — settings.example.yaml**: Open `config/settings.example.yaml`. Find the `agent:` block (sibling to `database:` and `llm:`). Under it, add the new key with its default value and an inline comment:

```yaml
  # Phase 3 D-CHAT-03: per-turn step budget for the multi-step chat agent loop.
  # Independent from `max_steps` (which governs the legacy single-turn agent).
  # Counts ALL tool calls including the terminal `present_result` call.
  chat_max_steps: 12
```

Indent with 2 spaces to match the existing `agent:` sub-keys. Keep the rest of the file byte-stable (do NOT reorder existing keys).

**Decision traceability**: `Per D-CHAT-03 — extending AgentConfig (research-recommended over a new AgentChatConfig submodel; avoids YAML schema bump per RESEARCH.md "Alternatives Considered").`
  </action>
  <verify>
    <automated>grep -E "^sse-starlette" requirements.txt && grep -n "chat_max_steps" app/core/agent/config.py && grep -n "chat_max_steps" config/settings.example.yaml && python -c "from app.core.agent.config import AgentConfig; c = AgentConfig(); assert c.chat_max_steps == 12, f'expected 12, got {c.chat_max_steps}'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `requirements.txt` contains a line matching `^sse-starlette>=3\.3,<4\.0$`
    - `app/core/agent/config.py` contains the literal substring `chat_max_steps: int`
    - `app/core/agent/config.py` contains the literal substring `default=12, ge=1, le=50`
    - `app/core/agent/config.py` `AgentConfig` retains all existing fields (model, max_steps, row_cap, timeout_s, allowed_tables, max_context_tokens) — grep each one
    - `config/settings.example.yaml` contains the literal substring `chat_max_steps: 12`
    - `python -c "from app.core.agent.config import AgentConfig; assert AgentConfig().chat_max_steps == 12"` exits 0
    - Existing test file `tests/agent/test_nl_agent.py` still passes (run `pytest tests/agent/test_nl_agent.py -x`)
  </acceptance_criteria>
  <done>
    The new `sse-starlette` dependency is explicit in requirements.txt; AgentConfig.chat_max_steps defaults to 12 and validates [1,50]; settings.example.yaml documents the new key; existing tests still green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add extra_head block to base.html + vendor htmx-ext-sse + Plotly bundles</name>
  <files>app_v2/templates/base.html, app_v2/static/vendor/htmx/htmx-ext-sse.js, app_v2/static/vendor/htmx/VERSIONS.txt, app_v2/static/vendor/plotly/plotly.min.js, app_v2/static/vendor/plotly/VERSIONS.txt</files>
  <read_first>
    - .planning/phases/03-overhaul-ask-feature-into-multi-step-agentic-chat-replace-on/03-CONTEXT.md (no D-CHAT for vendoring directly — but RESEARCH.md gap 10 + UI-SPEC §A pin the loading strategy)
    - .planning/phases/03-overhaul-ask-feature-into-multi-step-agentic-chat-replace-on/03-RESEARCH.md ("Gap 10: Plotly bundle loading on /ask only", "Pitfall 2: HTMX SSE extension not vendored", "Environment Availability" table)
    - .planning/phases/03-overhaul-ask-feature-into-multi-step-agentic-chat-replace-on/03-UI-SPEC.md ("§Registry Safety" — vendoring sources + license + vetting)
    - app_v2/templates/base.html (the file being modified)
    - app_v2/static/vendor/htmx/VERSIONS.txt (the existing manifest format to mirror)
  </read_first>
  <action>
**Step 1 — base.html `extra_head` block (RESEARCH Gap 10 / UI-SPEC §A)**: Open `app_v2/templates/base.html`. Locate the `<head>` block; specifically find the line `<link rel="stylesheet" href="{{ url_for('static', path='css/app.css') }}">`. Immediately AFTER that link tag and before the next infra comment (`{# INFRA-02: HTMX loaded with defer; ... #}` per RESEARCH.md verified diff), insert a blank line then exactly:

```jinja
  {% block extra_head %}{% endblock %}
```

Indented with 2 spaces to match existing head-tag indentation. Do NOT add any default content inside the block; per-page templates extend it.

**Step 2 — Vendor `htmx-ext-sse@2.2.4` (RESEARCH Pitfall 2 + UI-SPEC §Registry Safety)**: From the project root, download the HTMX SSE extension:

```bash
mkdir -p app_v2/static/vendor/htmx
curl -fsSL 'https://cdn.jsdelivr.net/npm/htmx-ext-sse@2.2.4/dist/sse.js' \
  -o app_v2/static/vendor/htmx/htmx-ext-sse.js
```

Then APPEND (do not overwrite) the existing `app_v2/static/vendor/htmx/VERSIONS.txt` with a second manifest block separated by a blank line. Use the existing manifest format verbatim (verified by reading current file). Format the appended block as:

```
htmx-ext-sse: 2.2.4
source: https://cdn.jsdelivr.net/npm/htmx-ext-sse@2.2.4/dist/sse.js
downloaded: <today's ISO date YYYY-MM-DD>
pinned-reason: HTMX 2.x core does not bundle SSE; required for Phase 3 SSE chat surface.
files:
  - htmx-ext-sse.js
```

**Step 3 — Vendor Plotly 2.x bundle (RESEARCH Gap 10 + Pitfall 5 + UI-SPEC §Registry Safety)**: Create the plotly vendor dir and download Plotly 2.35.2 (UI-SPEC pinned this exact version under "Vendored JS bundles" — Plotly 2.x family stable, MIT license):

```bash
mkdir -p app_v2/static/vendor/plotly
curl -fsSL 'https://cdn.plot.ly/plotly-2.35.2.min.js' \
  -o app_v2/static/vendor/plotly/plotly.min.js
```

Verify the file is non-empty and looks like minified JS (first byte should be `!` or `/` — typical Plotly bundle prologue). Create `app_v2/static/vendor/plotly/VERSIONS.txt` (NEW FILE) with:

```
plotly.js: 2.35.2
source: https://cdn.plot.ly/plotly-2.35.2.min.js
downloaded: <today's ISO date YYYY-MM-DD>
pinned-reason: Phase 3 D-CHAT-05 — Plotly chart in _final_card.html. Loaded only on /ask page via per-page extra_head block (RESEARCH Pitfall 5: avoid global load).
files:
  - plotly.min.js
```

**Step 4 — Verify both bundles parse**: Run `node -e "process.exit(0)"` then `node --check app_v2/static/vendor/htmx/htmx-ext-sse.js` and `node --check app_v2/static/vendor/plotly/plotly.min.js` to confirm they are valid JS. (If `node` is unavailable, fallback: `python -c "import json; open('...').read()"` to assert non-empty.)

**Decision traceability**: `htmx-ext-sse@2.2.4 vendored per RESEARCH Pitfall 2 (HTMX 2.x core does not bundle SSE). Plotly 2.35.2 vendored per RESEARCH Gap 10 (Plotly required for D-CHAT-05 chart). UI-SPEC §Registry Safety records both as license-vetted vendored assets.`
  </action>
  <verify>
    <automated>grep -n "block extra_head" app_v2/templates/base.html && test -f app_v2/static/vendor/htmx/htmx-ext-sse.js && test -s app_v2/static/vendor/htmx/htmx-ext-sse.js && test -f app_v2/static/vendor/plotly/plotly.min.js && test -s app_v2/static/vendor/plotly/plotly.min.js && grep -q "htmx-ext-sse: 2.2.4" app_v2/static/vendor/htmx/VERSIONS.txt && grep -q "plotly.js: 2.35.2" app_v2/static/vendor/plotly/VERSIONS.txt</automated>
  </verify>
  <acceptance_criteria>
    - `app_v2/templates/base.html` contains the literal substring `{% block extra_head %}{% endblock %}`
    - The `extra_head` block sits inside `<head>` (run `python -c "import re; src = open('app_v2/templates/base.html').read(); head = re.search(r'<head>.*?</head>', src, re.DOTALL).group(0); assert 'block extra_head' in head"`)
    - `app_v2/static/vendor/htmx/htmx-ext-sse.js` exists and is non-empty (`wc -c` > 1000)
    - `app_v2/static/vendor/plotly/plotly.min.js` exists and is non-empty (`wc -c` > 1000000 — Plotly bundle is ~3.5MB)
    - `app_v2/static/vendor/htmx/VERSIONS.txt` contains exact line `htmx-ext-sse: 2.2.4`
    - `app_v2/static/vendor/htmx/VERSIONS.txt` retains the original `htmx: 2.0.10` block (do not overwrite — append)
    - `app_v2/static/vendor/plotly/VERSIONS.txt` exists and contains exact line `plotly.js: 2.35.2`
    - The original `htmx: 2.0.10` manifest block is still present in VERSIONS.txt (run `grep -c "htmx: 2.0.10" app_v2/static/vendor/htmx/VERSIONS.txt` returns >= 1)
    - Existing v2 tests still green (run `pytest tests/v2/test_main.py -x`)
  </acceptance_criteria>
  <done>
    base.html exposes a per-page `extra_head` block; the HTMX SSE extension and Plotly 2.x bundles are vendored with manifest entries; existing v2 tests continue to pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| jsDelivr CDN → repo | Vendoring third-party JS into the repo crosses the supply-chain trust boundary; bundles execute in user browsers under the app's origin. |
| Browser → app | Vendored bundles run with full app-origin privileges (DOM, cookies, fetch). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Tampering | Vendored htmx-ext-sse.js / plotly.min.js | mitigate | Pin exact versions in VERSIONS.txt manifests (htmx-ext-sse@2.2.4, plotly@2.35.2); UI-SPEC §Registry Safety records source URL + license + vetting date; future updates require re-vetting. |
| T-03-01-02 | Spoofing | jsDelivr CDN download | accept | jsDelivr is the de-facto npm CDN with TLS; subresource-integrity hashes could be added in a future hardening phase but are out of scope here (intranet deployment, single download moment, file checked into git). |
| T-03-01-03 | Information Disclosure | Plotly bundle on every page | mitigate | RESEARCH Pitfall 5: Plotly is loaded only on the Ask page via per-page extra_head block. Browse / JV / Settings pages do NOT load Plotly — verified later in plan 04 by grep on `templates/{browse,joint_validation,settings}/index.html` for `plotly.min.js` (must return 0 hits). |
| T-03-01-04 | Elevation of Privilege | New `chat_max_steps` config field | accept | Pydantic Field validators (ge=1, le=50) bound the value; the field is read-only at agent-loop time; no privilege boundary crossed. |
| T-03-01-05 | Denial of Service | High `chat_max_steps` cap of 50 | mitigate | UsageLimits enforces tool_calls_limit per turn; combined with existing `timeout_s` (default 30s, capped at 300s) and per-tool `row_cap`, max realistic damage is 50 SQL queries per turn. Read-only DB user (project security backstop) makes injection-class threats impossible. |
</threat_model>

<verification>
After both tasks complete, the foundation is in place for downstream plans:
- `from app.core.agent.config import AgentConfig` exposes the new `chat_max_steps` attribute
- `templates/ask/index.html` (created in plan 04) can `{% block extra_head %}<script src="{{ url_for('static', path='vendor/plotly/plotly.min.js') }}" defer></script>{% endblock %}` to load Plotly only on /ask
- `templates/ask/index.html` can also load `vendor/htmx/htmx-ext-sse.js` to enable the HTMX SSE extension on the chat surface
- No runtime behavior change yet — Ask page still renders the v2.0 Phase 6 surface (rewritten in plan 04)

Run `pytest tests/v2/ -x` and `pytest tests/agent/ -x` to confirm no regressions.
</verification>

<success_criteria>
- `requirements.txt` pins `sse-starlette>=3.3,<4.0`
- `AgentConfig.chat_max_steps` defaults to 12, validates [1,50], documented inline
- `config/settings.example.yaml` documents the new `chat_max_steps` knob
- `app_v2/templates/base.html` exposes a `{% block extra_head %}{% endblock %}` slot inside `<head>`
- `app_v2/static/vendor/htmx/htmx-ext-sse.js` (htmx-ext-sse@2.2.4) and `app_v2/static/vendor/plotly/plotly.min.js` (Plotly 2.35.2) are vendored with VERSIONS.txt manifests
- All existing tests under `tests/v2/` and `tests/agent/` remain green
</success_criteria>

<output>
After completion, create `.planning/phases/03-overhaul-ask-feature-into-multi-step-agentic-chat-replace-on/03-01-SUMMARY.md` with the standard summary template.
</output>
