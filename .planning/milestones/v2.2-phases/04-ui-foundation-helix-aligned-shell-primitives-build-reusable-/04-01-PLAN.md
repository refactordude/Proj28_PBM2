---
phase: 04-ui-foundation-helix-aligned-shell-primitives-build-reusable
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app_v2/static/css/app.css
  - app_v2/templates/base.html
autonomous: true
requirements:
  - D-UIF-01
  - D-UIF-06
  - D-UIF-07
  - D-UIF-09
  - D-UIF-10
must_haves:
  truths:
    - "app.css carries every Helix primitive class needed by Wave 2 partials (.topbar, .brand, .brand-mark, .brand-sep, .av, .tabs, .tab, .ph, .chips, .chip, .hero, .kpis, .kpi, .pop, .st, .pill, .typ, .oemchip, .ufschip, .lnk, .tiny-chip, .table-sticky-corner)."
    - ".panel-header CSS rules and .panel-header .panel-title rule are preserved AS-IS in this plan; Wave 5 owns their atomic rewrite to .ph rules together with the markup migration."
    - "base.html <head> loads Inter Tight (400/500/600/700/800) and JetBrains Mono (400/500/600) from Google Fonts BEFORE tokens.css link."
    - "Existing .navbar { padding: 16px 0 } rule is preserved (Wave 3 removes it together with the topbar swap; this plan does NOT touch .navbar rules)."
    - "Wave 2 partials and Wave 4 showcase route can render against this CSS+font foundation without further token additions."
  artifacts:
    - path: "app_v2/static/css/app.css"
      provides: "All new Helix primitive CSS rules appended at end of file"
      contains:
        - ".topbar"
        - ".brand-mark"
        - ".av"
        - ".tabs"
        - ".tab"
        - ".tab[aria-selected=\"true\"]"
        - ".ph "
        - ".chip"
        - ".chip.on"
        - ".hero"
        - ".hero .num"
        - ".kpis"
        - ".kpis.five"
        - ".kpi .spark"
        - ".pop"
        - ".pop-wrap"
        - ".pop .opt.on"
        - ".tiny-chip"
        - ".table-sticky-corner"
    - path: "app_v2/templates/base.html"
      provides: "Google Fonts <link> tag in <head>"
      contains:
        - "fonts.googleapis.com"
        - "Inter+Tight:wght@400;500;600;700;800"
        - "JetBrains+Mono:wght@400;500;600"
  key_links:
    - from: "base.html <head>"
      to: "Google Fonts CDN"
      via: "<link rel='stylesheet'> before tokens.css link"
      pattern: "fonts\\.googleapis\\.com.*Inter\\+Tight"
    - from: "app.css .ph rule"
      to: "Wave 2 page_head.html and showcase sections"
      via: "class name match"
      pattern: "\\.ph \\{"
    - from: "app.css .topbar rule"
      to: "Wave 3 base.html topbar partial"
      via: "class name match"
      pattern: "\\.topbar \\{"
---

<objective>
Wave 1 — CSS foundation. Append every Helix primitive class to `app_v2/static/css/app.css` so Wave 2 Jinja partials and Wave 4 showcase markup have rules to bind to. Add the Google Fonts `<link>` in `base.html` so weights 400–800 of Inter Tight (and JetBrains Mono 400/500/600) load (Pitfall 1 — `.page-title` font-weight 800 currently degrades to system fallback).

**Implements: D-UIF-01 (CSS half), D-UIF-06, D-UIF-07, D-UIF-09, D-UIF-10**

**Migration strategy (load-bearing — D-UIF-01 LOCKED rename path):** Per D-UIF-01 the user explicitly LOCKED the panel-header → .ph migration on every existing surface (Browse / JV / Ask). This plan ships `.ph` as the SOLE selector for the new shell with the normalized 16px 24px padding (no alias rule needed because Wave 5 atomically rewrites markup AND the existing `.panel-header` CSS rules to `.ph` rules in the same wave that updates Phase 02 invariant tests). 

For this Wave 1 plan specifically:
- Append `.ph` rules at the end of `app.css` (the new shell rules — see Task 2).
- The existing `.panel-header { ... }`, `.panel-header b`, `.panel-header .tag`, and `.panel-header .panel-title` rules at lines 58–74 stay UNTOUCHED in Wave 1. **Wave 5 owns the atomic CSS rule rewrite (`.panel-header .panel-title` → `.ph .panel-title`) together with the template markup migration and the Phase 02 invariant test updates** — same atomicity pattern as Wave 3's topbar swap + test_main.py rewrite.
- D-UI2-12 invariant (`.panel-header .panel-title { font-size: 18px; font-weight: 700; margin: 0 }`) becomes `.ph .panel-title { ... }` in Wave 5 with semantically identical declarations — pinned visual title size/weight stays the same.

**Why two waves and not one:** Wave 1 lays the new CSS foundation (every Helix primitive rule) without touching templates. Wave 5 performs the atomic markup + CSS rule rewrite + test rewrite (the rewrite has to be atomic so no commit leaves the suite half-broken). Splitting Wave 1 from Wave 5 keeps each plan small and reviewable; the Phase 02 invariants stay green between waves because Wave 1 does not modify any `.panel-header` rule or any template.

**What this plan does NOT do:**
- Does NOT touch `.navbar` rule (Wave 3 deletes it atomically with the topbar swap and the test updates).
- Does NOT modify any template under `app_v2/templates/{browse,overview,ask,joint_validation,platforms}/`.
- Does NOT add `--cyan` / `--cyan-soft` tokens (no Phase 4 primitive uses them — confirmed RESEARCH.md §Standard Stack and CONTEXT.md §Deferred).
- Does NOT touch `app_v2/static/css/tokens.css` (D-UI2-04 type scale locked — do not churn).

Output: app.css extended with ~270 new lines under a `Phase 04 — UI Foundation (Helix primitives)` banner; base.html `<head>` carries the Google Fonts link before tokens.css.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-ui-foundation-helix-aligned-shell-primitives-build-reusable-/04-CONTEXT.md
@.planning/phases/04-ui-foundation-helix-aligned-shell-primitives-build-reusable-/04-RESEARCH.md
@.planning/phases/04-ui-foundation-helix-aligned-shell-primitives-build-reusable-/04-UI-SPEC.md

# Source-of-truth files (visual + token references)
@/home/yh/Desktop/02_Projects/Proj27_PBM1_fork_bootstrap/Dashboard_v2.html
@app_v2/static/css/tokens.css
@app_v2/static/css/app.css
@app_v2/templates/base.html

# Phase 02 invariants that MUST stay green during Wave 1 (Wave 5 atomically rewrites them with the markup + CSS rule migration)
@tests/v2/test_phase02_invariants.py

<interfaces>
<!-- Existing tokens that this plan's new rules must consume (do NOT add new tokens). -->

From app_v2/static/css/tokens.css:
```css
:root {
  --bg: #f3f4f6;
  --panel: #ffffff;
  --ink: #171c24;
  --ink-2: #4a5361;
  --mute: #8b93a0;
  --dim: #b4bac3;
  --line: #eaecef;
  --line-2: #e1e4e8;
  --accent: #3366ff;
  --accent-soft: #ebf1ff;
  --accent-ink: #1f4dcf;
  --green: #17a963; --green-soft: #e5f7ee;
  --red: #ef3e4a;   --red-soft: #fde8ea;
  --amber: #f59e0b; --amber-soft: #fef3d9;
  --violet: #7a5af8; --violet-soft: #efebfe;
  --radius-panel: 22px;
  --radius-card: 16px;
  --radius-btn: 10px;
  --radius-pill: 999px;
  --shadow-panel: 0 1px 2px rgba(16,24,40,.04);
  --font-size-logo: 20px;
  --font-size-h1: 28px;
  --font-size-th: 12px;
  --font-size-body: 15px;
}
html, body {
  font-family: "Inter Tight", system-ui, -apple-system, "Segoe UI", Roboto, sans-serif;
  ...
}
```

Existing rules that must NOT be touched:
- `.panel-header { padding: 18px 26px; ... }` (app.css line 58)
- `.panel-header b { font-size: 18px; font-weight: 700; ... }` (line 68)
- `.panel-header .tag { ... }` (line 69)
- `.panel-header .panel-title { font-size: 18px; font-weight: 700; margin: 0; ... }` (line 74)
- `.navbar { padding-top: 16px; padding-bottom: 16px; }` (line 33–36) — Wave 3 will remove
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Google Fonts link to base.html (Pitfall 1 fix)</name>
  <files>app_v2/templates/base.html</files>
  <read_first>
    - app_v2/templates/base.html (current head structure)
    - .planning/phases/04-ui-foundation-helix-aligned-shell-primitives-build-reusable-/04-UI-SPEC.md (§Font Loading Fix lines 32–43)
    - .planning/phases/04-ui-foundation-helix-aligned-shell-primitives-build-reusable-/04-RESEARCH.md (Pitfall 1, lines 351–367)
    - /home/yh/Desktop/02_Projects/Proj27_PBM1_fork_bootstrap/Dashboard_v2.html (lines 7–9 for verbatim Google Fonts URL)
  </read_first>
  <action>
    Edit `app_v2/templates/base.html`. Locate the existing `<link rel="stylesheet" href="{{ url_for('static', path='css/tokens.css') }}">` line (currently line 14). Insert THREE new lines IMMEDIATELY BEFORE it (between the bootstrap-icons.css link on line 10 and the tokens.css link). Insert these lines verbatim:

    ```html
      {# Phase 04 — Inter Tight + JetBrains Mono via Google Fonts (Pitfall 1).
         tokens.css declares font-family "Inter Tight" but base.html had no link;
         system fallback maxed at weight 700, silently degrading .page-title's
         font-weight: 800. Loaded BEFORE tokens.css so the @font-face rules are
         available when tokens.css applies font-family. Weight set 400/500/600/700/800
         matches Dashboard_v2.html line 9 verbatim. #}
      <link rel="preconnect" href="https://fonts.googleapis.com">
      <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
      <link href="https://fonts.googleapis.com/css2?family=Inter+Tight:wght@400;500;600;700;800&family=JetBrains+Mono:wght@400;500;600&display=swap" rel="stylesheet">
    ```

    Do NOT modify any other line in base.html. Do NOT touch the navbar block (lines 39–68). Do NOT touch the footer block (lines 84–94).

    **Why this exact insertion point:** font @font-face rules MUST be loaded BEFORE the stylesheet that uses `font-family: "Inter Tight"`. tokens.css declares the font-family on `html, body`; placing the Google Fonts link AFTER tokens.css would make the first paint use system fallback for body text. Bootstrap-icons.css (the line above) declares its own bi-* font-face independently — order between bootstrap-icons.css and the Inter Tight link is irrelevant.
  </action>
  <verify>
    <automated>grep -c "fonts.googleapis.com" app_v2/templates/base.html</automated>
    Expected: returns >= 1 (specifically 2 — preconnect + stylesheet).
  </verify>
  <acceptance_criteria>
    - `grep -c 'fonts\.googleapis\.com' app_v2/templates/base.html` returns 2
    - `grep -c 'fonts\.gstatic\.com' app_v2/templates/base.html` returns 1
    - `grep -c 'Inter+Tight:wght@400;500;600;700;800' app_v2/templates/base.html` returns 1
    - `grep -c 'JetBrains+Mono:wght@400;500;600' app_v2/templates/base.html` returns 1
    - The Google Fonts `<link>` line appears BEFORE the line containing `css/tokens.css` (verify by reading the file: the fonts links appear at smaller line numbers than the tokens.css link)
    - `grep -c '<nav class="navbar' app_v2/templates/base.html` returns 1 (navbar still present — Wave 3 removes it)
    - `grep -c 'block footer' app_v2/templates/base.html` returns 2 (open + close — unchanged)
    - `python -c "from fastapi.testclient import TestClient; from app_v2.main import app; c = TestClient(app); r = c.get('/'); assert r.status_code == 200; assert 'Inter+Tight' in r.text"` exits 0
  </acceptance_criteria>
  <done>
    base.html `<head>` loads Google Fonts before tokens.css; existing structure (navbar, footer, scripts, htmx-error-container) byte-stable; root page still returns 200.
  </done>
</task>

<task type="auto">
  <name>Task 2: Append all Helix primitive CSS rules to app.css</name>
  <files>app_v2/static/css/app.css</files>
  <read_first>
    - app_v2/static/css/app.css (current contents — to know what's already there and where to append)
    - app_v2/static/css/tokens.css (token names available)
    - /home/yh/Desktop/02_Projects/Proj27_PBM1_fork_bootstrap/Dashboard_v2.html (lines 31–152, 173–219, 263–268, 1136–1206 — verbatim CSS source)
    - .planning/phases/04-ui-foundation-helix-aligned-shell-primitives-build-reusable-/04-UI-SPEC.md (§Spacing, §Component Inventory lines 168–242)
    - .planning/phases/04-ui-foundation-helix-aligned-shell-primitives-build-reusable-/04-RESEARCH.md (§Code Examples lines 616–656 for sticky-corner, Pitfall 5 for z-index ladder)
    - tests/v2/test_phase02_invariants.py (lines 178–195 — confirm `.panel-header .panel-title` rule untouched)
  </read_first>
  <action>
    Append a single contiguous block to the END of `app_v2/static/css/app.css` (after the last existing rule, the `@media (prefers-reduced-motion: reduce)` block at lines 537–540). Do NOT modify any existing rule. Do NOT delete the `.navbar { padding-top: 16px; padding-bottom: 16px; }` rule (Wave 3 owns its removal).

    Append exactly this block (token references use existing tokens.css vars; do NOT introduce new tokens):

    ```css

    /* ================================================================
       Phase 04 — UI Foundation (Helix primitives)
       Source: Dashboard_v2.html lines 31-152, 173-219, 263-268, 1155-1177.
       Spacing normalized to 4px grid per UI-SPEC §Spacing:
         .topbar  10px 14px -> 8px 16px
         .ph      18px 26px -> 16px 24px
         .hero    30px 32px -> 32px 32px
         .btn     38px      -> 40px (height re-anchored mod-4)
       D-UIF-01 rename path: this plan adds .ph as the new shell rule.
       Existing .panel-header rules at lines 58-74 stay UNTOUCHED in
       Wave 1; Wave 5 atomically rewrites .panel-header .panel-title
       to .ph .panel-title together with the template markup migration
       and the Phase 02 invariant test rewrites. D-UI2-12 declarations
       (font-size: 18px; font-weight: 700; margin: 0) carry over to
       .ph .panel-title byte-equivalent in Wave 5.
       ================================================================ */

    /* §Topbar — Helix flat-pill nav (D-UIF-06, D-UIF-07).
       Replaces Bootstrap navbar in Wave 3. Brand "P" + wordmark "PBM2"
       + tab strip + static "PM" avatar. No env pill, no notification
       bell, no live tab counts. */
    .topbar {
      display: flex;
      align-items: center;
      gap: 12px;
      padding: 8px 16px;
      background: var(--panel);
      border-radius: 16px;
      box-shadow: var(--shadow-panel);
      margin: 12px 16px 0;
    }
    .brand { display: flex; align-items: center; gap: 10px; font-weight: 700; font-size: 16px; color: var(--ink); }
    .brand-mark {
      width: 26px; height: 26px;
      border-radius: 8px;
      background: linear-gradient(135deg, #3366ff, #5e7cff);
      color: #fff;
      display: inline-flex; align-items: center; justify-content: center;
      font-size: 13px; font-weight: 800;
      letter-spacing: 0;
    }
    .brand-sep { color: var(--dim); font-weight: 500; }
    .av {
      width: 32px; height: 32px;
      border-radius: 999px;
      background: linear-gradient(135deg, #3366ff, #5e7cff);
      color: #fff;
      display: inline-flex; align-items: center; justify-content: center;
      font-size: 12px; font-weight: 700;
      letter-spacing: 0;
    }
    .top-right { display: flex; align-items: center; gap: 8px; margin-left: auto; }

    /* §Tabs — horizontal strip inside .topbar */
    .tabs {
      display: flex;
      gap: 2px;
      flex: 1;
      margin-left: 18px;
    }
    .tab {
      display: inline-flex;
      align-items: center;
      gap: 6px;
      height: 36px;
      padding: 0 14px;
      border-radius: 10px;
      font-size: 14px;
      font-weight: 600;
      color: var(--mute);
      text-decoration: none;
      letter-spacing: -.005em;
      background: transparent;
    }
    .tab:hover { color: var(--ink-2); background: #fafbfc; }
    .tab[aria-selected="true"] { background: #f4f6f8; color: var(--ink); }
    .tab .count {
      display: inline-flex; align-items: center; justify-content: center;
      min-width: 18px; height: 18px;
      padding: 0 5px;
      border-radius: 999px;
      background: #fff;
      box-shadow: inset 0 0 0 1px var(--line-2);
      font-size: 11px; font-weight: 600;
      color: var(--ink-2);
    }
    .tab[aria-selected="true"] .count {
      background: var(--accent-soft);
      color: var(--accent);
      box-shadow: none;
    }

    /* §.ph — new shell-header selector (D-UIF-01 rename path).
       Rules are byte-equivalent to the existing .panel-header rule (lines 58-74) at
       the property level (with the 16px 24px padding normalized per UI-SPEC §Spacing).
       Wave 5 atomically rewrites the existing .panel-header CSS rules to .ph rules
       together with the markup migration and Phase 02 invariant test rewrites. */
    .ph {
      padding: 16px 24px;
      display: flex;
      align-items: center;
      gap: 12px;
      flex-wrap: wrap;
      border-bottom: 1px solid var(--line);
    }
    .ph b { font-size: 18px; font-weight: 700; letter-spacing: -.015em; }
    .ph .tag { font-size: 12px; color: var(--mute); background: #f4f6f8; padding: 3px 10px; border-radius: var(--radius-pill); font-weight: 500; }
    .ph .spacer { flex: 1; }
    .ph .panel-title { font-size: 18px; font-weight: 700; letter-spacing: -.015em; margin: 0; line-height: 1.2; }

    /* §Chips — toggleable pills (Dashboard_v2.html lines 122-128) */
    .chips { display: flex; gap: 6px; flex-wrap: wrap; }
    .chip {
      display: inline-flex;
      align-items: center;
      gap: 6px;
      padding: 7px 14px;
      border-radius: var(--radius-pill);
      background: #f4f6f8;
      color: var(--ink);
      font-size: 13px;
      font-weight: 500;
      cursor: pointer;
      border: 0;
      letter-spacing: -.005em;
      font-family: inherit;
    }
    .chip:hover { background: #eaecf0; }
    .chip.on { background: var(--ink); color: #fff; }
    .chip .n {
      display: inline-flex; align-items: center; justify-content: center;
      min-width: 16px; height: 16px;
      padding: 0 4px;
      border-radius: 999px;
      background: rgba(0,0,0,.06);
      color: var(--ink-2);
      font-size: 11px;
      font-weight: 600;
    }
    .chip.on .n { background: rgba(255,255,255,.18); color: #fff; }

    /* §Hero — 1.3:1 grid panel with 72px stat number (D-UIF-11).
       Dashboard_v2.html lines 82-103. Padding normalized: 30px 32px -> 32px 32px. */
    .hero {
      position: relative;
      background: var(--panel);
      border-radius: var(--radius-panel);
      box-shadow: var(--shadow-panel);
      padding: 32px 32px;
      display: grid;
      grid-template-columns: 1.3fr 1fr;
      gap: 36px;
      overflow: hidden;
    }
    .hero::after {
      content: "";
      position: absolute;
      top: -40%; right: -20%;
      width: 70%; height: 140%;
      background: radial-gradient(circle, rgba(51,102,255,.08), transparent 60%);
      pointer-events: none;
      z-index: 0;
    }
    .hero > * { position: relative; z-index: 1; }
    .hero .label { font-size: 13px; font-weight: 600; color: var(--mute); margin-bottom: 8px; }
    .hero .num {
      display: inline-flex;
      align-items: baseline;
      gap: 8px;
      font-size: 72px;
      font-weight: 800;
      letter-spacing: -.045em;
      line-height: .95;
      color: var(--ink);
    }
    .hero .num .u { font-size: 24px; font-weight: 600; color: var(--mute); letter-spacing: -.015em; }
    .hero .delta {
      display: inline-block;
      margin-top: 14px;
      padding: 6px 12px;
      border-radius: var(--radius-pill);
      background: var(--accent-soft);
      color: var(--accent);
      font-size: 13px;
      font-weight: 600;
    }
    .hero-bar {
      display: flex;
      height: 10px;
      border-radius: var(--radius-pill);
      background: #f2f4f7;
      overflow: hidden;
      margin-top: 18px;
    }
    .hero-bar span { display: block; height: 100%; }
    .hero .side {
      display: flex;
      flex-direction: column;
      gap: 2px;
      background: #fafbfc;
      border-radius: var(--radius-card);
      padding: 20px 22px;
      align-self: start;
    }
    .hero .side .r {
      display: flex; align-items: center; justify-content: space-between;
      padding: 10px 0;
      font-size: 14px;
      border-bottom: 1px solid var(--line);
    }
    .hero .side .r:last-child { border-bottom: 0; }
    .hero .side .r .k { color: var(--mute); font-weight: 500; }
    .hero .side .r .v { font-size: 15px; font-weight: 700; letter-spacing: -.01em; color: var(--ink); }
    .hero .side .r .v.red { color: var(--red); }
    .hero .side .r .v.green { color: var(--green); }

    /* §KPIs — 4-up + 5-up card grid (Dashboard_v2.html lines 105-114) */
    .kpis {
      display: grid;
      grid-template-columns: repeat(4, 1fr);
      gap: 12px;
      margin-bottom: 14px;
    }
    .kpis.five { grid-template-columns: repeat(5, 1fr); }
    .kpi {
      position: relative;
      background: var(--panel);
      border-radius: var(--radius-card);
      box-shadow: var(--shadow-panel);
      padding: 18px 20px;
      display: flex;
      flex-direction: column;
      gap: 2px;
    }
    .kpi .l { font-size: 13px; font-weight: 500; color: var(--mute); }
    .kpi .v {
      font-size: 30px;
      font-weight: 700;
      letter-spacing: -.025em;
      margin-top: 4px;
      display: inline-flex;
      align-items: baseline;
      gap: 5px;
      color: var(--ink);
    }
    .kpi .v .u { font-size: 14px; font-weight: 500; color: var(--mute); }
    .kpi .d { font-size: 13px; font-weight: 500; margin-top: 4px; }
    .kpi .d.up   { color: var(--accent); }
    .kpi .d.down { color: var(--red); }
    .kpi .d.flat { color: var(--mute); }
    .kpi .d.ok   { color: var(--green); }
    .kpi .spark { position: absolute; right: 18px; top: 18px; }

    /* §Popover (.pop) — Bootstrap dropdown panel content (D-UIF-03).
       Dashboard_v2.html lines 130-152. Pitfall 3: explicit width override
       so Bootstrap's --bs-dropdown-min-width: 10rem default doesn't clip. */
    .pop-wrap { position: relative; }
    .pop {
      width: 300px !important;
      min-width: 300px !important;
      padding: 14px;
      background: var(--panel);
      border: 1px solid var(--line-2);
      border-radius: 14px;
      box-shadow: 0 12px 32px -8px rgba(16,24,40,.18);
      font-size: 14px;
      color: var(--ink);
    }
    .pop-head {
      display: flex;
      align-items: center;
      justify-content: space-between;
      font-size: 13px;
      font-weight: 700;
      color: var(--ink);
      margin-bottom: 10px;
    }
    .pop-head a { font-size: 12px; font-weight: 600; color: var(--accent); text-decoration: none; }
    .pop-head a:hover { text-decoration: underline; }
    .pop .qrow {
      display: flex;
      flex-wrap: wrap;
      gap: 6px;
      margin-bottom: 10px;
    }
    .pop .qrow button {
      height: 28px;
      padding: 0 10px;
      border-radius: var(--radius-pill);
      border: 0;
      background: #f4f6f8;
      color: var(--ink-2);
      font-size: 12px;
      font-weight: 600;
      cursor: pointer;
      font-family: inherit;
    }
    .pop .qrow button.active { background: var(--accent-soft); color: var(--accent); }
    .pop .dates {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 8px;
    }
    .pop .dates label {
      display: block;
      font-size: 11px;
      font-weight: 600;
      color: var(--mute);
      text-transform: uppercase;
      letter-spacing: .04em;
      margin-bottom: 4px;
    }
    .pop .dates input {
      width: 100%;
      border: 1px solid var(--line-2);
      border-radius: 8px;
      padding: 8px 10px;
      font-size: 13px;
      background: #fafbfc;
      color: var(--ink);
      font-family: inherit;
    }
    .pop .dates input:focus { outline: none; border-color: var(--accent); background: #fff; }
    .pop .foot {
      display: flex;
      align-items: center;
      justify-content: space-between;
      margin-top: 12px;
    }
    .pop .grp { margin-bottom: 12px; }
    .pop .grp:last-child { margin-bottom: 0; }
    .pop .grp-l {
      font-size: 11px;
      font-weight: 700;
      color: var(--mute);
      text-transform: uppercase;
      letter-spacing: .05em;
      margin-bottom: 6px;
    }
    .pop .opts { display: flex; flex-wrap: wrap; gap: 4px; }
    .pop .opt {
      padding: 5px 10px;
      border-radius: var(--radius-pill);
      background: #f4f6f8;
      color: var(--ink-2);
      font-size: 12px;
      font-weight: 500;
      border: 0;
      cursor: pointer;
      font-family: inherit;
    }
    .pop .opt.on { background: var(--accent); color: #fff; }

    /* §Status pills (.st / .pill — same rules; Dashboard_v2.html lines 173-181, 203-207) */
    .st, .pill {
      display: inline-flex;
      align-items: center;
      gap: 5px;
      padding: 4px 10px;
      border-radius: var(--radius-pill);
      font-size: 12px;
      font-weight: 600;
      letter-spacing: -.005em;
    }
    .st::before, .pill::before {
      content: "";
      display: inline-block;
      width: 6px; height: 6px;
      border-radius: 999px;
      background: currentColor;
    }
    .st.open, .pill.open       { background: var(--accent-soft); color: var(--accent); }
    .st.closed, .pill.closed   { background: var(--green-soft);  color: var(--green); }
    .st.over, .pill.over       { background: var(--red-soft);    color: var(--red); }

    /* §Type / OEM / UFS chips (Dashboard_v2.html lines 208-212) */
    .typ {
      display: inline-flex;
      align-items: center;
      gap: 6px;
      font-size: 12.5px;
      font-weight: 500;
      color: var(--ink-2);
    }
    .typ-ic {
      width: 22px; height: 22px;
      border-radius: 6px;
      background: #f4f6f8;
      display: inline-flex; align-items: center; justify-content: center;
    }
    .oemchip {
      display: inline-flex; align-items: center;
      padding: 3px 9px;
      border-radius: 6px;
      background: #fff;
      box-shadow: inset 0 0 0 1px var(--line-2);
      color: var(--ink-2);
      font-size: 12px;
      font-weight: 600;
    }
    .ufschip {
      display: inline-flex; align-items: center;
      padding: 3px 9px;
      border-radius: 6px;
      background: #f4f6f8;
      color: var(--ink-2);
      font-size: 12px;
      font-weight: 600;
    }
    .lnk {
      display: inline-flex; align-items: center;
      gap: 4px;
      height: 26px;
      padding: 0 9px;
      border-radius: 7px;
      background: transparent;
      color: var(--ink-2);
      font-size: 12px;
      font-weight: 500;
      text-decoration: none;
      border: 0;
      cursor: pointer;
      font-family: inherit;
    }
    .lnk:hover { background: #f4f6f8; color: var(--ink); }

    /* §Tiny chips (Dashboard_v2.html lines 263-268) */
    .tiny-chip {
      display: inline-flex;
      align-items: center;
      padding: 3px 9px;
      border-radius: var(--radius-pill);
      font-size: 11px;
      font-weight: 600;
      letter-spacing: -.005em;
    }
    .tiny-chip.ok      { background: var(--green-soft);  color: var(--green); }
    .tiny-chip.info    { background: var(--accent-soft); color: var(--accent); }
    .tiny-chip.warn    { background: var(--amber-soft);  color: var(--amber); }
    .tiny-chip.neutral { background: #f4f6f8;            color: var(--ink-2); }
    .tiny-chip.err     { background: var(--red-soft);    color: var(--red); }

    /* §Sticky-corner table (D-UIF-10; Dashboard_v2.html lines 1155-1177).
       z-index ladder: corner=3 > thead=2 > first-col=1 > body=auto.
       Explicit background on every sticky cell prevents bleed-through
       (Pitfall 5). Wrapping container needs overflow:auto + max-height. */
    .table-sticky-corner-wrap {
      overflow: auto;
      max-height: 560px;
      border: 1px solid var(--line);
      border-radius: var(--radius-card);
    }
    .table-sticky-corner {
      width: 100%;
      border-collapse: collapse;
      font-size: 13px;
    }
    .table-sticky-corner thead th {
      position: sticky;
      top: 0;
      z-index: 2;
      background: #fafbfc;
      text-align: left;
      padding: 10px 14px;
      font-size: 11px;
      color: var(--mute);
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: .04em;
      border-bottom: 1px solid var(--line);
      white-space: nowrap;
    }
    .table-sticky-corner thead th:first-child {
      left: 0;
      z-index: 3;
    }
    .table-sticky-corner tbody td:first-child {
      position: sticky;
      left: 0;
      z-index: 1;
      background: #fff;
      border-right: 1px solid var(--line);
    }
    .table-sticky-corner tbody td {
      padding: 10px 14px;
      border-bottom: 1px solid var(--line);
      color: var(--ink);
    }

    /* §Buttons — Helix .btn primary + .btn.sm + .btn.ghost (Dashboard_v2.html lines 69-80).
       Re-anchored 38px -> 40px height per UI-SPEC §Spacing. Co-exists with
       Bootstrap .btn (Bootstrap rules win for unscoped .btn callers; these
       Phase 04 rules apply when paired with .btn-helix or used inside .pop). */
    .btn-helix {
      display: inline-flex;
      align-items: center;
      gap: 6px;
      height: 40px;
      padding: 0 16px;
      border-radius: var(--radius-btn);
      background: var(--accent);
      color: #fff;
      font-size: 14px;
      font-weight: 600;
      border: 0;
      cursor: pointer;
      letter-spacing: -.005em;
      font-family: inherit;
    }
    .btn-helix:hover { background: var(--accent-ink); color: #fff; }
    .btn-helix.sm { height: 32px; padding: 0 12px; font-size: 13px; }
    .btn-helix.ghost { background: transparent; color: var(--ink-2); }
    .btn-helix.ghost:hover { background: #f4f6f8; color: var(--ink); }
    ```

    Do NOT add `--cyan` or any new tokens to `tokens.css`. Do NOT modify `.panel-header`, `.panel-body`, `.panel-footer`, `.shell`, `.page-head`, `.page-title`, `.page-sub`, `.page-actions`, `.btn-white`, `.navbar`, `.ai-btn`, `.ai-chip`, `.chat-*`, `.pivot-table`, `.overview-table`, `.browse-filter-bar`, `.overview-filter-bar`, `.markdown-content`, or `.properties-table` rules. They stay byte-stable.

    **Why these specific values:** Every padding / size / color is from Dashboard_v2.html with the four normalizations declared in UI-SPEC §Spacing (.topbar 10px 14px → 8px 16px; .ph 18px 26px → 16px 24px; .hero 30px 32px → 32px 32px; .btn 38px → 40px). Each token reference uses an existing tokens.css variable — no new tokens.

    **Why .pop uses `!important`:** Bootstrap's `.dropdown-menu` carries `min-width: 10rem` via `--bs-dropdown-min-width` (Pitfall 3). When the .pop class is applied to a `.dropdown-menu` element (Wave 2 partials), the cascade gives Bootstrap's rule equal specificity. `!important` ensures the 300px width wins. Researcher cited Bootstrap docs for this collision.

    **Why .ph rules carry the normalized padding (and existing .panel-header rules stay untouched in Wave 1):** D-UIF-01 locked the rename path. This plan defines `.ph` with the 16px 24px padding (UI-SPEC §Spacing 4px-grid normalization). Existing `.panel-header` rules at lines 58–74 stay byte-stable IN WAVE 1 because Phase 02 invariant tests pin them; Wave 5 atomically rewrites those rules (`.panel-header .panel-title` → `.ph .panel-title`, `.panel-header b` → `.ph b`, etc.) together with the template markup migration AND the Phase 02 invariant test updates. This phase ordering is what makes the migration commit atomic — same pattern as Wave 3's topbar swap + test_main.py rewrite.

    **Why btn-helix instead of overriding .btn:** Bootstrap defines `.btn`. Any rule for `.btn` here would either fight Bootstrap or override every Bootstrap `.btn` site-wide. `.btn-helix` is namespace-safe; popovers and showcase use `<button class="btn-helix sm">`.
  </action>
  <verify>
    <automated>python -c "
src = open('app_v2/static/css/app.css').read()
required = ['.topbar {', '.brand-mark {', '.av {', '.tabs {', '.tab {', '.tab[aria-selected=\"true\"]', '.ph {', '.chip {', '.chip.on {', '.hero {', '.hero .num {', '.kpis {', '.kpis.five {', '.kpi .spark {', '.pop {', '.pop-wrap {', '.pop .opt.on {', '.tiny-chip {', '.table-sticky-corner {', '.btn-helix {']
missing = [r for r in required if r not in src]
assert not missing, f'Missing: {missing}'
# Existing rules still present (Wave 5 will rewrite them atomically with markup migration + test updates)
assert '.panel-header { padding: 18px 26px;' in src
assert '.panel-header .panel-title { font-size: 18px; font-weight: 700; margin: 0;' in src
assert '.navbar {' in src and 'padding-top: 16px' in src
print('OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '\.topbar {' app_v2/static/css/app.css` returns >= 1
    - `grep -c '\.brand-mark {' app_v2/static/css/app.css` returns >= 1
    - `grep -c '\.tab\[aria-selected="true"\]' app_v2/static/css/app.css` returns >= 2 (base rule + .count rule)
    - `grep -c '\.ph {' app_v2/static/css/app.css` returns >= 1
    - `grep -c '\.chip\.on {' app_v2/static/css/app.css` returns >= 1
    - `grep -c '\.kpis\.five {' app_v2/static/css/app.css` returns >= 1
    - `grep -c '\.kpi \.spark {' app_v2/static/css/app.css` returns >= 1
    - `grep -c '\.pop {' app_v2/static/css/app.css` returns >= 1
    - `grep -c '\.pop \.opt\.on {' app_v2/static/css/app.css` returns >= 1
    - `grep -c '\.tiny-chip\.ok' app_v2/static/css/app.css` returns >= 1
    - `grep -c '\.table-sticky-corner thead th:first-child' app_v2/static/css/app.css` returns >= 1
    - `grep -c 'z-index: 3' app_v2/static/css/app.css` returns >= 1 (sticky-corner top-left z-index)
    - `grep -c 'linear-gradient(135deg, #3366ff, #5e7cff)' app_v2/static/css/app.css` returns 2 (brand-mark + av)
    - `grep -c '\.panel-header { padding: 18px 26px' app_v2/static/css/app.css` returns 1 (existing rule UNCHANGED)
    - `grep -c '\.panel-header \.panel-title { font-size: 18px; font-weight: 700; margin: 0' app_v2/static/css/app.css` returns 1 (D-UI2-12 unchanged)
    - `grep -c '\.navbar {' app_v2/static/css/app.css` returns 1 (Wave 3 will remove)
    - `pytest tests/v2/test_phase02_invariants.py -x -q` exits 0 (D-UI2-12 + .navbar invariants stay green)
    - `pytest tests/v2/test_main.py -x -q` exits 0 (existing topbar still renders)
    - `python -c "from fastapi.testclient import TestClient; from app_v2.main import app; c = TestClient(app); r = c.get('/static/css/app.css'); assert r.status_code == 200; assert '.topbar' in r.text"` exits 0
  </acceptance_criteria>
  <done>
    app.css carries every Helix primitive class needed by Wave 2 partials and Wave 4 showcase. Existing rules untouched. Phase 02 invariants stay green. test_main.py stays green (navbar still present, will be replaced in Wave 3).
  </done>
</task>

</tasks>

<verification>
After both tasks:
1. `pytest tests/v2/ -x -q` exits 0 (full suite — no regressions)
2. `grep -c 'fonts.googleapis.com' app_v2/templates/base.html` returns 2
3. `grep -c '.panel-header {' app_v2/static/css/app.css` returns 1 (NOT removed)
4. `grep -c '.ph {' app_v2/static/css/app.css` returns 1 (NEW)
5. `grep -c '.topbar {' app_v2/static/css/app.css` returns 1
6. `grep -c '.hero .num {' app_v2/static/css/app.css` returns 1
7. `grep -c '.table-sticky-corner thead th:first-child' app_v2/static/css/app.css` returns 1
8. Manually open http://localhost:8000/ in dev — page renders with no visible regressions (existing pages still use .panel-header / .navbar markup; new .ph / .topbar rules are dormant until Wave 2 / Wave 3 markup ships).
</verification>

<success_criteria>
- All `.topbar`, `.brand`, `.brand-mark`, `.av`, `.tabs`, `.tab`, `.ph`, `.chips`, `.chip`, `.hero`, `.kpis`, `.kpi`, `.pop`, `.st`, `.pill`, `.typ`, `.oemchip`, `.ufschip`, `.lnk`, `.tiny-chip`, `.table-sticky-corner`, `.btn-helix` rules present in app.css with values from UI-SPEC §Component Inventory
- Existing `.panel-header`, `.panel-body`, `.panel-footer`, `.panel-header .panel-title`, `.navbar`, `.shell`, `.page-head`, `.page-title`, `.ai-btn`, `.ai-chip`, `.chat-*`, `.pivot-table`, `.overview-table` rules byte-stable IN WAVE 1 (Wave 5 atomically rewrites the panel-header family rules with the markup migration + test updates per D-UIF-01)
- `tokens.css` not modified
- Google Fonts link present in `base.html` `<head>` BEFORE `tokens.css` link
- Full test suite exits 0
- Manual check at `/` confirms page still renders identically (existing markup uses old classes)
</success_criteria>

<output>
After completion, create `.planning/phases/04-ui-foundation-helix-aligned-shell-primitives-build-reusable-/04-01-SUMMARY.md` documenting:
- Files modified (app.css with line count delta; base.html with the 3-line head insertion)
- Decision: D-UIF-01 rename path — this plan adds `.ph` as the new shell selector; existing `.panel-header` CSS rules stay untouched in Wave 1; Wave 5 atomically rewrites them with the template migration + Phase 02 invariant test updates
- Tokens NOT added (intentional — no Phase 4 primitive uses --cyan / --cyan-soft)
- Confirmation: `.navbar { padding: 16px 0 }` rule still present (Wave 3 owns its removal)
- Wave 2 unblocked: every macro in `_components/` can now bind to a concrete CSS rule
</output>
