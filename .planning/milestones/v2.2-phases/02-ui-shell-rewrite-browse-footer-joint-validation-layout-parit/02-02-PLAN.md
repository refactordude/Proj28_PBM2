---
phase: 02-ui-shell-rewrite-browse-footer-joint-validation-layout-parit
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - app_v2/templates/browse/index.html
  - tests/v2/test_phase02_invariants.py
autonomous: true
requirements:
  - D-UI2-06
must_haves:
  truths:
    - "On GET /browse, the count caption ('N platforms × M parameters') appears inside the sticky footer (not inside the panel-header right zone)."
    - "On POST /browse/grid (HTMX filter change), the OOB count_oob fragment merges into `<span id=\"grid-count\">` regardless of its DOM location — count updates correctly even though the receiving element moved into the footer."
    - "Browse's panel-header still shows `<b>Browse</b>` + `<span class=\"tag\">Pivot grid</span>` and is otherwise byte-stable; the `.ms-auto d-flex` wrapper that previously housed `#grid-count` is removed."
    - "Browse's filter row, picker macros, grid markup, OOB picker_badges, params_picker_oob, and warnings_oob blocks are byte-stable — only the count's DOM location changes."
  artifacts:
    - path: "app_v2/templates/browse/index.html"
      provides: "Restructured panel-header (no count) + new {% block footer %} carrying #grid-count"
      contains: "{% block footer %}"
    - path: "tests/v2/test_phase02_invariants.py"
      provides: "Invariant tests for Browse footer count migration + OOB id stability"
      exports: ["test_*"]
  key_links:
    - from: "app_v2/templates/browse/index.html block footer"
      to: "app_v2/templates/base.html block footer"
      via: "Jinja template inheritance — {% extends 'base.html' %} + {% block footer %}"
      pattern: "block footer"
    - from: "app_v2/routers/browse.py POST /browse/grid"
      to: "<span id='grid-count'> in footer"
      via: "block_names=[..., 'count_oob', ...] OOB swap by id"
      pattern: "id=\"grid-count\".*hx-swap-oob"
---

<objective>
Migrate Browse's "N platforms × M parameters" count caption from inside `.panel-header` into the new sticky footer slot. The count text and OOB swap mechanics are byte-stable (same `id="grid-count"`, same `count_oob` block, same router code) — only the DOM location moves. This proves the footer pattern is useful before Plan 02-03 reuses it.

Purpose: D-UI2-06 — fill the footer with content that earns it visual weight. Browse is the first consumer; JV becomes the second in 02-03/02-04.

Output:
- `app_v2/templates/browse/index.html` panel-header right zone is empty (the `<div class="ms-auto d-flex...">` wrapper plus its `<span id="grid-count">` are deleted).
- A new `{% block footer %}...{% endblock footer %}` is added at the page level (outside the `.shell`/`.panel` containers) carrying `<span id="grid-count" class="text-muted small" aria-live="polite">...</span>` with the same conditional rendering logic as before.
- The `{% block count_oob %}` (lines 73-77 of current browse/index.html) is unchanged — HTMX merges OOB by id regardless of new DOM location.
- Test invariants enforce that the count is in the footer, the OOB id is unchanged, and POST /browse/grid still emits a valid `count_oob` fragment.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/02-ui-shell-rewrite-browse-footer-joint-validation-layout-parit/02-CONTEXT.md
@.planning/phases/02-ui-shell-rewrite-browse-footer-joint-validation-layout-parit/02-UI-SPEC.md
@.planning/phases/02-ui-shell-rewrite-browse-footer-joint-validation-layout-parit/02-01-PLAN.md
@app_v2/templates/browse/index.html
@app_v2/templates/base.html

<interfaces>
<!-- Existing browse view-model fields (BrowseGridViewModel) consumed by this template. -->
<!-- Source: app_v2/services/browse_grid_service.py — these fields exist and are stable. -->

vm.is_empty_selection: bool       # When True, count caption is omitted (existing contract)
vm.n_rows: int                    # Platform count (rendered as "{n} platforms")
vm.n_cols: int                    # Parameter count (rendered as "{n} parameters")
vm.selected_platforms: list[str]  # Used by picker_badges_oob (Plan 02-02 leaves this byte-stable)
vm.selected_params: list[str]     # Same — byte-stable
vm.params_disabled: bool          # Same — byte-stable

<!-- Existing browse/index.html structure that this plan EDITS (lines from current file): -->
<!-- Lines 26-37: .shell > .panel > .panel-header { <b>Browse</b> + <span class="tag"> + <div class="ms-auto d-flex"><span id="grid-count">...</span></div> } -->
<!--   → Plan deletes the .ms-auto d-flex wrapper and its inner #grid-count span. -->
<!-- Lines 42-66: filter form, _filter_bar.html include, .browse-grid-body, {% block grid %} — BYTE-STABLE. -->
<!-- Lines 73-77: {% block count_oob %} — BYTE-STABLE (same id="grid-count" merge target). -->
<!-- Lines 82-102: warnings_oob, picker_badges_oob — BYTE-STABLE. -->
<!-- Lines 117-137: params_picker_oob, params_picker — BYTE-STABLE. -->

<!-- New block to add (per UI-SPEC §Browse Page Contract): -->
{% block footer %}
<span id="grid-count" class="text-muted small" aria-live="polite">
  {% if not vm.is_empty_selection %}{{ vm.n_rows }} platforms &times; {{ vm.n_cols }} parameters{% endif %}
</span>
{% endblock footer %}

<!-- Existing browse router behavior (app_v2/routers/browse.py:135) — DO NOT EDIT in this plan. -->
<!-- block_names = ["grid", "count_oob", "warnings_oob", "picker_badges_oob"] — already includes count_oob. -->
<!-- Footer is NOT included in block_names — partial fragment responses do not return the footer block (correct: HTMX doesn't swap the footer; it merges count_oob by id). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate Browse count caption into the footer block</name>
  <files>app_v2/templates/browse/index.html, tests/v2/test_phase02_invariants.py</files>
  <read_first>
    - app_v2/templates/browse/index.html (full file — 138 lines)
    - app_v2/templates/base.html (verify Plan 02-01 added {% block footer %})
    - app_v2/routers/browse.py lines 95-150 (browse_grid POST handler — confirm block_names list shape; do NOT edit)
    - .planning/phases/02-ui-shell-rewrite-browse-footer-joint-validation-layout-parit/02-UI-SPEC.md §Browse Page Contract (Count Text Migration subsection)
    - .planning/phases/02-ui-shell-rewrite-browse-footer-joint-validation-layout-parit/02-CONTEXT.md D-UI2-06
    - tests/v2/test_browse_routes.py (skim — pattern for asserting on rendered Browse page strings)
  </read_first>
  <behavior>
    - Test 16 (test_browse_panel_header_no_count): `app_v2/templates/browse/index.html` does NOT contain the substring `class="ms-auto d-flex align-items-center gap-3"` (the wrapper that previously housed #grid-count is deleted).
    - Test 17 (test_browse_footer_block_carries_count): `app_v2/templates/browse/index.html` contains a `{% block footer %}...{% endblock footer %}` region that includes `<span id="grid-count"` AND `vm.n_rows` AND `vm.n_cols` AND `&times;`.
    - Test 17b (test_browse_grid_count_receiver_emitter_tag_alignment) — W7: both `block footer`'s and `block count_oob`'s `<span id="grid-count">` use IDENTICAL opening tag attribute order (HTMX `hx-swap-oob="true" outerHTML` replaces the receiver tag, including attributes — keeping shapes aligned avoids surprise on element-tag drift). Concretely:
        - `'<span id="grid-count" class="text-muted small" aria-live="polite">'` appears exactly once (the initial-render receiver inside `{% block footer %}`).
        - `'<span id="grid-count" hx-swap-oob="true" class="text-muted small" aria-live="polite">'` appears exactly once (the OOB emitter inside `{% block count_oob %}`).
    - Test 18 (test_browse_count_oob_unchanged): `app_v2/templates/browse/index.html` `{% block count_oob %}` block still renders `<span id="grid-count" hx-swap-oob="true"` with the same conditional `vm.is_empty_selection` guard. Single occurrence of `hx-swap-oob="true"` paired with `id="grid-count"`.
    - Test 19 (test_get_browse_renders_count_in_footer): TestClient GET /browse — the response body has `<footer class="site-footer"` and inside that footer (substring search after the `<footer class="site-footer"` index) contains `id="grid-count"` AND `platforms` (or empty when vm.is_empty_selection).
    - Test 20 (test_post_browse_grid_emits_count_oob): TestClient POST /browse/grid with sample form data — response body contains `id="grid-count"` AND `hx-swap-oob="true"` (OOB still emitted; merge-by-id mechanism works regardless of receiver location).
    - Test 21 (test_browse_panel_header_byte_stable_otherwise): `app_v2/templates/browse/index.html` panel-header still contains `<b>Browse</b>` AND `<span class="tag">Pivot grid</span>` (the title + tag are unchanged; only the count wrapper is removed).
  </behavior>
  <action>
    Edit `app_v2/templates/browse/index.html`. Three discrete edits:

    **Edit A (lines 32-36 — panel-header right zone):** Delete this block:
    ```html
          <div class="ms-auto d-flex align-items-center gap-3">
            <span id="grid-count" class="text-muted small" aria-live="polite">
              {% if not vm.is_empty_selection %}{{ vm.n_rows }} platforms &times; {{ vm.n_cols }} parameters{% endif %}
            </span>
          </div>
    ```
    Leave `<b>Browse</b>` (line 30) and `<span class="tag">Pivot grid</span>` (line 31) intact. The panel-header retains its existing CSS-driven flex layout.

    **Edit B (insert before the closing `{% endblock %}` of the main content block, around line 102 — i.e. AFTER `{% endblock picker_badges_oob %}` and BEFORE `{% endblock %}` that closes `block content`):** Add the new footer block:
    ```html

    {# Phase 02 D-UI2-06 — Count caption migrated from .panel-header into the
       sticky footer (Plan 02-01 introduced the slot). Same id="grid-count",
       same conditional rendering, same OOB merge target — only DOM location
       moves. The count_oob fragment below this still emits a <span> with the
       same id, so HTMX merges it correctly into the footer span by id. #}
    {% block footer %}
    <span id="grid-count" class="text-muted small" aria-live="polite">
      {% if not vm.is_empty_selection %}{{ vm.n_rows }} platforms &times; {{ vm.n_cols }} parameters{% endif %}
    </span>
    {% endblock footer %}
    ```

    Block placement note (W6 — corrected; defined-outside-content-block pattern from Phase 01-05): Place `{% block footer %}...{% endblock footer %}` IMMEDIATELY AFTER the `{% endblock %}` that closes `block content` (at the time of writing, line 103 of `browse/index.html`), in the same structural region as `block params_picker_oob` (lines 117-122 today) and `block params_picker` (lines 132-137 today). The footer block is OUTSIDE `block content` so jinja2-fragments rendering with `block_names=['grid', 'count_oob', 'warnings_oob', 'picker_badges_oob']` does NOT pull the footer into OOB responses. Concretely the order at the bottom of `browse/index.html` becomes: `{% endblock picker_badges_oob %}` → `{% endblock %}` (closing block content) → `{% block footer %}...{% endblock footer %}` → `{% block params_picker_oob %}...{% endblock params_picker_oob %}` → `{% block params_picker %}...{% endblock params_picker %}`. Verify by running the acceptance greps below before declaring done.

    **Edit C (no-op verification — `{% block count_oob %}` lines 73-77 must remain UNCHANGED):**
    ```html
    {% block count_oob %}
      <span id="grid-count" hx-swap-oob="true" class="text-muted small" aria-live="polite">
        {% if not vm.is_empty_selection %}{{ vm.n_rows }} platforms &times; {{ vm.n_cols }} parameters{% endif %}
      </span>
    {% endblock count_oob %}
    ```
    This block is byte-stable. HTMX merges by id regardless of where the receiver `<span id="grid-count">` lives in the DOM — Plan 02-01 introduces the receiver in the footer; this OOB block continues to target it.

    Then APPEND tests 16-21 to `tests/v2/test_phase02_invariants.py` (extend the file from Plan 02-01).

    Implementing per D-UI2-06: count text moves from panel-header into the sticky footer; OOB id `grid-count` is byte-stable.
  </action>
  <verify>
    <automated>cd /home/yh/Desktop/02_Projects/Proj28_PBM2 && python -m pytest tests/v2/test_phase02_invariants.py tests/v2/test_browse_routes.py -x -q 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'class="ms-auto d-flex align-items-center gap-3"' app_v2/templates/browse/index.html` outputs `0`.
    - `grep -c '{% block footer %}' app_v2/templates/browse/index.html` outputs `1`.
    - `grep -c '{% endblock footer %}' app_v2/templates/browse/index.html` outputs `1`.
    - `grep -c 'id="grid-count"' app_v2/templates/browse/index.html` outputs exactly `2` (one inside `block footer`, one inside `block count_oob`).
    - `grep -c '<b>Browse</b>' app_v2/templates/browse/index.html` outputs `1` (panel-header title preserved).
    - `grep -c '<span class="tag">Pivot grid</span>' app_v2/templates/browse/index.html` outputs `1` (panel-header tag preserved).
    - W6 footer placement (line above `{% block footer %}` is the closing `{% endblock %}` of block content): `grep -B1 '{% block footer %}' app_v2/templates/browse/index.html` shows the line above is the closing `{% endblock %}` of `block content` (NOT `{% endblock picker_badges_oob %}` directly).
    - W6 footer placement (line above `{% block params_picker_oob %}` is `{% endblock footer %}`): `grep -B1 '{% block params_picker_oob %}' app_v2/templates/browse/index.html` shows the line above is `{% endblock footer %}`.
    - W7 receiver tag alignment (initial render): `grep -c '<span id="grid-count" class="text-muted small" aria-live="polite">' app_v2/templates/browse/index.html` outputs exactly `1`.
    - W7 emitter tag alignment (OOB render): `grep -c '<span id="grid-count" hx-swap-oob="true" class="text-muted small" aria-live="polite">' app_v2/templates/browse/index.html` outputs exactly `1`.
    - All tests 16-21 (including new Test 17b for receiver/emitter tag alignment) pass; tests 1-15 from Plan 02-01 remain green; `tests/v2/test_browse_routes.py` has no regressions.
  </acceptance_criteria>
  <done>Browse's count caption renders inside the new footer on GET /browse; the OOB swap on POST /browse/grid still merges the count by id; the panel-header is otherwise byte-stable; the 21-test invariant suite is green and existing browse routes tests are intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → POST /browse/grid | HTMX form submission carrying user-selected platforms/params. Existing Phase 04 router escapes via Jinja `| e` and form validation. This plan does not alter the input handling. |
| Browser → GET /browse | Unauthenticated read; query-string filter values pass through Phase 04 validation. This plan does not alter that pipeline. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02-01 | Tampering | OOB swap target id `grid-count` | mitigate | The `id="grid-count"` is hard-coded in BOTH the receiver (`block footer` in browse/index.html) AND the OOB emitter (`block count_oob`). Test 18 + Test 20 enforce single-id consistency. HTMX merges by id; relocating the receiver to the footer does NOT introduce a duplicate-id risk because the receiver in the panel-header was DELETED in Edit A. |
| T-02-02-02 | Information Disclosure | Count caption text in footer | accept | The count is `vm.n_rows` and `vm.n_cols` — already publicly visible to any authenticated browse user. No new exposure surface. |
| T-02-02-03 | XSS | Footer span rendering vm.n_rows / vm.n_cols | accept | `vm.n_rows` and `vm.n_cols` are integers from the BrowseGridViewModel — no Jinja `| e` needed for `int` rendering, but Jinja's autoescape is enabled at the template loader level (existing project default). Same rendering as the previous in-panel-header span; no behavioral change. |
| T-02-02-04 | Tampering (T-02-04 from phase context) | DOM id collision during HTMX swap | mitigate | Edit A deletes the panel-header `<span id="grid-count">` so on every full-page render, only ONE `<span id="grid-count">` exists in the rendered DOM (in the footer). Test 19 enforces single-occurrence; Test 16 verifies the panel-header wrapper is gone. No intermediate state has duplicate ids. |

No high-severity threats. Mitigations are inline assertions (single-id contract) backed by tests 18, 19, 20.
</threat_model>

<verification>
- 21 invariant tests pass (15 from Plan 02-01 + 6 new in this plan).
- `tests/v2/test_browse_routes.py` continues green — POST /browse/grid still emits valid `count_oob` HTML.
- Manual smoke: GET /browse → count appears in the footer (visible bottom of page). Apply a filter via the picker → POST /browse/grid → footer count updates without page reload.
- `grep -c 'id="grid-count"' app_v2/templates/browse/index.html` returns `2` (one in footer, one in count_oob).
</verification>

<success_criteria>
- D-UI2-06: count text lives in `{% block footer %}` of `browse/index.html`; OOB swap mechanism unchanged.
- Browse panel-header is otherwise byte-stable (`<b>Browse</b>` + `Pivot grid` tag preserved).
- HTMX merge-by-id continues to update the count after every filter change, with the receiver living in the footer.
- All 21 invariant tests + existing browse routes tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/02-ui-shell-rewrite-browse-footer-joint-validation-layout-parit/02-02-SUMMARY.md`.
</output>
