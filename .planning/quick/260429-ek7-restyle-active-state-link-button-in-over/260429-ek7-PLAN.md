---
phase: 260429-ek7
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app_v2/templates/overview/_grid.html
autonomous: false
requirements:
  - QUICK-260429-EK7
must_haves:
  truths:
    - "Active-state Link button renders a chain icon (bi-link-45deg) that is near-black, not muted grey."
    - "Active-state Link button's visible 'Link' label text is near-black, not muted grey."
    - "Active-state Link button is visually distinguishable from the disabled-state Link button (disabled stays muted grey)."
    - "Active-state Link button retains identical geometry (border, padding, height, border-radius) to the AI button on its right."
    - "Disabled-state Link button (no row.link) is unchanged — still bi-link-45deg + text-muted."
    - "AI button block (lines 89-102) is unchanged."
  artifacts:
    - path: "app_v2/templates/overview/_grid.html"
      provides: "Restyled active-state Link button inside {% if row.link %} branch"
      contains: "bi-link-45deg text-dark"
  key_links:
    - from: "app_v2/templates/overview/_grid.html (active Link <a>)"
      to: "Bootstrap text-dark utility"
      via: "class attribute on <i> and <span> children"
      pattern: "text-dark"
    - from: "app_v2/templates/overview/_grid.html (active Link <a>)"
      to: "Bootstrap Icons chain glyph"
      via: "<i class> on the icon child"
      pattern: "bi-link-45deg"
---

<objective>
Restyle the active-state Link button in the Overview Actions cell so the icon and label read as near-black (clickable affordance) instead of looking like a "solid grey fill". Two surgical edits inside the `{% if row.link %}` branch only — disabled branch and AI button stay byte-stable.

Purpose: User feedback — the active Link button currently uses the open-in-new-tab arrow icon (`bi-box-arrow-up-right`) and inherits Bootstrap's outline-secondary muted text colour, which made it look indistinguishable from the disabled state. Switching to the chain icon (`bi-link-45deg`, matching the user's 🔗 ask) plus `text-dark` on both icon and label restores the active-vs-disabled contrast while preserving identical button geometry to the AI button on its right.

Output: Updated `app_v2/templates/overview/_grid.html` with active-state Link button using `bi-link-45deg text-dark` icon + `<span class="text-dark">Link</span>` label, and the comment block above the Link button updated to mention the active-state chain-icon + dark-text treatment.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@app_v2/templates/overview/_grid.html

<interfaces>
<!-- Current active-state Link button (lines 69-78) — to be modified -->
<!-- Current disabled-state Link button (lines 79-87) — DO NOT TOUCH -->
<!-- Current AI button block (lines 89-102) — visual reference, DO NOT TOUCH -->

Active branch (TO MODIFY):
```html
{% if row.link %}
  <a href="{{ row.link | e }}"
     class="btn btn-sm btn-outline-secondary me-1"
     target="_blank"
     rel="noopener noreferrer"
     title="Open {{ row.link | e }}"
     aria-label="Open external link for {{ row.title | e }}">
    <i class="bi bi-box-arrow-up-right" aria-hidden="true"></i>
    Link
  </a>
{% else %}
```

Disabled branch (REFERENCE — already correct, DO NOT MODIFY):
```html
<a class="btn btn-sm btn-outline-secondary disabled me-1"
   role="button"
   tabindex="-1"
   aria-disabled="true"
   title="No link configured in frontmatter">
  <i class="bi bi-link-45deg text-muted" aria-hidden="true"></i>
  <span class="text-muted">Link</span>
</a>
```

Bootstrap utilities relied on (already loaded globally via base.html):
- `text-dark` — sets color to near-black (Bootstrap 5.3 token `--bs-dark`)
- `text-muted` — preserved on disabled branch unchanged
- `bi-link-45deg` — chain icon glyph (already in use on disabled branch; same icon font already loaded)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle active Link button — chain icon + dark text on icon and label</name>
  <files>app_v2/templates/overview/_grid.html</files>
  <action>
Make exactly two surgical edits inside the `{% if row.link %}` branch (currently lines 69-78), plus a comment update above the Link button block. Do NOT touch the `{% else %}` disabled branch (lines 79-87) and do NOT touch the AI button block (lines 89-102).

Edit A — line 76 (icon):
- BEFORE: `<i class="bi bi-box-arrow-up-right" aria-hidden="true"></i>`
- AFTER:  `<i class="bi bi-link-45deg text-dark" aria-hidden="true"></i>`

Why `bi-link-45deg`: matches the chain glyph the user asked for (🔗) and matches the disabled branch's icon, so active vs disabled differ only by colour — not by glyph identity. Why `text-dark`: Bootstrap's `btn-outline-secondary` sets the icon colour via inherited button text colour, which is muted; explicitly applying `text-dark` on the `<i>` overrides the inherited colour to near-black. `aria-hidden="true"` preserved verbatim per constraint.

Edit B — line 77 (visible label):
- BEFORE: `              Link`
- AFTER:  `              <span class="text-dark">Link</span>`

Why wrap in `<span class="text-dark">`: the bare text node inherits the button's outline-secondary muted colour. Wrapping in a span lets us scope `text-dark` to the label without touching the anchor's class list (which would conflict with `btn btn-sm btn-outline-secondary me-1` and risk breaking the geometry contract that matches the AI button — that class list MUST stay byte-stable on the `<a>` tag itself).

Edit C — comment block above the Link button (currently lines 62-68):
Lightly update the comment so future readers understand the visual intent. Keep the existing D-OV-16 reference and the explanation of disabled state; ADD a sentence noting that the active state uses `bi-link-45deg text-dark` (chain icon + near-black) while the disabled state uses `bi-link-45deg text-muted` so the two states differ only by colour. Replace the existing 7-line comment (lines 62-68) with:

```
{# Link button — D-OV-16. Opens row.link (sanitized service-side
   to http(s) only; bare domains promoted to https://). When
   frontmatter has no `link` key, renders as a Bootstrap
   `.disabled` anchor with no href and a tooltip explaining why.
   Active state: bi-link-45deg + text-dark (chain icon + near-black
   on icon and label) so it reads as clickable. Disabled state:
   bi-link-45deg + text-muted (same chain glyph, lighter colour) so
   the two states differ only by colour. Both states share
   `btn btn-sm btn-outline-secondary me-1` so border / padding /
   height / border-radius continue to match the AI button.
   The .md file is NOT reachable from this button anymore — the
   title cell is the canonical handle for the platform's content
   page (see <td> at the top of this row). #}
```

Constraints (verbatim from quick-task brief):
- No CSS file changes — Bootstrap utilities `text-dark` and `text-muted` are already loaded globally via base.html
- Do NOT modify the disabled branch (lines 79-87 in the current file) — already correct
- Do NOT modify the AI button block (lines 89-102) — visual reference
- Preserve aria attributes verbatim (`aria-hidden="true"` on icon, `aria-label="Open external link for {{ row.title | e }}"` on the anchor)
- Keep the anchor's `class="btn btn-sm btn-outline-secondary me-1"` exactly as-is so geometry continues to match the AI button on its right
- Keep `target="_blank"`, `rel="noopener noreferrer"`, `href`, `title` attributes verbatim
  </action>
  <verify>
    <automated>grep -nE 'bi-link-45deg text-dark|&lt;span class="text-dark"&gt;Link&lt;/span&gt;' app_v2/templates/overview/_grid.html | wc -l</automated>
  </verify>
  <done>
- File `app_v2/templates/overview/_grid.html` contains exactly one `bi-link-45deg text-dark` occurrence (active branch icon)
- File contains exactly one `<span class="text-dark">Link</span>` occurrence (active branch label)
- File still contains `bi-link-45deg text-muted` (disabled branch icon) AND `<span class="text-muted">Link</span>` (disabled branch label) — both byte-stable
- File no longer contains `bi-box-arrow-up-right` (the old icon glyph)
- AI button block (the `<button ...>...AI</button>` element) is byte-stable
- Anchor on the active branch still carries `class="btn btn-sm btn-outline-secondary me-1"` exactly (no extra utilities, no removed utilities)
- Comment block above the Link button mentions both `bi-link-45deg text-dark` (active) and `bi-link-45deg text-muted` (disabled) so the two-state intent is documented
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Browser HUMAN-UAT — confirm restyled active Link button</name>
  <what-built>Restyled active-state Link button in the Overview Actions cell. Active state now uses the chain icon (bi-link-45deg) with text-dark on both icon and label, while the disabled state continues to use the same chain icon with text-muted. Both states share `btn btn-sm btn-outline-secondary me-1` so geometry (border, padding, height, border-radius) remains identical to the AI button on the right.</what-built>
  <how-to-verify>
1. Start the v2 app: `uvicorn app_v2.main:app --reload --port 8000`
2. Visit `http://localhost:8000/overview` (or `http://localhost:8000/`)
3. Confirm the Overview grid renders at least one row with a Link button (a curated platform whose content frontmatter contains a `link:` key). If no such row exists, edit one `content/platforms/<pid>.md` file's frontmatter to add a `link: https://example.com` and reload.
4. Visually inspect the Actions cell:
   - Active Link button shows a chain icon (🔗-style) plus the word "Link" — both in near-black, NOT muted grey.
   - Active Link button border / height / padding matches the AI button immediately to its right (eyeball alignment along the bottom edge).
   - On a row whose frontmatter has no `link:` key, the disabled Link button still shows the chain icon and "Link" in muted grey — unchanged from before.
   - AI button to the right is unchanged in every visible respect.
5. Hover the active Link button — tooltip still shows "Open <url>" verbatim.
6. Click the active Link button — opens the URL in a new tab (target=_blank still works).

Expected outcome: active state reads clearly as clickable (near-black icon + label), disabled state reads as not-clickable (muted grey icon + label), AI button geometry untouched.
  </how-to-verify>
  <resume-signal>Type "approved" if the restyle matches the user's intent, or describe what still looks off (e.g., "icon still too light", "label not aligned with AI button", "geometry shifted").</resume-signal>
</task>

</tasks>

<verification>
- File-level grep: exactly one `bi-link-45deg text-dark`, exactly one `<span class="text-dark">Link</span>`, zero `bi-box-arrow-up-right` in `app_v2/templates/overview/_grid.html`
- File-level grep: `bi-link-45deg text-muted` and `<span class="text-muted">Link</span>` still present (disabled branch byte-stable)
- File-level grep: AI button block (`hx-post="/platforms/{{ row.platform_id | e }}/summary"` … `data-bs-target="#summary-modal"` … `<span aria-hidden="true">✨</span>`) byte-stable
- Browser HUMAN-UAT (Task 2) signs off on the visual outcome
</verification>

<success_criteria>
- Active-state Link button renders chain icon + "Link" label in near-black (text-dark)
- Disabled-state Link button unchanged (bi-link-45deg + text-muted on icon and label)
- AI button block byte-stable
- Active and AI buttons remain geometrically identical (same `btn btn-sm btn-outline-secondary` class on the anchor / button)
- HUMAN-UAT approval recorded
</success_criteria>

<output>
After completion, create `.planning/quick/260429-ek7-restyle-active-state-link-button-in-over/260429-ek7-SUMMARY.md` capturing:
- The two surgical edits (icon class + label wrapper)
- The comment update describing the two-state intent
- HUMAN-UAT outcome
- Confirmation that the disabled branch and AI button are byte-stable
</output>
