---
quick_task: 260507-s5c
type: execute
wave: 1
status: planned
autonomous: true
files_modified:
  - app_v2/services/joint_validation_grid_service.py
  - app_v2/routers/overview.py
  - app_v2/templates/overview/_filter_bar.html
  - app_v2/templates/overview/index.html
  - app_v2/static/css/app.css
  - config/presets.example.yaml
  - tests/v2/test_joint_validation_grid_service.py
  - tests/v2/test_joint_validation_routes.py
  - tests/v2/test_joint_validation_invariants.py
  - tests/v2/test_phase02_invariants.py
  - tests/v2/test_overview_presets.py
must_haves:
  truths:
    - "JV listing filter bar shows 6 picker popovers in left-to-right order: Customer, AP Company, AP Model, Device, Controller, Application"
    - "Selecting an AP Model value narrows the JV grid to rows whose ap_model column matches (set membership; multi-select OR within column)"
    - "Selected AP Model values render as chips in the active-filter summary, with a unique color slot (c-3) distinct from the surrounding facets"
    - "AP Model selections survive the URL roundtrip — bookmarking /overview?ap_model=SM8450 re-applies the filter on reload"
    - "Filter changes still reset to page 1 (form-association invariant unchanged)"
    - "Existing 5 filters keep working; full v2 suite (~580 tests) stays green after invariant + chip-palette test updates"
  artifacts:
    - path: "app_v2/services/joint_validation_grid_service.py"
      provides: "FILTERABLE_COLUMNS 6-tuple including ap_model in position 3"
      contains: '"customer", "ap_company", "ap_model", "device", "controller", "application"'
    - path: "app_v2/routers/overview.py"
      provides: "ap_model threaded through _parse_filter_dict, _build_overview_url, GET /overview, POST /overview/grid, GET /overview/preset/{name}"
    - path: "app_v2/templates/overview/_filter_bar.html"
      provides: "6 picker_popover(...) calls, ap_model inserted between ap_company and device"
    - path: "app_v2/templates/overview/index.html"
      provides: "filter_badges_oob block updated: 6-key labels/variants maps, 6-element for-loop, palette c-1..c-6"
  key_links:
    - from: "_filter_bar.html ap_model picker"
      to: "vm.filter_options['ap_model']"
      via: "picker_popover macro options arg"
      pattern: "name=\"ap_model\""
    - from: "_parse_filter_dict"
      to: "build_joint_validation_grid_view_model filters arg"
      via: "dict[str, list[str]] with 'ap_model' key"
      pattern: "ap_model="
    - from: "index.html filter_badges_oob"
      to: "selected_filters['ap_model']"
      via: "Jinja for-loop over 6-tuple"
      pattern: "\"ap_model\""
---

<objective>
Add **AP Model** as a 6th filter facet on the Joint Validation listing page (`/overview` route), inserted between AP Company and Device in the filter bar order. The mirror-image of quick-260507-rmj which reduced 6→5 facets by dropping Status.

Purpose: PMs/analysts need to slice JV rows by AP Model (e.g. SM8450, MT6989) — a high-cardinality value already parsed and rendered in the grid as a sortable column, but currently invisible to the filter UI. Surface it as a popover-checklist filter using the same picker_popover macro as the other 5 facets.

Scope is purely "expose existing data as a filter":
- `ap_model` is **already** parsed by `joint_validation_parser.py` (line 188), already stored on `JointValidationRow` (line 114), already in `SORTABLE_COLUMNS` (line 73), already rendered as a `<th>`/`<td>` column in `_grid.html`. The parser, the row model, and the grid template need ZERO changes.
- The work is wiring `ap_model` into `FILTERABLE_COLUMNS` and propagating that 5→6 expansion across the same 6 sites that 260507-rmj contracted in reverse: service constants, router signatures + URL builder, filter bar template, filter-badge OOB block, chip palette comment, presets header comment.

Output: 6-facet JV filter bar with AP Model wired end-to-end (UI → URL → form → service → view-model → chips), invariant tests bumped from 5→6, palette extended c-1..c-6, all v2 tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/260507-rmj-in-browse-page-ditch-status-and-end-colu/260507-rmj-SUMMARY.md
@app_v2/services/joint_validation_grid_service.py
@app_v2/services/joint_validation_parser.py
@app_v2/routers/overview.py
@app_v2/templates/overview/_filter_bar.html
@app_v2/templates/overview/index.html
@app_v2/templates/overview/_grid.html
@app_v2/static/css/app.css
@config/presets.example.yaml
@tests/v2/test_joint_validation_grid_service.py
@tests/v2/test_joint_validation_routes.py
@tests/v2/test_joint_validation_invariants.py
@tests/v2/test_phase02_invariants.py
@tests/v2/test_overview_presets.py

<interfaces>
<!-- Key contracts the executor needs. Already verified by the planner. -->

From app_v2/services/joint_validation_parser.py (NO changes — verify only):
```python
class ParsedJV(BaseModel):
    title: str = ""
    status: str = ""
    customer: str = ""
    model_name: str = ""
    ap_company: str = ""
    ap_model: str = ""        # Already parsed (label "AP Model" — line 45 of _FIELD_LABELS)
    device: str = ""
    controller: str = ""
    application: str = ""
    assignee: str = ""
    start: str = ""
    end: str = ""
    link: str = ""
```
Confirmed by grep: 6 of 6 real fixture folders under `content/joint_validation/319386810[09]/` and `3193868[2-6]00/` contain `<strong>AP Model</strong>` rows with values like `SM8450`. Older `3193869*` fixtures also include AP Model.

From app_v2/services/joint_validation_grid_service.py (lines 64-66 — what to expand):
```python
FILTERABLE_COLUMNS: Final[tuple[str, ...]] = (
    "customer", "ap_company", "device", "controller", "application",
)
```
Target 6-tuple (this plan):
```python
FILTERABLE_COLUMNS: Final[tuple[str, ...]] = (
    "customer", "ap_company", "ap_model", "device", "controller", "application",
)
```

From app_v2/services/joint_validation_grid_service.py — `JointValidationRow` (NO changes):
```python
class JointValidationRow(BaseModel):
    confluence_page_id: str = Field(..., pattern=r"^\d+$")
    title: str = ""
    status: str = ""
    customer: str = ""
    model_name: str = ""
    ap_company: str = ""
    ap_model: str = ""        # Already on the row — used by sort + grid render today
    device: str = ""
    ...
```

From app_v2/templates/browse/_picker_popover.html — macro signature (NO changes — reused as-is):
```jinja
{% macro picker_popover(name, label, options, selected, form_id, hx_post, hx_target, disable_auto_commit=False, disabled=False) %}
```
Each `picker_popover(name="ap_model", label="AP Model", ...)` call must mirror the existing 5 verbatim (D-UI2-09 / D-JV-11 byte-stable contract preserved).

From app_v2/static/css/app.css (lines 1199-1204) — palette already has 6 slots:
```css
.ff-chip.c-1 { background: var(--accent-soft); color: var(--accent-ink); }
.ff-chip.c-2 { background: var(--green-soft);  color: var(--green); }
.ff-chip.c-3 { background: var(--violet-soft); color: var(--violet); }
.ff-chip.c-4 { background: var(--amber-soft);  color: var(--amber); }
.ff-chip.c-5 { background: var(--red-soft);    color: var(--red); }
.ff-chip.c-6 { background: #f4f6f8;            color: var(--ink-2); }
```
Only the **comment** above line 1199 needs updating (currently says "c-1 status, c-2 customer..." — pre-260507-rmj). The rules themselves are byte-stable.

Final palette mapping (post-this-plan):
```
customer    → c-1
ap_company  → c-2
ap_model    → c-3   (NEW)
device      → c-4   (was c-3)
controller  → c-5   (was c-4)
application → c-6   (was c-5)
```
</interfaces>

<impacted_test_pinning>
<!-- Tests that pin the OLD 5-facet shape. The executor MUST update these. -->

`tests/v2/test_joint_validation_invariants.py::test_jv_filter_bar_uses_picker_popover_macro_unmodified` (line 305):
  - `assert fb.count("picker_popover(") >= 5` → `>= 6`
  - Comment "post-260507-rmj — Status dropped" → "post-260507-s5c — AP Model added between AP Company and Device"

`tests/v2/test_phase02_invariants.py::test_overview_filter_bar_six_picker_calls` (line 542-555):
  - `assert count == 5` → `== 6`
  - Docstring: "byte-stable post-260507-rmj" → "byte-stable post-260507-s5c (5→6 with ap_model insert)"
  - The function name `test_overview_filter_bar_six_picker_calls` was originally written for 6 — git-blame symmetry is restored.

`tests/v2/test_joint_validation_grid_service.py`:
  - `test_active_filter_counts_match_input` (line 110-122): expected dict expands by one key:
    ```python
    {"customer": 2, "ap_company": 1, "ap_model": 0,
     "device": 0, "controller": 0, "application": 0}
    ```
  - `test_five_filter_options_enumerated_from_full_set` (line 78): rename → `test_six_filter_options_enumerated_from_full_set` (mirror-image of 260507-rmj's rename); function body OK as-is — it's still just exercising customer.
  - `test_invalid_sort_col_falls_back_to_default` (line 125): no change — ap_model is already sortable, this test pins removed-from-sortable behaviors only.

`tests/v2/test_joint_validation_routes.py`:
  - `test_overview_filter_chips_render_actual_values` (line 366-395): no chip-color shift for `customer` (still c-1) or `ap_company` (still c-2). The function is OK as-is.
  - `test_overview_filter_chips_no_active_filters_renders_empty_wrapper` (line 449-454): probe range `c-1..c-5` → `c-1..c-6`:
    ```python
    for variant in ("c-1", "c-2", "c-3", "c-4", "c-5", "c-6"):
        assert f'class="ff-chip {variant}"' not in r.text, variant
    ```
  - `_write_many_jv_customer_values` helper (line 340): NO change — still uses customer (c-1) for the overflow probe.

`tests/v2/test_overview_presets.py`:
  - `test_get_overview_preset_overrides_filters_and_returns_oob_blocks` (line 211-238): the `qualcomm-wearables` preset selects `ap_company=Qualcomm` (still c-2) and `application=Wearable` — application shifts c-5 → **c-6** because ap_model takes c-3, pushing device→c-4, controller→c-5, application→c-6:
    ```python
    assert 'class="ff-chip c-6">Wearable</span>' in r.text
    ```
    Update the inline comment too.
  - Other preset tests (loader test line 87-91, malformed entries, override tests) — no facet-position assertions; unaffected.

`tests/v2/test_jv_pagination.py::test_filter_options_built_from_all_rows_not_paged`:
  - Already migrated to use `customer` facet by 260507-rmj. **No change** — it doesn't pin any facet count.
</impacted_test_pinning>

</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire ap_model into service + router (5→6 facets)</name>
  <files>
    app_v2/services/joint_validation_grid_service.py,
    app_v2/routers/overview.py,
    tests/v2/test_joint_validation_grid_service.py
  </files>
  <action>
**Goal:** Insert `ap_model` as the 3rd entry in `FILTERABLE_COLUMNS` (between `ap_company` and `device`) and propagate the expansion through the router's 5 sites that touch the facet tuple. Mirror-image of commit `e575163` from 260507-rmj.

**Edits:**

1) **`app_v2/services/joint_validation_grid_service.py`** — `FILTERABLE_COLUMNS` 5-tuple → 6-tuple:
   - Line 64-66:
     ```python
     FILTERABLE_COLUMNS: Final[tuple[str, ...]] = (
         "customer", "ap_company", "ap_model", "device", "controller", "application",
     )
     ```
   - Update the comment block above (lines 59-63): change "5 filterable columns (260507-rmj reduces from 6 by dropping Status)" → "6 filterable columns (260507-s5c restores to 6 by inserting AP Model between AP Company and Device; 260507-rmj had reduced to 5 by dropping Status)". State explicitly: "Title, Model Name, Start are NOT filterable" (drop the now-stale "AP Model" mention from the not-filterable list).
   - Update module docstring D-JV-11 line (line 23-25 in the docstring): "5 popover-checklist filters (customer, ap_company, device, controller, application — 260507-rmj dropped Status)" → "6 popover-checklist filters (customer, ap_company, ap_model, device, controller, application — 260507-s5c inserted AP Model after AP Company; 260507-rmj had dropped Status)".
   - Update D-JV-10 reference if needed: SORTABLE_COLUMNS already contains `ap_model` (line 73-75) — verify no doc drift, NO code change needed there.
   - **DO NOT touch** `JointValidationRow` (already has `ap_model` field), `ALL_METADATA_KEYS`, parser, store, sort algorithm, or pagination. ALL of these are already ap_model-aware.
   - In the public `build_joint_validation_grid_view_model` orchestrator (around line 415-433), the `JointValidationRow(...)` construction already passes `ap_model=parsed.ap_model` (line 426). NO change.
   - The `filter_options` builder loop (line 439-446) iterates `for col_name in FILTERABLE_COLUMNS` — automatically picks up the new `ap_model` key. NO code change beyond the tuple expansion.
   - `active_filter_counts` builder (line 492-494) — same: automatic via `for c in FILTERABLE_COLUMNS`.
   - `_normalize_filters` (line 233-250) — automatic via `for col in FILTERABLE_COLUMNS`.
   - The `_row_matches` predicate (line 449-453) is already `getattr(r, fcol, None)` so it works for any column on JointValidationRow including `ap_model`.

2) **`app_v2/routers/overview.py`** — 5 wiring sites:

   a) `_parse_filter_dict` (lines 48-68): add `ap_model: list[str]` parameter between `ap_company` and `device`; add `"ap_model": ap_model` entry between the existing two in the returned dict. Update the docstring's "Filter columns enumerated explicitly..." paragraph: "5 filter form lists" → "6 filter form lists"; "260507-rmj dropped status from the 6-tuple" → "260507-s5c inserted ap_model into the 5-tuple (post-260507-rmj reality), restoring the 6-tuple shape with a different ordering".

   b) `_build_overview_url` (lines 71-107): line 93 — extend the iteration tuple to include `"ap_model"` between `"ap_company"` and `"device"`:
      ```python
      for col in ("customer", "ap_company", "ap_model", "device", "controller", "application"):
      ```
      Update the docstring's "260507-rmj: status was dropped from the facet tuple" closing line: append "260507-s5c: ap_model inserted after ap_company".

   c) `get_overview` (lines 110-175): add `ap_model: Annotated[list[str], Query(default_factory=list)],` parameter between `ap_company` and `device` (line ~122). Pass `ap_model=ap_model` to the `_parse_filter_dict(...)` call at line 142 — though since the helper now takes positional/keyword args, passing positionally between `ap_company` and `device` is also valid (mirror existing call style — current code uses positional args).
      - Current call (line 142): `filters = _parse_filter_dict(customer, ap_company, device, controller, application)`
      - New call: `filters = _parse_filter_dict(customer, ap_company, ap_model, device, controller, application)`

   d) `post_overview_grid` (lines 178-247): add `ap_model: Annotated[list[str], Form()] = [],` parameter between `ap_company` and `device` (line ~187). Pass `ap_model` positionally to `_parse_filter_dict(...)` at line 208 — same shape as (c).

   e) `get_overview_preset` (lines 250-326): in the `_parse_filter_dict(...)` invocation (lines 289-295), add `ap_model=pf.get("ap_model", [])` between `ap_company` and `device`:
      ```python
      filters = _parse_filter_dict(
          customer=pf.get("customer", []),
          ap_company=pf.get("ap_company", []),
          ap_model=pf.get("ap_model", []),
          device=pf.get("device", []),
          controller=pf.get("controller", []),
          application=pf.get("application", []),
      )
      ```
      Update the inline comment: "260507-rmj: status was dropped from the facet set." → append "260507-s5c: ap_model added to the facet set (between ap_company and device)."

3) **`tests/v2/test_joint_validation_grid_service.py`** — 2 test bumps:

   a) `test_active_filter_counts_match_input` (line 110-122): the expected dict gains an `ap_model: 0` key (the test does NOT pass an ap_model filter, so the count must be 0). Update both the body and the docstring comment to mention 260507-s5c:
      ```python
      assert vm.active_filter_counts == {
          "customer": 2, "ap_company": 1, "ap_model": 0,
          "device": 0, "controller": 0, "application": 0,
      }
      ```
      Comment updates: "active_filter_counts now has 5 keys (was 6)" → "active_filter_counts now has 6 keys (was 5 post-260507-rmj; 260507-s5c re-added ap_model)".

   b) `test_five_filter_options_enumerated_from_full_set` (line 78-86): rename to `test_six_filter_options_enumerated_from_full_set` (mirror-image rename of 260507-rmj's `test_six → test_five`). Body unchanged — still uses customer to exercise the contract; only the test name + comment updated. Comment: "260507-rmj: status was dropped..." → "260507-s5c: AP Model added back so the listing has 6 facets again..."

   c) `test_invalid_sort_col_falls_back_to_default` (line 125): NO change. ap_model has been sortable since Phase 1; only the dropped-from-sort fields probe.

   d) `test_view_model_shape` (line 51-57): NO change — uses `set(vm.filter_options.keys()) == set(FILTERABLE_COLUMNS)` which is shape-agnostic; automatically picks up the new key.

**Why interface-first ordering?** The service tuple + router signatures are the contract. Templates (Task 2) consume them. Doing this task first means Task 2 has a known-green backend to test against.

**Verification — run during this task:**
  - `pytest tests/v2/test_joint_validation_grid_service.py -x` (touched module, ~30 tests).
  - `pytest tests/v2/test_joint_validation_routes.py::test_get_overview_with_filters_round_trip_url -x` to confirm router signatures still parse query params; this test does not yet probe ap_model and will still pass.
  - `python -c "from app_v2.services.joint_validation_grid_service import FILTERABLE_COLUMNS; assert FILTERABLE_COLUMNS == ('customer','ap_company','ap_model','device','controller','application'); print('OK')"`
  - `python -c "from app_v2.routers.overview import _parse_filter_dict; d = _parse_filter_dict(['c'],['ac'],['am'],['d'],['ct'],['ap']); assert d == {'customer':['c'],'ap_company':['ac'],'ap_model':['am'],'device':['d'],'controller':['ct'],'application':['ap']}; print('OK')"`
  </action>
  <verify>
    <automated>cd /home/yh/Desktop/02_Projects/Proj28_PBM2 && pytest tests/v2/test_joint_validation_grid_service.py -x -q</automated>
  </verify>
  <done>
- `FILTERABLE_COLUMNS` is a 6-tuple with `ap_model` at index 2.
- `_parse_filter_dict` accepts 6 list args and returns a 6-key dict; `_build_overview_url` iterates 6 keys.
- `get_overview`, `post_overview_grid`, `get_overview_preset` all accept ap_model and thread it into the service.
- `test_active_filter_counts_match_input` asserts `ap_model: 0`; `test_six_filter_options_enumerated_from_full_set` (renamed) green.
- `pytest tests/v2/test_joint_validation_grid_service.py` green.
- `pytest tests/v2/test_joint_validation_routes.py` green for non-template tests (template-specific tests covered in Task 3).
  </done>
</task>

<task type="auto">
  <name>Task 2: Add 6th picker_popover + extend filter-badges OOB to 6 facets + chip-palette comment refresh</name>
  <files>
    app_v2/templates/overview/_filter_bar.html,
    app_v2/templates/overview/index.html,
    app_v2/static/css/app.css,
    config/presets.example.yaml
  </files>
  <action>
**Goal:** UI surface — render the AP Model picker in the filter bar (between AP Company and Device), extend the filter-badge OOB block from 5 → 6 facet rows with the palette renumbered so ap_model claims `c-3` and the rest shift down, refresh the chip-palette comment in `app.css` to match the new mapping, and append `ap_model` to the presets YAML header comment for documentation honesty.

**Edits:**

1) **`app_v2/templates/overview/_filter_bar.html`** — insert a 6th `picker_popover` invocation. Copy the **AP Company** block (lines 38-48) verbatim, paste it BETWEEN the AP Company block and the Device block (i.e. after line 48, before line 50), then s/ap_company/ap_model/g and s/AP Company/AP Model/g in the new copy.
   - Top-of-file docstring (lines 1-11): update "5 picker dropdowns" → "6 picker dropdowns" and the closing parenthetical "260507-rmj dropped the Status picker" → "260507-rmj dropped the Status picker; 260507-s5c re-added the AP Model picker between AP Company and Device".
   - Verify resulting picker order top-to-bottom: customer, ap_company, **ap_model**, device, controller, application = 6 invocations.

2) **`app_v2/templates/overview/index.html`** — extend the `filter_badges_oob` block (lines 69-103):
   - **`ff_labels` map** (lines 70-76): add `"ap_model": "AP Model",` between `"ap_company"` and `"device"`:
     ```jinja
     {%- set ff_labels = {
         "customer":    "Customer",
         "ap_company":  "AP Company",
         "ap_model":    "AP Model",
         "device":      "Device",
         "controller":  "Controller",
         "application": "Application",
     } -%}
     ```
   - **`ff_variants` map** (lines 77-83): renumber so customer/ap_company keep their slots and the rest shift down by one to make room for ap_model at c-3:
     ```jinja
     {%- set ff_variants = {
         "customer":    "c-1",
         "ap_company":  "c-2",
         "ap_model":    "c-3",
         "device":      "c-4",
         "controller":  "c-5",
         "application": "c-6",
     } -%}
     ```
   - **For-loop literal** (line 85): expand the 5-element list to 6 with ap_model in position 3:
     ```jinja
     {% for col in ["customer", "ap_company", "ap_model", "device", "controller", "application"] %}
     ```
   - **Surrounding comment block** (lines 50-68): update the post-260507-rmj paragraph to also describe 260507-s5c: change "Chip color is determined per-facet via .c-1..c-5 in app.css" → "Chip color is determined per-facet via .c-1..c-6 in app.css"; change "260507-rmj: dropped the Status facet; surviving 5 facets..." → "260507-rmj: dropped the Status facet; 260507-s5c: re-added AP Model between AP Company and Device, restoring 6 facets (palette c-1..c-6: customer, ap_company, ap_model, device, controller, application)".
   - **DO NOT touch** the panel-header, preset chip strip, grid block, or pagination block. They are already 6-facet-agnostic.

3) **`app_v2/static/css/app.css`** — update the chip-palette mapping comment ONLY (lines 1195-1198):
   - From:
     ```
     /* 6 deterministic palette slots — one per FILTERABLE_COLUMNS entry.
        c-1 status, c-2 customer, c-3 ap_company, c-4 device, c-5 controller,
        c-6 application. Mapping is done in the template via a Jinja dict so
        both color and slot stay byte-stable. */
     ```
   - To:
     ```
     /* 6 deterministic palette slots — one per FILTERABLE_COLUMNS entry.
        Post-260507-s5c (and post-260507-rmj which dropped Status):
        c-1 customer, c-2 ap_company, c-3 ap_model, c-4 device,
        c-5 controller, c-6 application. Mapping is done in the template
        via a Jinja dict so both color and slot stay byte-stable. */
     ```
   - **DO NOT touch** the `.ff-chip.c-1`..`.ff-chip.c-6` declarations themselves (lines 1199-1204) — they are byte-stable and already provide all 6 slots; this is a doc-only edit.

4) **`config/presets.example.yaml`** — header comment refresh (line 7):
   - From: `#   filters: dict keyed by FILTERABLE_COLUMNS (customer, ap_company, device,`
     `#            controller, application). Each value is a list of strings —`
   - To: `#   filters: dict keyed by FILTERABLE_COLUMNS (customer, ap_company, ap_model,`
     `#            device, controller, application). Each value is a list of strings —`
   - Append a new comment line after the existing `# (260507-rmj — Status was dropped...)` note:
     `# (260507-s5c — AP Model re-added between AP Company and Device — listing has 6 facets again.)`
   - **DO NOT add** `ap_model:` keys to any of the three seed presets (`korean-oems-in-progress`, `qualcomm-wearables`, `pending-ufs4`). User explicitly only asked for the filter; presets stay unmodified seed data. The preset_store loader already accepts unknown facet absences (returns `[]` for any FILTERABLE_COLUMNS entry not mentioned in the YAML).

**Verification — run during this task:**
  - `python -c "import re; src = open('app_v2/templates/overview/_filter_bar.html').read(); assert src.count('picker_popover(') == 6, src.count('picker_popover('); assert 'name=\"ap_model\"' in src; assert src.find('name=\"ap_company\"') < src.find('name=\"ap_model\"') < src.find('name=\"device\"'); print('OK')"`
  - `python -c "src = open('app_v2/templates/overview/index.html').read(); assert '\"ap_model\":    \"AP Model\"' in src; assert '\"ap_model\":    \"c-3\"' in src; assert 'application\": \"c-6\"' in src; print('OK')"`
  - Smoke: `python -c "from fastapi.testclient import TestClient; from app_v2.main import app; c = TestClient(app); r = c.get('/overview', params=[('ap_model','SM8450')]); print(r.status_code); assert r.status_code == 200; assert 'data-facet=\"ap_model\"' in r.text or 'SM8450' not in str(r.text)  # if real fixtures don\'t have SM8450 the chip won\'t render but the page must still 200"`
  - `pytest tests/v2/test_joint_validation_invariants.py -x` will FAIL on the 5→6 picker count test until Task 3 updates it; that is expected and Task 3 fixes it.
  </action>
  <verify>
    <automated>cd /home/yh/Desktop/02_Projects/Proj28_PBM2 && python -c "src = open('app_v2/templates/overview/_filter_bar.html').read(); assert src.count('picker_popover(') == 6; assert 'name=\"ap_model\"' in src; assert src.find('name=\"ap_company\"') < src.find('name=\"ap_model\"') < src.find('name=\"device\"'); idx = open('app_v2/templates/overview/index.html').read(); assert '\"ap_model\":    \"AP Model\"' in idx and '\"ap_model\":    \"c-3\"' in idx and '\"application\": \"c-6\"' in idx; print('UI WIRED')"</automated>
  </verify>
  <done>
- `_filter_bar.html` contains 6 picker_popover invocations, ap_model in 3rd position.
- `index.html` filter_badges_oob block declares ap_model in both `ff_labels` and `ff_variants`, with palette mapping customer→c-1, ap_company→c-2, ap_model→c-3, device→c-4, controller→c-5, application→c-6.
- `app.css` palette comment reflects the post-260507-s5c mapping (rules untouched).
- `config/presets.example.yaml` header comment lists `ap_model` in the FILTERABLE_COLUMNS enumeration; seed presets unchanged.
- `GET /overview` returns 200; `GET /overview?ap_model=SM8450` returns 200 with the chip rendered when sample data has matching values.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update invariant + chip-palette tests for 6-facet reality + run full suite</name>
  <files>
    tests/v2/test_joint_validation_invariants.py,
    tests/v2/test_phase02_invariants.py,
    tests/v2/test_joint_validation_routes.py,
    tests/v2/test_overview_presets.py
  </files>
  <action>
**Goal:** Bump grep-count and chip-palette pinning tests from the 5-facet shape to the 6-facet shape so the full v2 suite is green. Mirror-image of commit `03e6fca` from 260507-rmj.

**Edits:**

1) **`tests/v2/test_joint_validation_invariants.py::test_jv_filter_bar_uses_picker_popover_macro_unmodified`** (line 299-312):
   - Line 305: `assert fb.count("picker_popover(") >= 5` → `>= 6`.
   - Line 306: `"Filter bar must invoke picker_popover 5 times (D-JV-11 post-260507-rmj — Status dropped)"` → `"Filter bar must invoke picker_popover 6 times (D-JV-11 post-260507-s5c — AP Model added; post-260507-rmj — Status dropped)"`.
   - Update the section-header comment block above (lines 293-296): "(260507-rmj reduced from 6 by dropping the Status picker)" → "(260507-rmj reduced from 6 by dropping Status; 260507-s5c restored to 6 by adding AP Model between AP Company and Device — net facet set differs from pre-260507-rmj)".

2) **`tests/v2/test_phase02_invariants.py::test_overview_filter_bar_six_picker_calls`** (line 542-555):
   - Line 551: `assert count == 5` → `== 6`.
   - Lines 543-548 (docstring): "Test 30: ... must contain exactly 5 occurrences of picker_popover( (byte-stable post-260507-rmj). Originally 6 (D-UI2-09); 260507-rmj dropped the Status picker, leaving customer / ap_company / device / controller / application = 5 calls. Test name kept for git-blame continuity." → "Test 30: ... must contain exactly 6 occurrences of picker_popover( (byte-stable post-260507-s5c). D-UI2-09 originally 6; 260507-rmj dropped Status (5); 260507-s5c added AP Model (back to 6, customer / ap_company / ap_model / device / controller / application). Test name now matches reality again."
   - Line 552-554 (assertion message): mirror the docstring update — "must contain exactly 6 picker_popover( calls" + reflect the 260507-s5c history.

3) **`tests/v2/test_joint_validation_routes.py`** — chip-palette range probe in `test_overview_filter_chips_no_active_filters_renders_empty_wrapper` (line 449-454):
   - Line 449 comment: `(.c-1..c-5 post-260507-rmj)` → `(.c-1..c-6 post-260507-s5c)`.
   - Line 453: extend the variant tuple:
     ```python
     for variant in ("c-1", "c-2", "c-3", "c-4", "c-5", "c-6"):
         assert f'class="ff-chip {variant}"' not in r.text, variant
     ```
   - `test_overview_filter_chips_render_actual_values` (line 366-395): NO change — customer still c-1 and ap_company still c-2 post-260507-s5c (those slots don't shift).
   - `test_overview_filter_chips_overflow_shows_plus_n_more` (line 398-428): NO change — uses customer (c-1), still c-1.

4) **`tests/v2/test_overview_presets.py::test_get_overview_preset_overrides_filters_and_returns_oob_blocks`** (line 211-238):
   - Line 222-223 (inline comment): "260507-rmj: palette shifted up after dropping Status from facets — ap_company is now c-2 (was c-3) and application is now c-5 (was c-6)." → "260507-rmj: palette shifted up after dropping Status; 260507-s5c: ap_model inserted at c-3 so device→c-4, controller→c-5, application→c-6 (ap_company stays c-2 — unaffected by the insertion below it)."
   - Line 227: `assert 'class="ff-chip c-5">Wearable</span>' in r.text` → `assert 'class="ff-chip c-6">Wearable</span>' in r.text`.
   - Other assertions (ap_company c-2, push URL, no status/customer): NO change.

5) **(if any other test pins c-N for a facet that shifted from c-3..c-5 to c-4..c-6)** — run a sweep:
   ```bash
   grep -rn 'class="ff-chip c-' tests/v2/
   ```
   - Expected hits after edits: chips for customer (c-1 — unchanged), ap_company (c-2 — unchanged), application (c-6 in `test_get_overview_preset_overrides_filters_and_returns_oob_blocks`), and the empty-wrapper variant probe in `test_overview_filter_chips_no_active_filters_renders_empty_wrapper`. Any other test pinning c-3/c-4/c-5 against device/controller MUST be updated to c-4/c-5/(unchanged for controller wait — let's recompute: controller c-4 → c-5). NOTE: controller WAS c-4 post-260507-rmj and IS c-5 post-260507-s5c. If any test pins controller to c-4, it must become c-5. Check grep results carefully.

**Auto-fix discipline:** if grep finds a test pinning c-N for a facet whose slot shifted (device c-3→c-4, controller c-4→c-5, application c-5→c-6) that is NOT in the files_modified list, add the file to the commit and update with a `# 260507-s5c: palette slot shifted by ap_model insertion` comment — same pattern as 260507-rmj's auto-fix `Rule 1 - Bug` triage.

**Final verification — run after all edits:**
  - `pytest tests/v2/ -x -q` — full v2 suite must be green (target: 580 passed, same as post-260507-rmj baseline; net delta zero because tests are bumped, not added/removed).
  - `pytest tests/v2/test_joint_validation_invariants.py tests/v2/test_phase02_invariants.py tests/v2/test_joint_validation_routes.py tests/v2/test_joint_validation_grid_service.py tests/v2/test_overview_presets.py tests/v2/test_jv_pagination.py -x -q` — touched-module subset, all green.
  - Live smoke: start the app, visit `/overview`, see 6 pickers in order Customer, AP Company, AP Model, Device, Controller, Application; click an AP Model value (e.g. "SM8450" if present in the unstaged `3193868109/`...`3193868600/` fixtures); observe rows narrowed and an `ff-chip c-3` chip rendering "AP Model: SM8450" in the active-filter row; the URL becomes `/overview?ap_model=SM8450&sort=start&order=desc`.
  </action>
  <verify>
    <automated>cd /home/yh/Desktop/02_Projects/Proj28_PBM2 && pytest tests/v2/ -x -q</automated>
  </verify>
  <done>
- `test_jv_filter_bar_uses_picker_popover_macro_unmodified` passes with `>= 6`.
- `test_overview_filter_bar_six_picker_calls` passes with `== 6` (test name finally matches reality).
- `test_overview_filter_chips_no_active_filters_renders_empty_wrapper` probes c-1..c-6.
- `test_get_overview_preset_overrides_filters_and_returns_oob_blocks` asserts application chip on c-6.
- Full `pytest tests/v2/` green; net test-count delta is 0 (renames + literal bumps only).
- `grep -rn 'class="ff-chip c-' tests/v2/` shows no surviving c-N pin that contradicts the post-260507-s5c palette mapping.
  </done>
</task>

</tasks>

<verification>
- **Service contract:** `FILTERABLE_COLUMNS == ('customer','ap_company','ap_model','device','controller','application')` (6-tuple).
- **Router contract:** `_parse_filter_dict` 6 args; `_build_overview_url` iterates 6 facets; `get_overview` / `post_overview_grid` / `get_overview_preset` all accept ap_model.
- **Template contract:** `_filter_bar.html` has 6 picker_popover invocations, ap_model in position 3 (between ap_company and device).
- **Filter-badges contract:** `index.html` filter_badges_oob block iterates 6 facets, palette c-1..c-6 with ap_model claiming c-3.
- **CSS contract:** chip-palette comment in `app.css` reflects the post-260507-s5c mapping; rules byte-stable.
- **Test invariants:** picker count tests bumped 5→6; chip-palette tests probe c-1..c-6; preset chip test asserts application on c-6.
- **Live smoke (manual):** `/overview` shows 6 pickers in correct order; clicking an AP Model value narrows the grid + renders the chip in the active-filter row + updates the URL.
- **Net test count:** 580 → 580 (no tests added or removed; only renames and literal bumps).
</verification>

<success_criteria>
- 6-facet filter bar renders correctly on `/overview`.
- Selecting an AP Model value filters the JV grid (server-side, set-membership semantics).
- Selected AP Model values appear as chips in the active-filter summary with the c-3 color slot.
- URL roundtrip works: `?ap_model=X` survives bookmark/reload.
- `pytest tests/v2/` is green.
- All 5 existing facets (customer, ap_company, device, controller, application) keep working byte-equally.
- Chip palette mapping in `app.css` comment matches the 6-facet runtime mapping.
- `presets.example.yaml` header comment lists ap_model in the FILTERABLE_COLUMNS enumeration; seed presets unchanged.
</success_criteria>

<output>
After completion, create `.planning/quick/260507-s5c-in-jv-page-add-ap-model-filter-between-a/260507-s5c-SUMMARY.md` documenting:
- The 6 source/template/CSS/YAML files changed and the test files bumped (mirror of 260507-rmj's 12-file footprint, minus the parser/store/grid/pagination tests because ap_model was already wired through those).
- 3 atomic commits (Task 1 service+router, Task 2 templates+CSS+yaml, Task 3 test invariants — same shape as 260507-rmj's e575163/b2215b4/03c9717 trio).
- Test count: 580 → 580 (no delta).
- Decisions logged: ap_model already on the row + already sortable + already in the grid template, so the change surface is purely "expose-as-filter wiring" (no parser/model/grid/pagination changes); chip palette extended c-1..c-5 → c-1..c-6 with ap_model claiming c-3 and the trailing 3 facets shifting one slot down.
- Disambiguation note: this is the JV listing (active_tab=overview, route /overview), NOT the Browse pivot grid; mirrors the disambiguation captured in 260507-rmj's SUMMARY.
- Verification snapshot table mirroring 260507-rmj's: picker count 5→6, FILTERABLE_COLUMNS len 5→6, chip variants c-1..c-5 → c-1..c-6, etc.
</output>
