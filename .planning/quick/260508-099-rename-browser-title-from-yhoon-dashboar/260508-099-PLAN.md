---
phase: quick-260508-099
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app_v2/templates/base.html
  - tests/v2/test_phase04_uif_components.py
  - tests/v2/test_content_routes.py
autonomous: true
requirements:
  - QT-260508-099
must_haves:
  truths:
    - "Browser tab title for routes with no page_title renders as 'Platform Dashboard V1 — Platform Dashboard V1' (no 'Yhoon Dashboard', no 'v2.0' suffix)"
    - "Routes with a page_title (e.g. /platforms/{pid}) render as '{page_title} — Platform Dashboard V1'"
    - "Topbar wordmark assertion in test_phase04_uif_components.py matches the live topbar text 'Platform Dashboard V1' (test goes from RED to GREEN)"
    - "Title-suffix assertion in test_content_routes.py matches the new ' — Platform Dashboard V1</title>' string (test stays GREEN under the new suffix)"
    - "Full v2 test suite (`pytest tests/v2/`) is fully green — no failures, no new skips, no regressions in the other ~590 tests"
  artifacts:
    - path: "app_v2/templates/base.html"
      provides: "<title> tag and topbar comment using new wordmark"
      contains: "Platform Dashboard V1"
    - path: "tests/v2/test_phase04_uif_components.py"
      provides: "Topbar wordmark assertion (line 38) updated to new wordmark"
      contains: "Platform Dashboard V1"
    - path: "tests/v2/test_content_routes.py"
      provides: "Title-suffix docstring + assertion (lines 104, 109) updated to new suffix"
      contains: "Platform Dashboard V1</title>"
  key_links:
    - from: "app_v2/templates/base.html:7"
      to: "tests/v2/test_content_routes.py:109"
      via: "<title> string assertion"
      pattern: " — Platform Dashboard V1</title>"
    - from: "app_v2/templates/_components/topbar.html:16"
      to: "tests/v2/test_phase04_uif_components.py:38"
      via: "topbar wordmark substring assertion"
      pattern: ">Platform Dashboard V1<"
---

<objective>
Rename the browser `<title>` from "Yhoon Dashboard" to "Platform Dashboard V1" so the tab name matches the topbar wordmark, and drop the trailing " v2.0" suffix per user decision. Update the two v2 tests that pin the old strings so the suite stays green.

Purpose: The topbar already shows "Platform Dashboard V1" (shipped earlier this session); the `<title>` is the only remaining surface still showing the legacy "Yhoon Dashboard" wordmark. Bringing the tab title in lockstep removes the visual mismatch between the browser tab and the page header. The test that asserts ">Yhoon Dashboard<" against the topbar markup is currently RED (it pre-existed before this task — flagged in quick task 260507-wf6 STATE notes); fixing the wordmark and test together flips it back to GREEN.
Output: 3 files modified; full v2 test suite green; one atomic commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@app_v2/templates/base.html
@app_v2/templates/_components/topbar.html
@tests/v2/test_phase04_uif_components.py
@tests/v2/test_content_routes.py

<interfaces>
<!-- Exact byte targets for each edit. Executor should match these literals
     character-for-character; no surrounding code restructuring. -->

`app_v2/templates/base.html` line 7 — current:
```html
  <title>{{ page_title | default("Yhoon Dashboard") }} — Yhoon Dashboard v2.0</title>
```
Target:
```html
  <title>{{ page_title | default("Platform Dashboard V1") }} — Platform Dashboard V1</title>
```
Both occurrences of "Yhoon Dashboard" become "Platform Dashboard V1". The trailing " v2.0" is removed (user explicitly chose this format despite the redundant duplication when no page_title is supplied — do NOT add a Jinja `{% if %}` branch to dedupe). Em-dash `—` (U+2014) byte-stable.

`app_v2/templates/base.html` line 55 — current:
```
     "Yhoon Dashboard" + tab strip (Joint Validation / Browse / Ask) + static "YH"
```
Target:
```
     "Platform Dashboard V1" + tab strip (Joint Validation / Browse / Ask) + static "YH"
```
Comment-only change; quoted wordmark literal updated to match the live topbar.

`tests/v2/test_phase04_uif_components.py` line 38 — current:
```python
    assert ">Yhoon Dashboard<" in body
```
Target:
```python
    assert ">Platform Dashboard V1<" in body
```
This test asserts against the topbar's `<span>` content (line 16 of `_components/topbar.html`), not against `<title>`. The topbar already says "Platform Dashboard V1", so this test is currently failing — flipping the literal turns it GREEN.

`tests/v2/test_content_routes.py` lines 104 + 109 — current:
```python
    """Page title contains platform_id and base.html '— Yhoon Dashboard v2.0' suffix
    (rebranded in quick task 260507-mmv; test name kept for git blame continuity)."""
    client, cd = isolated_content
    r = client.get(f"/platforms/{_PID}")
    assert r.status_code == 200
    assert f"<title>{_PID} — Yhoon Dashboard v2.0</title>" in r.text
```
Target:
```python
    """Page title contains platform_id and base.html '— Platform Dashboard V1' suffix
    (rebranded in quick task 260508-099; test name kept for git blame continuity)."""
    client, cd = isolated_content
    r = client.get(f"/platforms/{_PID}")
    assert r.status_code == 200
    assert f"<title>{_PID} — Platform Dashboard V1</title>" in r.text
```
Docstring quote string updated; quick-task reference rolled forward from 260507-mmv to 260508-099 (the most recent rebrand). f-string literal updated to the new suffix. Em-dash byte-stable.

`app_v2/templates/_components/topbar.html` — DO NOT EDIT. Reference only — the topbar wordmark on line 16 is already correct.
</interfaces>

Out-of-scope (constraint): No CSS changes, no router changes, no edits to `topbar.html` or any other test file. The only files allowed to be touched are the three listed in `files_modified`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update base.html title tag and stale comment to "Platform Dashboard V1"</name>
  <files>app_v2/templates/base.html</files>
  <action>
Make exactly two edits in `app_v2/templates/base.html`:

1. Line 7 — replace the entire `<title>` line:
   - From: `  <title>{{ page_title | default("Yhoon Dashboard") }} — Yhoon Dashboard v2.0</title>`
   - To:   `  <title>{{ page_title | default("Platform Dashboard V1") }} — Platform Dashboard V1</title>`

   Both `Yhoon Dashboard` literals become `Platform Dashboard V1`. The trailing ` v2.0` is removed. The user has explicitly chosen this format and is aware that, with no `page_title`, the tab will read `Platform Dashboard V1 — Platform Dashboard V1`. Do NOT introduce a Jinja conditional to dedupe — keep the structure byte-stable except for the string changes.

2. Line 55 — replace the quoted wordmark inside the existing comment:
   - From: `     "Yhoon Dashboard" + tab strip (Joint Validation / Browse / Ask) + static "YH"`
   - To:   `     "Platform Dashboard V1" + tab strip (Joint Validation / Browse / Ask) + static "YH"`

   Indentation, surrounding comment lines, em-dash characters, Jinja delimiters, and all other whitespace stay byte-identical.

Do NOT touch any other line in this file. Do NOT reformat. Do NOT remove the surrounding Phase 04 comment block. Do NOT modify `topbar.html` (already correct).
  </action>
  <verify>
    <automated>grep -c "Yhoon Dashboard" app_v2/templates/base.html | grep -qx 0 &amp;&amp; grep -c "Platform Dashboard V1" app_v2/templates/base.html | grep -qE "^[2-9]$|^[1-9][0-9]+$"</automated>
  </verify>
  <done>
- `grep "Yhoon Dashboard" app_v2/templates/base.html` returns 0 matches
- `grep -c "Platform Dashboard V1" app_v2/templates/base.html` returns at least 2 (line 7 has two occurrences; line 55 has one — total 3)
- Line 7 still contains the exact literal `{{ page_title | default("Platform Dashboard V1") }} — Platform Dashboard V1</title>`
- No other lines changed (`git diff --stat app_v2/templates/base.html` shows 2 lines changed)
  </done>
</task>

<task type="auto">
  <name>Task 2: Update both v2 tests to assert the new wordmark and title suffix</name>
  <files>tests/v2/test_phase04_uif_components.py, tests/v2/test_content_routes.py</files>
  <action>
Make exactly three string-literal edits across two test files. Do NOT change any other test, fixture, import, or comment.

**Edit A — `tests/v2/test_phase04_uif_components.py` line 38:**
- From: `    assert ">Yhoon Dashboard<" in body`
- To:   `    assert ">Platform Dashboard V1<" in body`

This assertion targets the topbar `<span>` content rendered by `_components/topbar.html:16`. The topbar already emits "Platform Dashboard V1", so this assertion is currently RED — the edit flips it GREEN. Test function name (`test_showcase_inherits_topbar`) stays unchanged.

**Edit B — `tests/v2/test_content_routes.py` line 104 (docstring):**
- From: `    """Page title contains platform_id and base.html '— Yhoon Dashboard v2.0' suffix`
- To:   `    """Page title contains platform_id and base.html '— Platform Dashboard V1' suffix`

**Edit C — `tests/v2/test_content_routes.py` line 105 (docstring continuation):**
- From: `    (rebranded in quick task 260507-mmv; test name kept for git blame continuity)."""`
- To:   `    (rebranded in quick task 260508-099; test name kept for git blame continuity)."""`

**Edit D — `tests/v2/test_content_routes.py` line 109 (assertion):**
- From: `    assert f"<title>{_PID} — Yhoon Dashboard v2.0</title>" in r.text`
- To:   `    assert f"<title>{_PID} — Platform Dashboard V1</title>" in r.text`

Em-dash (`—`, U+2014) is byte-stable. f-string syntax preserved. `_PID` reference unchanged. Function body otherwise byte-identical.

Do NOT update any other test in `test_content_routes.py` (this file has ~30+ tests; only the one with the title assertion gets touched). Do NOT modify `test_phase04_uif_components.py` outside line 38.
  </action>
  <verify>
    <automated>grep -c "Yhoon Dashboard" tests/v2/test_phase04_uif_components.py tests/v2/test_content_routes.py | grep -E ":0$" | wc -l | grep -qx 2 &amp;&amp; pytest tests/v2/test_phase04_uif_components.py::test_showcase_inherits_topbar tests/v2/test_content_routes.py::test_get_detail_includes_page_title -q</automated>
  </verify>
  <done>
- `grep "Yhoon Dashboard" tests/v2/test_phase04_uif_components.py tests/v2/test_content_routes.py` returns 0 matches across both files
- `grep "Platform Dashboard V1" tests/v2/test_phase04_uif_components.py tests/v2/test_content_routes.py` returns 3 matches total (1 in phase04 file, 2 in content_routes file: docstring + assertion)
- Targeted pytest `pytest tests/v2/test_phase04_uif_components.py::test_showcase_inherits_topbar tests/v2/test_content_routes.py::test_get_detail_includes_page_title -q` passes (both tests GREEN)
- `git diff --stat` shows only the two test files plus `base.html` changed in this plan
  </done>
</task>

<task type="auto">
  <name>Task 3: Run full v2 test suite to confirm zero regressions</name>
  <files></files>
  <action>
Execute the full v2 test suite to verify the rename is complete and no other tests broke:

```bash
pytest tests/v2/ -q
```

Expected outcome:
- All ~592 v2 tests pass (the count fluctuates as the suite grows; the absolute baseline from STATE.md "Quick Tasks Completed" line for 260508-009 is "591 v2 tests pass (3 pre-existing failures unrelated)")
- After this plan, those 3 pre-existing failures should be reduced — at minimum `test_showcase_inherits_topbar` flips from RED to GREEN (it was one of the 3 known pre-existing failures because the topbar wordmark was already updated but the test wasn't). Whether the other 2 are also resolved by this plan or remain pre-existing depends on what they assert; if they assert anything against the old "Yhoon Dashboard" string they will also flip GREEN.
- Zero NEW failures introduced. Zero new skips. Zero errors.

If any test fails:
1. Read the failure output
2. Confirm the failure is caused by stale "Yhoon Dashboard" or " v2.0" string assertions — if so, the failure is expected pre-existing tech-debt revealed by this rename, NOT a regression caused by this plan. Document any such case in the SUMMARY.
3. If the failure is unrelated (different file, different assertion), it is pre-existing and should be flagged in the SUMMARY as carry-over, not fixed in this plan (constraint: only the 3 listed files may be touched).

Do NOT touch any additional files to "fix" tests outside the three files in this plan. If any test asserts on the old strings in a file outside the plan's scope, escalate to the user rather than expanding scope.
  </action>
  <verify>
    <automated>pytest tests/v2/ -q 2>&amp;1 | tail -5 | grep -E "passed" &amp;&amp; ! pytest tests/v2/ -q 2>&amp;1 | tail -5 | grep -qE "[1-9][0-9]* failed"</automated>
  </verify>
  <done>
- `pytest tests/v2/ -q` exits 0
- Final summary line shows `N passed` (no `failed`, no `errored`)
- `test_showcase_inherits_topbar` and `test_get_detail_includes_page_title` both pass
- The PASSED count is at least equal to the prior baseline minus any pre-existing red tests resolved as a side effect (i.e. the count cannot decrease — only stay the same or increase as previously-RED tests turn GREEN)
  </done>
</task>

</tasks>

<verification>
Manual sanity check after the suite is green:

1. **Browser tab** (intranet smoke test, optional but recommended):
   - Start the v2 app and visit `http://localhost:8000/`. The browser tab should read `Platform Dashboard V1 — Platform Dashboard V1`.
   - Visit `http://localhost:8000/platforms/3193868109` (or any existing platform id from `content/joint_validation/`). The tab should read `3193868109 — Platform Dashboard V1`.
   - Visit `http://localhost:8000/browse`, `/ask`. Each tab title reflects whatever `page_title` the route emits, suffixed with ` — Platform Dashboard V1`.

2. **Repo-wide grep**:
   - `grep -rn "Yhoon Dashboard" app_v2/ tests/v2/` returns 0 matches.
   - `grep -rn "Yhoon Dashboard v2.0" .` outside `.planning/` returns 0 matches.

3. **Diff scope**:
   - `git diff --name-only` shows exactly 3 files: `app_v2/templates/base.html`, `tests/v2/test_phase04_uif_components.py`, `tests/v2/test_content_routes.py`. No CSS, no router, no other test files.
</verification>

<success_criteria>
- [ ] `app_v2/templates/base.html` line 7 reads `<title>{{ page_title | default("Platform Dashboard V1") }} — Platform Dashboard V1</title>`
- [ ] `app_v2/templates/base.html` line 55 comment quotes `"Platform Dashboard V1"` (not `"Yhoon Dashboard"`)
- [ ] `tests/v2/test_phase04_uif_components.py:38` asserts `">Platform Dashboard V1<" in body`
- [ ] `tests/v2/test_content_routes.py:109` asserts `f"<title>{_PID} — Platform Dashboard V1</title>" in r.text`
- [ ] `tests/v2/test_content_routes.py:104-105` docstring updated; quick-task reference rolled to `260508-099`
- [ ] `grep "Yhoon Dashboard" app_v2/ tests/v2/` returns 0 matches
- [ ] `pytest tests/v2/ -q` is fully green (all passed, no failed, no error)
- [ ] `git diff --name-only` shows exactly 3 files changed
- [ ] `app_v2/templates/_components/topbar.html` is byte-identical to its pre-plan state (verify with `git diff app_v2/templates/_components/topbar.html` → empty output)
- [ ] No CSS, router, or non-listed test files touched
</success_criteria>

<output>
After completion, create `.planning/quick/260508-099-rename-browser-title-from-yhoon-dashboar/260508-099-SUMMARY.md` documenting:
- The 3 string edits made (with before/after for each)
- Final v2 test pass count (vs. prior baseline of 591 passed + 3 pre-existing failures)
- Whether any of the 3 pre-existing failures resolved as a side effect of this rename
- Confirmation that `topbar.html` was not touched
</output>
