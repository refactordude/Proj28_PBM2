---
phase: 260429-kn7
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - streamlit_app.py
  - app/pages/__init__.py
  - app/pages/browse.py
  - app/pages/settings.py
  - app/components/__init__.py
  - app/components/export_dialog.py
  - app/services/ufs_service.py
  - app_v2/services/cache.py
  - app_v2/services/browse_service.py
  - tests/services/test_ufs_service.py
  - tests/services/test_ufs_service_core.py
  - tests/pages/__init__.py
  - tests/v2/test_browse_service.py
  - tests/v2/test_browse_routes.py
  - tests/v2/test_cache.py
  - tests/v2/test_phase06_invariants.py
  - requirements.txt
  - config/auth.yaml
  - .env.example
  - CLAUDE.md
  - .planning/PROJECT.md
autonomous: true
requirements:
  - SUNSET-V1-STREAMLIT
must_haves:
  truths:
    - "Zero `import streamlit` / `from streamlit` lines anywhere under app/, app_v2/, tests/"
    - "Zero `@st.cache_data` / `@st.cache_resource` / `@st.dialog` decorators anywhere under app/"
    - "`streamlit_app.py`, `app/pages/`, `app/components/`, `tests/pages/`, `config/auth.yaml` no longer exist on disk"
    - "`requirements.txt` no longer pins streamlit, streamlit-authenticator, nest-asyncio, bcrypt"
    - "`app/services/ufs_service.py` exposes canonical `list_platforms` / `list_parameters` / `fetch_cells` / `pivot_to_wide` (no `*_core` suffix); the `*_core` aliases no longer exist"
    - "`pytest tests/ -q` exits 0 with the v1.0 test surface purged"
    - "`python -c 'import app_v2.main'` succeeds without ModuleNotFoundError"
    - "CLAUDE.md and .planning/PROJECT.md describe FastAPI + Bootstrap + HTMX as the canonical UI (no Streamlit framing in 'What This Is' / 'Tech Stack' / 'Constraints')"
  artifacts:
    - path: "app/services/ufs_service.py"
      provides: "Streamlit-free canonical functions: list_platforms, list_parameters, fetch_cells, pivot_to_wide"
      contains: "def list_platforms"
    - path: "app_v2/services/cache.py"
      provides: "TTLCache wrappers importing canonical (no `_core`) names"
      contains: "from app.services.ufs_service import"
    - path: "app_v2/services/browse_service.py"
      provides: "Browse view-model assembler importing canonical pivot_to_wide"
      contains: "from app.services.ufs_service import pivot_to_wide"
    - path: "tests/services/test_ufs_service.py"
      provides: "Framework-agnostic ufs_service tests (no streamlit import)"
      contains: "def test_"
    - path: "requirements.txt"
      provides: "FastAPI-only runtime deps"
      contains: "fastapi"
    - path: "CLAUDE.md"
      provides: "FastAPI-framed project description"
      contains: "FastAPI"
  key_links:
    - from: "app_v2/services/cache.py"
      to: "app/services/ufs_service.py"
      via: "from app.services.ufs_service import list_platforms, list_parameters, fetch_cells"
      pattern: "from app\\.services\\.ufs_service import"
    - from: "app_v2/services/browse_service.py"
      to: "app/services/ufs_service.py"
      via: "from app.services.ufs_service import pivot_to_wide"
      pattern: "from app\\.services\\.ufs_service import pivot_to_wide"
    - from: "tests/v2/test_phase06_invariants.py"
      to: "(absence of streamlit_app.py)"
      via: "filesystem assert"
      pattern: "streamlit_app\\.py"
    - from: "tests/v2/test_cache.py"
      to: "app_v2/services/cache.py (renamed _uncached patch targets)"
      via: "patch('app_v2.services.cache._list_platforms_uncached', ...) etc."
      pattern: "_list_platforms_uncached|_list_parameters_uncached|_fetch_cells_uncached"
---

<objective>
Sunset v1.0 Streamlit shell so app_v2 (FastAPI + Bootstrap + HTMX) is the single source of truth for PBM2's UI. v1.0 Ask was already deleted in Phase 6 D-22 (commit bb584c7); this completes the sunset by removing the remaining Streamlit pages, components, entry point, dependencies, and all `@st.cache_data` / `*_core` scaffolding.

Purpose: Eliminate dead UI code, drop ~3 unused dependencies, and align the documented tech stack with the deployed reality. No behavior change — every v2.0 route, the NL agent, and all v2.0 tests continue to pass with the framework-agnostic `app/` layer (config / adapters / agent / services) preserved verbatim.

Output: A repo where `grep -rE "^\s*import streamlit" .` returns zero hits across `app/`, `app_v2/`, `tests/`; pytest stays green; CLAUDE.md and PROJECT.md describe FastAPI as the canonical UI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/PROJECT.md
@CLAUDE.md
@streamlit_app.py
@app/services/ufs_service.py
@app_v2/services/cache.py
@app_v2/services/browse_service.py
@tests/services/test_ufs_service.py
@tests/services/test_ufs_service_core.py
@tests/v2/test_phase06_invariants.py
@tests/v2/test_phase04_invariants.py
@tests/v2/test_browse_service.py
@tests/v2/test_cache.py
@requirements.txt
@.env.example

<interfaces>
<!-- Canonical names that ufs_service.py will expose AFTER Task 2's refactor.
     Every v2.0 importer in this plan updates to these (no `_core` suffix). -->

After Task 2, app/services/ufs_service.py exports exactly:
```python
def list_platforms(db: DBAdapter, db_name: str = "") -> list[str]: ...
def list_parameters(db: DBAdapter, db_name: str = "") -> list[dict]: ...
def fetch_cells(
    db: DBAdapter,
    platforms: tuple[str, ...],
    infocategories: tuple[str, ...],
    items: tuple[str, ...],
    row_cap: int = 200,
    db_name: str = "",
) -> tuple[pd.DataFrame, bool]: ...
def pivot_to_wide(
    df_long: pd.DataFrame,
    swap_axes: bool = False,
    col_cap: int = 30,
) -> tuple[pd.DataFrame, bool]: ...
```

The `*_core` aliases / wrappers and the `pivot_to_wide_core = pivot_to_wide` alias are GONE.
The `_TABLE`/`_ALLOWED_TABLES` settings load (committed yesterday in 260429-kc1) is preserved verbatim — only the Streamlit decoration layer changes.

v2.0 import sites that MUST be updated in lockstep with Task 2:
- app_v2/services/cache.py: imports `fetch_cells_core, list_parameters_core, list_platforms_core` → renames to `fetch_cells as _fetch_cells_uncached, list_parameters as _list_parameters_uncached, list_platforms as _list_platforms_uncached`
- app_v2/services/browse_service.py: imports `pivot_to_wide_core` → renames to `pivot_to_wide`
- tests/v2/test_browse_service.py: `monkeypatch.setattr(browse_service, "pivot_to_wide_core", ...)` → `pivot_to_wide`
- tests/v2/test_browse_routes.py: comment-only reference, update for accuracy
- tests/v2/test_cache.py: 15 `patch("app_v2.services.cache.{list_platforms_core|list_parameters_core|fetch_cells_core}", ...)` sites → rename to `_list_platforms_uncached` / `_list_parameters_uncached` / `_fetch_cells_uncached` (matching the `_uncached`-aliased import names in cache.py)
</interfaces>

<constraints_from_research>
Stays untouched (D-22 #5 keep-list + Phase 6 invariants):
- app/core/agent/nl_service.py, app/core/agent/nl_agent.py
- app/adapters/llm/pydantic_model.py
- app/core/config.py + Settings/load_settings
- app/services/{result_normalizer,sql_validator,sql_limiter,path_scrubber,ollama_fallback}.py
- app/adapters/db/* and app/adapters/llm/* (excluding the deleted Streamlit consumers)
- All app_v2/ source (only the 2 service files import-rename in lockstep)
- All tests/v2/ except the 4 files explicitly listed in files_modified (test_browse_service.py, test_browse_routes.py, test_cache.py, test_phase06_invariants.py)
- All tests/agent/ and the 5 framework-agnostic service test files

Out of scope:
- Renaming app/ → app_core/ (would invalidate every v2.0 import; not surgical)
- Auth re-enablement (D-04 deferral continues — config/auth.yaml is dropped because nothing imports it)
- v2.0 Browse export resurrection (D-19..D-22 stand)
- Versioning — user tags v2.1 separately AFTER this plan ships and tests pass
</constraints_from_research>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete v1.0 Streamlit UI surface and dead config files; flip Phase 6 streamlit_app.py invariant in the same atomic commit</name>
  <files>
    streamlit_app.py (DELETE),
    app/pages/__init__.py (DELETE),
    app/pages/browse.py (DELETE),
    app/pages/settings.py (DELETE),
    app/components/__init__.py (DELETE),
    app/components/export_dialog.py (DELETE),
    tests/pages/__init__.py (DELETE),
    config/auth.yaml (DELETE),
    .env.example (EDIT — remove the `# AUTH_PATH=config/auth.yaml` line),
    tests/v2/test_phase06_invariants.py (EDIT — flip lines 200-202 to assert non-existence)
  </files>
  <action>
    Delete the entire Streamlit UI surface AND flip the invariant that reads streamlit_app.py — all in one atomic commit. These files are NOT imported by anything in `app_v2/` or by the framework-agnostic `app/core/`, `app/adapters/`, or `app/services/{result_normalizer,sql_validator,sql_limiter,path_scrubber,ollama_fallback}.py` modules.

    Why the invariant flip belongs HERE (not in Task 3): if streamlit_app.py is deleted in commit A but the invariant `streamlit_src = (REPO / "streamlit_app.py").read_text()` is not flipped until commit B, then between A and B the test raises FileNotFoundError. That breaks the plan's "green at every SHA" principle. The atomic move is delete-file-and-update-its-invariant-together.

    Steps:
    1. `git rm streamlit_app.py`
    2. `git rm -r app/pages/` (browse.py, settings.py, __init__.py — Ask was already deleted in D-22 commit bb584c7)
    3. `git rm -r app/components/` (export_dialog.py + __init__.py — only consumed by the now-deleted browse.py per `tests/v2/test_phase04_invariants.py:60` D-22 invariant)
    4. `git rm -r tests/pages/` (only `__init__.py` remains; the v1.0 page tests were purged in D-22)
    5. `git rm config/auth.yaml` — it was for streamlit-authenticator only. `grep -rn "auth\.yaml\|streamlit_authenticator" app/ app_v2/ tests/ --include='*.py'` confirms zero v2.0 importers. The file is also gitignored per FOUND-03.
    6. Edit `.env.example`: remove the two-line block `# Optional: override the default auth.yaml path.` + `# AUTH_PATH=config/auth.yaml` (lines 9–10). KEEP `OPENAI_API_KEY`, `SETTINGS_PATH`, `LOG_DIR`.
    7. Edit `tests/v2/test_phase06_invariants.py` lines 200-202 — replace:
       ```python
           # streamlit_app.py must not reference the deleted file
           streamlit_src = (REPO / "streamlit_app.py").read_text()
           assert "app/pages/ask.py" not in streamlit_src, "streamlit_app.py still references the deleted Ask page"
       ```
       with:
       ```python
           # streamlit_app.py was deleted in quick task 260429-kn7 (v1.0 sunset)
           assert not (REPO / "streamlit_app.py").exists(), \
               "streamlit_app.py must not exist after v1.0 Streamlit sunset (quick task 260429-kn7)"
       ```
       The surrounding D-22 #5 keep-list assertions (nl_service.py, nl_agent.py, pydantic_model.py existence) at lines 204-210 stay verbatim — those modules are still preserved.

    Why per file:
    - `streamlit_app.py` — the v1.0 entry point that builds `st.navigation([...Browse, Settings])`. Replaced by `app_v2/main.py` mounting FastAPI routers.
    - `app/pages/browse.py` — v1.0 Browse with `@st.cache_resource get_db_adapter`, sidebar widgets, Pivot/Detail/Chart tabs. Replaced by `app_v2/routers/browse.py` + `app_v2/services/browse_service.py`.
    - `app/pages/settings.py` — v1.0 DB/LLM CRUD with `@st.dialog`. Replaced by `app_v2/routers/settings.py`.
    - `app/components/export_dialog.py` — v1.0 export modal with `@st.dialog`. Already banned from app_v2 by D-22 (Phase 4 invariant). v2.0 Browse is view-only per D-19.
    - `config/auth.yaml` — streamlit-authenticator demo creds. Auth re-enablement (D-04) will pick a fresh credential strategy.
    - `tests/v2/test_phase06_invariants.py` — the original assertion was a safety check that streamlit_app.py never re-references the deleted v1.0 Ask page. After the v1.0 sunset, the stronger invariant is "streamlit_app.py itself no longer exists." The test now enforces sunset rather than half-deletion.

    Use `git rm` (not `rm`) for the tracked files so deletion records cleanly. The `__pycache__` directories will be left in place; they're gitignored.
  </action>
  <verify>
    <automated>test ! -e streamlit_app.py && test ! -d app/pages && test ! -d app/components && test ! -d tests/pages && test ! -e config/auth.yaml && ! grep -q "AUTH_PATH" .env.example && pytest tests/v2/test_phase04_invariants.py tests/v2/test_phase06_invariants.py::test_v1_streamlit_ask_deleted_per_d22 -q</automated>
  </verify>
  <done>
    - All five paths confirmed absent on disk (streamlit_app.py, app/pages/, app/components/, tests/pages/, config/auth.yaml)
    - `.env.example` no longer mentions `AUTH_PATH`
    - `tests/v2/test_phase04_invariants.py` (which already enforces `app.components.export_dialog` is not imported under app_v2/) still passes — proves the deletion didn't break the invariant or affect app_v2/
    - `tests/v2/test_phase06_invariants.py::test_v1_streamlit_ask_deleted_per_d22` passes with the new "streamlit_app.py must not exist" assertion (the file has been deleted AND the test has been flipped to match — both in this same commit, so the suite is green at the SHA)
    - Atomic commit: `chore(quick-260429-kn7): delete v1.0 streamlit UI surface, auth.yaml, and flip phase 6 invariant`
  </done>
</task>

<task type="auto">
  <name>Task 2: Refactor ufs_service.py to drop Streamlit decorators and *_core suffixes; update all importers (incl. test_cache.py patch targets) in lockstep</name>
  <files>
    app/services/ufs_service.py (REWRITE),
    app_v2/services/cache.py (EDIT — rename imports),
    app_v2/services/browse_service.py (EDIT — rename import + body call),
    tests/v2/test_browse_service.py (EDIT — rename monkeypatch attr name),
    tests/v2/test_browse_routes.py (EDIT — comment-only rename for accuracy),
    tests/v2/test_cache.py (EDIT — rename 15 patch() target strings),
    tests/services/test_ufs_service.py (DELETE — legacy file using `import streamlit as st`),
    tests/services/test_ufs_service_core.py (REWRITE → renamed in Task 2 commit to `test_ufs_service.py`)
  </files>
  <action>
    This is the core refactor. After Task 1 there are zero v1.0 callers of `ufs_service`; only `app_v2/services/cache.py`, `app_v2/services/browse_service.py`, and the test files import from it. We collapse the dual-API into a single canonical API and update every importer (including the patch-target strings in `tests/v2/test_cache.py`) in the SAME commit so tests stay green at every commit boundary.

    Step 2.1 — Rewrite `app/services/ufs_service.py`:

    Drop these top-of-file imports:
      `import streamlit as st`

    Keep these imports verbatim:
      `import logging`, `import pandas as pd`, `import sqlalchemy as sa`,
      `from app.adapters.db.base import DBAdapter`,
      `from app.core.config import load_settings`,
      `from app.services.result_normalizer import normalize`

    Function changes:
      - DELETE the `@st.cache_data(ttl=300, show_spinner=False)` wrapper `list_platforms` and the `@st.cache_data(ttl=300)` wrapper `list_parameters` and the `@st.cache_data(ttl=60)` wrapper `fetch_cells` (lines 119-126, 146-149, 232-246 in current file).
      - RENAME `list_platforms_core` → `list_platforms`. Body unchanged. Remove the docstring sentence "This is the framework-agnostic core called by: list_platforms() (v1.0 Streamlit wrapper — adds @st.cache_data) ..." — replace with a one-line "Return sorted distinct PLATFORM_ID values."
      - RENAME `list_parameters_core` → `list_parameters`. Body unchanged. Trim docstring similarly.
      - RENAME `fetch_cells_core` → `fetch_cells`. Body unchanged (the DATA-05 / SAFE-01 / T-03-01 contract stays). Trim docstring.
      - KEEP `pivot_to_wide` as-is (already has no decorator).
      - DELETE the trailing `pivot_to_wide_core = pivot_to_wide` alias and its 4-line comment block (lines 312-315).

    Module-level docstring (lines 1-38) — REWRITE to reflect single canonical API:
    ```
    """UFS data service — the single entry point for v2.0 Browse and NL queries.

    Handles server-side filtering, row-capping, and pivot-to-wide. Caching is
    layered ON TOP via app_v2/services/cache.py (TTLCache + threading.Lock —
    INFRA-08); this module is framework-agnostic and has zero Streamlit /
    FastAPI dependencies.

    Public API:
      list_platforms(db, db_name="") -> list[str]
      list_parameters(db, db_name="") -> list[dict]
      fetch_cells(db, platforms, infocategories, items, row_cap=200, db_name="") -> tuple[pd.DataFrame, bool]
      pivot_to_wide(df_long, swap_axes=False, col_cap=30) -> tuple[pd.DataFrame, bool]

    Security notes (T-03-01, T-03-02):
      - _TABLE is sourced from settings.app.agent.allowed_tables[0] at module
        load — configured constant, never interpolated from user data or HTTP
        input. _safe_table() validates against the allowlist before any
        sa.text() interpolation.
      - User-supplied filter values go through sa.bindparam(..., expanding=True).
      - No f-string interpolation of user-controlled values into SQL.
    """
    ```

    The `_load_table_config()` helper, the `_TABLE, _ALLOWED_TABLES = _load_table_config()` module-level load, and `_safe_table()` are PRESERVED verbatim — they were the subject of yesterday's quick task 260429-kc1 and that contract holds.

    Step 2.2 — Update `app_v2/services/cache.py` (edit lines 35-39):

    Change:
    ```python
    from app.services.ufs_service import (
        fetch_cells_core,
        list_parameters_core,
        list_platforms_core,
    )
    ```
    to:
    ```python
    from app.services.ufs_service import (
        fetch_cells as _fetch_cells_uncached,
        list_parameters as _list_parameters_uncached,
        list_platforms as _list_platforms_uncached,
    )
    ```

    Then update the three function bodies (lines 72, 82, 103) to call the renamed imports:
      `return list_platforms_core(db, db_name)` → `return _list_platforms_uncached(db, db_name)`
      `return list_parameters_core(db, db_name)` → `return _list_parameters_uncached(db, db_name)`
      `return fetch_cells_core(db, platforms, infocategories, items, row_cap, db_name)` → `return _fetch_cells_uncached(db, platforms, infocategories, items, row_cap, db_name)`

    Why the `_uncached` aliasing: the public wrappers in `cache.py` are themselves named `list_platforms` / `list_parameters` / `fetch_cells`. Without aliasing the import, the file would have two symbols with the same name and the wrapper would shadow itself / call itself recursively. The leading underscore + suffix makes intent explicit and prevents any future linter from auto-collapsing the import.

    Also update the file header comments (lines 3-4 + 19-23) — replace `_core() functions` and `@st.cache_data` references with the canonical names. The TTL rationale paragraph stays accurate.

    Step 2.3 — Update `app_v2/services/browse_service.py`:

    Line 29: `from app.services.ufs_service import pivot_to_wide_core` → `from app.services.ufs_service import pivot_to_wide`
    Line 145: `df_wide, col_capped = pivot_to_wide_core(` → `df_wide, col_capped = pivot_to_wide(`
    Line 12 (docstring): `pivot_to_wide_core` → `pivot_to_wide`
    Line 16 (docstring): `fetch_cells_core` → `fetch_cells`

    Step 2.4 — Update `tests/v2/test_browse_service.py`:

    Line 11 (docstring): `pivot_to_wide_core` → `pivot_to_wide`
    Line 203 (docstring/comment): `pivot_to_wide_core` → `pivot_to_wide`
    Line 213: `monkeypatch.setattr(browse_service, "pivot_to_wide_core", return_value=(capped_wide, True))` → `monkeypatch.setattr(browse_service, "pivot_to_wide", return_value=(capped_wide, True))`

    Why the monkeypatch must be renamed: `monkeypatch.setattr(module, "name", value)` patches the *attribute* on the module by string name. After Task 2.3 the only attribute the module imports is `pivot_to_wide`, so the legacy string would AttributeError.

    Step 2.5 — Update `tests/v2/test_browse_routes.py`:

    Line 256 (comment): `# pivot_to_wide_core (real, not mocked) caps at 30.` → `# pivot_to_wide (real, not mocked) caps at 30.`

    Step 2.6 — Replace the test files for ufs_service:

    `git rm tests/services/test_ufs_service.py` — current file uses `import streamlit as st` and tests the `@st.cache_data` wrappers. The wrappers no longer exist. Some test cases (the pure `pivot_to_wide` tests at lines 100-149) are duplicated in `test_ufs_service_core.py`'s `_InMemoryAdapter` fixture; the rest test the deleted wrappers.

    `git mv tests/services/test_ufs_service_core.py tests/services/test_ufs_service.py` and rewrite to drop Streamlit:

    - Drop `import streamlit as st` (line 15).
    - Drop the entire `_clear_streamlit_cache` autouse fixture (lines 65-71) — it called `st.cache_data.clear()` / `st.cache_resource.clear()` which only mattered while wrappers existed.
    - Drop the import block referencing `*_core` names (lines 19-28) — replace with:
      ```python
      from app.services.ufs_service import (
          fetch_cells,
          list_parameters,
          list_platforms,
          pivot_to_wide,
      )
      ```
    - Delete the four `*_core_matches_wrapper` tests (lines 103-131) — they compared `_core` with the wrapper; both are now the same function.
    - Delete `test_pivot_to_wide_core_is_pivot_to_wide` (lines 142-144) — the alias is gone.
    - Update `test_core_functions_importable_without_streamlit_session` (lines 147-165): rename to `test_ufs_service_importable_without_streamlit` and update the subprocess literal to import the canonical names instead of `*_core` names.
    - KEEP and rename: `test_fetch_cells_core_empty_filter_short_circuit` → `test_fetch_cells_empty_filter_short_circuit` (call site already takes the canonical-name variable; just remove the `_core` suffix in the function name and the function call).
    - ADD the `pivot_to_wide` tests that the deleted `test_ufs_service.py` carried (these test pure pivot behavior, no DB needed):
      - `test_pivot_to_wide_empty_returns_empty`
      - `test_pivot_to_wide_default_orientation_has_platform_id_column`
      - `test_pivot_to_wide_swap_axes_has_item_column`
      - `test_pivot_to_wide_caps_at_30_columns`
      - `test_pivot_to_wide_warns_on_duplicates` (uses `caplog.at_level(logging.WARNING, logger="app.services.ufs_service")`)
    - ADD a `fetch_cells` integration-style test against `mock_db` proving the function works end-to-end without any caching layer (port the body of the deleted `test_fetch_cells_core_matches_wrapper` but drop the wrapper comparison — assert directly on `fetch_cells(mock_db, ("p1",), (), ("item1",))` returning the expected `(df, capped)` tuple).

    Step 2.7 — Update `tests/v2/test_cache.py` (rename 15 patch-target strings):

    The file uses `unittest.mock.patch("app_v2.services.cache.<name>", ...)` to patch the underlying ufs_service functions as imported into `cache.py`. After Step 2.2 those imports are no longer named `list_platforms_core` / `list_parameters_core` / `fetch_cells_core` on the `cache` module — they are now `_list_platforms_uncached` / `_list_parameters_uncached` / `_fetch_cells_uncached`. If we don't rename the patch targets, every `patch(...)` call raises `AttributeError: <module 'app_v2.services.cache'> does not have the attribute '<old_name>'`, and all 14 tests in `test_cache.py` break.

    Mechanical rename — apply globally to the file:
    - `app_v2.services.cache.list_platforms_core` → `app_v2.services.cache._list_platforms_uncached`
    - `app_v2.services.cache.list_parameters_core` → `app_v2.services.cache._list_parameters_uncached`
    - `app_v2.services.cache.fetch_cells_core` → `app_v2.services.cache._fetch_cells_uncached`

    Specific call sites (verify with `grep -nE "list_platforms_core|list_parameters_core|fetch_cells_core" tests/v2/test_cache.py` before editing — expect 15 matches across the lines below; rename ALL of them):
    - Line 3 (module docstring): `mocking the underlying _core functions.` → `mocking the underlying _uncached functions.` (prose update for accuracy; not a patch target but should match the file's reality)
    - Line 35 (fixture comment): `# DBAdapter stand-in — never actually called; wrapper bypasses to _core.` → `# DBAdapter stand-in — never actually called; wrapper bypasses to _uncached.`
    - Line 43: `patch("app_v2.services.cache.list_platforms_core", ...)` → `patch("app_v2.services.cache._list_platforms_uncached", ...)`
    - Line 48: `mock_core.call_count` — KEEP variable name `mock_core` (it's a local name, no need to churn) BUT the patch-target string was already updated above
    - Line 52: `patch("app_v2.services.cache.list_platforms_core", ...)` → `patch("app_v2.services.cache._list_platforms_uncached", ...)`
    - Line 64: `patch("app_v2.services.cache.list_platforms_core", ...)` → `patch("app_v2.services.cache._list_platforms_uncached", ...)`
    - Line 77: `patch("app_v2.services.cache.list_parameters_core", ...)` → `patch("app_v2.services.cache._list_parameters_uncached", ...)`
    - Lines 87-90: two patches in one `with` block — rename both:
      `patch("app_v2.services.cache.list_platforms_core", ...)` → `patch("app_v2.services.cache._list_platforms_uncached", ...)`
      `patch("app_v2.services.cache.list_parameters_core", ...)` → `patch("app_v2.services.cache._list_parameters_uncached", ...)`
    - Line 106: `patch("app_v2.services.cache.fetch_cells_core", ...)` → `patch("app_v2.services.cache._fetch_cells_uncached", ...)`
    - Line 110 (comment inside test): `core must be called exactly once on a cache hit` — leave the local variable name `mock_core` and surrounding prose if cosmetic; OR optionally update prose to `_uncached must be called exactly once on a cache hit`. Cosmetic preference; the test still works either way.
    - Line 129: `patch("app_v2.services.cache.fetch_cells_core", ...)` → `patch("app_v2.services.cache._fetch_cells_uncached", ...)`
    - Line 138 (comment): `should still be a cache hit` — leave as-is
    - Line 149: `patch("app_v2.services.cache.fetch_cells_core", ...)` → `patch("app_v2.services.cache._fetch_cells_uncached", ...)`
    - Line 159: `patch("app_v2.services.cache.fetch_cells_core", ...)` → `patch("app_v2.services.cache._fetch_cells_uncached", ...)`
    - Line 168: `patch("app_v2.services.cache.fetch_cells_core", ...)` → `patch("app_v2.services.cache._fetch_cells_uncached", ...)`
    - Line 195: `patch("app_v2.services.cache.list_platforms_core", ...)` → `patch("app_v2.services.cache._list_platforms_uncached", ...)`
    - Line 223: `patch("app_v2.services.cache.list_platforms_core", ...)` → `patch("app_v2.services.cache._list_platforms_uncached", ...)`
    - Line 259: `patch("app_v2.services.cache.list_platforms_core", ...)` → `patch("app_v2.services.cache._list_platforms_uncached", ...)`

    The local variable names `mock_core` / `mp` / `mpp` and the `MagicMock(spec=object)` fixture stay as-is. Only the string passed to `patch(...)` changes (and the two cosmetic prose comments noted above).

    After the rewrite, run `grep -cE "list_platforms_core|list_parameters_core|fetch_cells_core" tests/v2/test_cache.py` — must return 0. If any of the three old names still appears (in code or prose), it will be a tripwire for the audit in Task 5.

    All other v2.0 tests (`tests/v2/`) and the agent/services tests stay untouched — they don't reference `*_core`.

    Atomic commit message: `refactor(quick-260429-kn7): drop @st.cache_data wrappers and *_core suffix from ufs_service`

    Why one big commit: any partial commit (rename in ufs_service.py without updating cache.py and test_cache.py together) would fail `pytest tests/v2/test_cache.py` mid-transition. Atomic = green at every SHA.
  </action>
  <verify>
    <automated>! grep -rE "^\s*import streamlit|^\s*from streamlit" app/ app_v2/ tests/ --include='*.py' && ! grep -rE "@st\.(cache_data|cache_resource|dialog)" app/ app_v2/ --include='*.py' && ! grep -rE "_core\b" app/services/ufs_service.py app_v2/services/cache.py app_v2/services/browse_service.py tests/services/test_ufs_service.py tests/v2/test_browse_service.py && [ "$(grep -cE 'list_platforms_core|list_parameters_core|fetch_cells_core' tests/v2/test_cache.py)" = "0" ] && pytest tests/services/test_ufs_service.py tests/v2/test_cache.py tests/v2/test_browse_service.py tests/v2/test_browse_routes.py -q</automated>
  </verify>
  <done>
    - `app/services/ufs_service.py` has zero `import streamlit` / `@st.cache_data` lines
    - The four canonical functions (`list_platforms`, `list_parameters`, `fetch_cells`, `pivot_to_wide`) are the only public names; no `*_core` suffix anywhere in the repo
    - `app_v2/services/cache.py` imports the canonical names with `_uncached` aliases; the public wrapper names (`list_platforms` / `list_parameters` / `fetch_cells`) still match the v2.0 router callers
    - `app_v2/services/browse_service.py` imports `pivot_to_wide` (no suffix) and the body call matches
    - `tests/v2/test_browse_service.py` monkeypatch attr name updated; the test still passes (proves the patched function is the real one consumed)
    - `tests/v2/test_cache.py` has zero remaining occurrences of `list_platforms_core` / `list_parameters_core` / `fetch_cells_core` (verified by `grep -cE "list_platforms_core|list_parameters_core|fetch_cells_core" tests/v2/test_cache.py` returning 0); all 14 tests in the file pass against the renamed `_*_uncached` patch targets
    - `tests/services/test_ufs_service.py` is the renamed file; covers all four canonical functions + four `pivot_to_wide` purity tests + duplicate-warning + fetch_cells short-circuit + subprocess importability without streamlit
    - The four pytest invocations in the verify block all exit 0
    - Atomic commit: `refactor(quick-260429-kn7): drop @st.cache_data wrappers and *_core suffix from ufs_service`
  </done>
</task>

<task type="auto">
  <name>Task 3: Drop streamlit/streamlit-authenticator/nest-asyncio/bcrypt from requirements.txt</name>
  <files>
    requirements.txt (EDIT — drop 4 lines)
  </files>
  <action>
    The Phase 6 invariant flip moved into Task 1 (atomic with the streamlit_app.py deletion). This task now scopes down to a single file edit: trimming the dropped deps from requirements.txt.

    Step 3.1 — Edit `requirements.txt`:

    Delete these four lines (currently lines 1, 2, 13, 18):
    ```
    streamlit>=1.40,<2.0
    streamlit-authenticator>=0.3.3,<1.0
    nest-asyncio>=1.6
    bcrypt>=4.2
    ```

    Why each:
    - `streamlit` — was the v1.0 framework; nothing imports it anymore (verified by Task 2's grep gate).
    - `streamlit-authenticator` — was paired with `config/auth.yaml` (deleted in Task 1) for shared-credential intranet auth. Re-enablement (D-04) will pick a fresh credential strategy at the time it's prioritized.
    - `nest-asyncio` — was used by `app/pages/ask.py` (deleted in D-22) to allow `agent.run_sync()` to coexist with Streamlit's event loop. The v2.0 Ask router uses sync `def` routes (Phase 6 invariant INFRA-05), so nest_asyncio is irrelevant; `run_in_threadpool` is the FastAPI mechanism.
    - `bcrypt` — was a transitive dep of streamlit-authenticator (used to verify YAML hashed passwords). With streamlit-authenticator gone and no v2.0 importer (`grep -rn "import bcrypt" app_v2/ tests/v2/` returns zero), it can be dropped.

    KEEP all other lines verbatim (sqlalchemy, pymysql, pandas, pyyaml, pydantic, pydantic-ai, sqlparse, openai, httpx, requests, plotly, altair, python-dotenv, openpyxl, fastapi, uvicorn, jinja2, jinja2-fragments, python-multipart, markdown-it-py, cachetools, pydantic-settings).

    Note on `openpyxl` — KEEP. The v2.0 NL Ask path may produce Excel output via pandas in a future feature, and v2.0 Browse export is "out of scope for v2.0" not "out of scope forever" (D-19 wording). Surgical principle: only drop deps that are provably orphan now.

    Note on `plotly` and `altair` — KEEP for the same reason. Phase 4 D-03 banned plotly *imports under app_v2/* (enforced by `tests/v2/test_phase04_invariants.py:42`) but did not require dropping the dep, since chart support is a future v2.0 feature.

    Atomic commit message: `chore(quick-260429-kn7): drop streamlit/streamlit-authenticator/nest-asyncio/bcrypt deps`

    After this commit, run `pip check` (or skip if the environment is read-only) to confirm no remaining package depends on the four dropped names. If a `pip check` failure surfaces (e.g., a transitive needs bcrypt), record the dep in this same commit's message — but the audit shows none of the kept packages depend on the dropped ones.
  </action>
  <verify>
    <automated>! grep -E "^streamlit\b|^streamlit-authenticator\b|^nest-asyncio\b|^bcrypt\b" requirements.txt</automated>
  </verify>
  <done>
    - `requirements.txt` no longer contains `streamlit`, `streamlit-authenticator`, `nest-asyncio`, or `bcrypt` lines
    - Atomic commit: `chore(quick-260429-kn7): drop streamlit/streamlit-authenticator/nest-asyncio/bcrypt deps`
    - (The `tests/v2/test_phase06_invariants.py::test_v1_streamlit_ask_deleted_per_d22` invariant was already flipped and verified in Task 1; no re-run needed here.)
  </done>
</task>

<task type="auto">
  <name>Task 4: Rewrite Streamlit framing in CLAUDE.md and PROJECT.md to reflect FastAPI as the canonical UI</name>
  <files>
    CLAUDE.md (EDIT — Project / Tech Stack / Constraints sections),
    .planning/PROJECT.md (EDIT — What This Is / Constraints sections)
  </files>
  <action>
    The deployed reality is FastAPI + Bootstrap 5 + HTMX + Jinja2 + jinja2-fragments. The docs still describe Streamlit as the canonical UI, which was true at v1.0 ship and during early v2.0 phases but is not true at v2.0 close. This task aligns the documented stack with what's running.

    Step 4.1 — Edit `CLAUDE.md`:

    Note: `CLAUDE.md` is generated from GSD source files (markers like `<!-- GSD:project-start source:PROJECT.md -->`) — the canonical edits go into `.planning/PROJECT.md` and the GSD-managed source files. CLAUDE.md is regenerated, not hand-edited. So Task 4 is primarily PROJECT.md edits + (if needed) regeneration of CLAUDE.md.

    Inspect the GSD markers in CLAUDE.md to confirm which sections are auto-synced from PROJECT.md vs. from `research/STACK.md` etc. The "Project" section (`<!-- GSD:project-start source:PROJECT.md -->`) is a verbatim mirror of `.planning/PROJECT.md`'s "What This Is" + "Constraints" — editing PROJECT.md and re-running the GSD sync is the correct path.

    For this task: edit `.planning/PROJECT.md`. CLAUDE.md regeneration happens after via the standard project flow (or, if the regen tool is offline, hand-mirror the same edits into the marked sections of CLAUDE.md as a fallback).

    Step 4.2 — Edit `.planning/PROJECT.md`:

    "What This Is" (line 5) — change:
      "PBM2 is an internal Streamlit website where..."
    to:
      "PBM2 is an internal FastAPI + Bootstrap 5 + HTMX website where..."

    KEEP the rest of the sentence verbatim (a team of non-SQL users, EAV-form MySQL parameter database `ufs_data`, UFS subsystem profiles, slice/pivot/filter/visualize/export, NL questions on top, no SQL).

    "Constraints" (line 160) — change:
      "**Tech stack**: Streamlit + SQLAlchemy (pymysql driver) + pandas + Pydantic v2 + python-dotenv — Why: scaffolding is already in place; no reason to diverge."
    to:
      "**Tech stack**: FastAPI + Bootstrap 5 + HTMX + Jinja2 + jinja2-fragments + SQLAlchemy (pymysql driver) + pandas + Pydantic v2 + python-dotenv — Why: v2.0 milestone shipped this stack 2026-04-29 (tag v2.0); v1.0 Streamlit shell sunset in quick task 260429-kn7."

    KEEP all other Constraints rows verbatim (Data, Scale, Deployment, LLM choice, Result heterogeneity, Security).

    Step 4.3 — Edit `.planning/PROJECT.md` "Out of Scope" section:

    Line 127 — the "v2.0 Browse Excel/CSV export" entry currently reads:
      "v1.0 Streamlit Browse remains the export surface until v1.0 sunset; v2.0 Browse is view-only by design choice (2026-04-26 user decision). The v1.0 export_dialog component remains in place and is NOT touched, copied, or imported by app_v2/."

    Replace with:
      "v2.0 Browse is view-only by design choice (2026-04-26 user decision). The v1.0 export_dialog component was removed in quick task 260429-kn7 along with the rest of the Streamlit shell; export resurrection (if needed) will design a v2.0-native flow rather than restoring Streamlit."

    Step 4.4 — Edit `.planning/PROJECT.md` "Active" requirements (lines 87-93):

    The "Platform (v2.0)" requirements bullet currently reads:
      "FastAPI app with Bootstrap 5 + HTMX shell, parallel to archived v1.0 Streamlit code"
    Update to:
      "FastAPI app with Bootstrap 5 + HTMX shell — v1.0 Streamlit code removed in quick task 260429-kn7"

    Step 4.5 — Edit `.planning/PROJECT.md` "Key Decisions" table (lines 173-184):

    The row "Shared-credential intranet auth via existing `streamlit-authenticator`" — leave the Decision/Rationale columns verbatim, but update the Outcome column:
      Current: "⚠️ Revisit — D-04 deferred auth to a pre-deployment phase. Must enable streamlit-authenticator + cookie-key guard before team-wide rollout"
      Updated: "⚠️ Revisit — D-04 deferred auth to a pre-deployment phase. streamlit-authenticator + auth.yaml removed in 260429-kn7 (v1.0 sunset); fresh credential strategy to be picked when auth is re-enabled"

    The row "Drop v2.0 Browse export to keep the port view-only":
      Current Outcome: "⚠️ Revisit at v1.0 sunset planning"
      Updated Outcome: "✓ Resolved — v1.0 export_dialog removed in 260429-kn7. Future export work (if any) is v2.0-native."

    Step 4.6 — Update PROJECT.md "Last updated" timestamp at the bottom (line 203):
      "*Last updated: 2026-04-29 after Phase 6 (Ask Tab Port) plan-complete..."
    to:
      "*Last updated: 2026-04-29 after quick task 260429-kn7 (v1.0 Streamlit shell sunset). v2.0 milestone phases all complete (6/6); FastAPI/Bootstrap/HTMX is now the single UI; HUMAN-UAT pending for live-server validation.*"

    Step 4.7 — Mirror the same edits into CLAUDE.md's `<!-- GSD:project-start ... -->` block (lines 1-19). The Tech Stack table (lines 22-56) under `<!-- GSD:stack-start source:research/STACK.md -->` describes Streamlit as "Web app framework, all UI primitives" — leave the table as-is (it's the historical research record, not the current stack), BUT add a one-line annotation at the top of the section:
      "> **Stack as of v2.0 (2026-04-29):** FastAPI + Bootstrap 5 + HTMX + Jinja2 superseded Streamlit as the UI primitive. The table below is the original v1.0 research; entries marked Streamlit / streamlit-authenticator / nest-asyncio are no longer active dependencies. See `.planning/PROJECT.md` Constraints for the current stack."

    Why preserve the historical research table: it's a record of *why* decisions were made (the "What NOT to Use" guard list — LangChain, litellm, Vanna, streamlit-aggrid — is still active and load-bearing). Removing the table would lose that institutional memory. Annotating it makes intent clear without rewriting history.

    Atomic commit message: `docs(quick-260429-kn7): rewrite project framing as FastAPI/Bootstrap/HTMX after v1.0 sunset`
  </action>
  <verify>
    <automated>grep -q "FastAPI" .planning/PROJECT.md && grep -q "FastAPI" CLAUDE.md && ! grep -E "Streamlit website" .planning/PROJECT.md && grep -q "260429-kn7" .planning/PROJECT.md</automated>
  </verify>
  <done>
    - `.planning/PROJECT.md` "What This Is" describes FastAPI + Bootstrap 5 + HTMX as the website framework (no longer "Streamlit website")
    - `.planning/PROJECT.md` Constraints "Tech stack" row lists FastAPI + Bootstrap 5 + HTMX + Jinja2 + jinja2-fragments alongside the preserved SQLAlchemy/pandas/Pydantic line
    - `.planning/PROJECT.md` Out of Scope export entry updated to reflect v1.0 export_dialog removal
    - `.planning/PROJECT.md` Active requirements + Key Decisions outcomes updated
    - `.planning/PROJECT.md` "Last updated" timestamp reflects this quick task
    - `CLAUDE.md` `<!-- GSD:project-start -->` block mirrors the new framing; the research stack table carries the v2.0 supersession annotation but is otherwise preserved
    - Atomic commit: `docs(quick-260429-kn7): rewrite project framing as FastAPI/Bootstrap/HTMX after v1.0 sunset`
  </done>
</task>

<task type="auto">
  <name>Task 5: Final verification gate — run full pytest suite, app_v2 import smoke test, and grep audits</name>
  <files>
    (no files modified — verification only; no commit unless gate fails and a fix is needed)
  </files>
  <action>
    This is the gate that confirms every must-have truth from the frontmatter holds. Run the full set of grep audits and the full test suite. Any failure here is a blocker — go back to the relevant prior task and fix.

    Step 5.1 — Streamlit absence audit:
    ```bash
    grep -rE "^\s*import streamlit|^\s*from streamlit" app/ app_v2/ tests/ --include='*.py'
    ```
    Expected: zero output. If anything returns, the stale import was missed in Task 1 or Task 2.

    Step 5.2 — Streamlit decorator audit:
    ```bash
    grep -rE "@st\." app/ app_v2/ --include='*.py'
    ```
    Expected: zero output under app/ and app_v2/. (The `app_v2/services/cache.py` file may still mention `@st.cache_data` in a docstring comparing to v1.0 — that's documentation, not a decorator. The `@st.` regex catches decorators specifically; if a docstring match shows up, decide case-by-case whether to leave or scrub.)

    Step 5.3 — `*_core` audit:
    ```bash
    grep -rE "_core\b" app/services/ufs_service.py app_v2/ tests/services/test_ufs_service.py tests/v2/ --include='*.py'
    ```
    Expected: zero output. (The `_load_table_config()` private helper inside `ufs_service.py` is fine — it doesn't match `_core\b`.)

    Step 5.4 — Filesystem audit:
    ```bash
    test ! -e streamlit_app.py
    test ! -d app/pages
    test ! -d app/components
    test ! -d tests/pages
    test ! -e config/auth.yaml
    ```
    All five must succeed (exit 0).

    Step 5.5 — Dependency audit:
    ```bash
    ! grep -E "^streamlit\b|^streamlit-authenticator\b|^nest-asyncio\b|^bcrypt\b" requirements.txt
    ```
    Expected exit 0 (the `!` inverts grep's success exit when matches are found).

    Step 5.6 — App import smoke test:
    ```bash
    python -c "import app_v2.main; print('ok')"
    ```
    Must print `ok`. If `ModuleNotFoundError: streamlit` surfaces, a v2.0 file (or the agent module, or an adapter) still pulls Streamlit transitively. Fix the import chain.

    Step 5.7 — Full pytest suite:
    ```bash
    pytest tests/ -q
    ```
    Must exit 0. Expected count is 506 minus the v1.0 tests removed in Task 2 — somewhere in the 480-500 range, depending on how many were the deleted `test_ufs_service.py` cases vs. the rewritten ones. The number itself is not the gate; green is the gate.

    Step 5.8 — Doc audit:
    ```bash
    grep -q "FastAPI" .planning/PROJECT.md
    grep -q "FastAPI" CLAUDE.md
    ! grep -q "Streamlit website" .planning/PROJECT.md
    ```
    All three must succeed.

    If every audit passes: report the test count and confirm. The user will then tag `v2.1` (Streamlit Sunset) as a separate manual action — DO NOT tag in this task.

    If any audit fails: stop, identify the responsible prior task, and fix in a new commit (not by amending). Re-run the full gate after the fix lands.
  </action>
  <verify>
    <automated>! grep -rE "^\s*import streamlit|^\s*from streamlit" app/ app_v2/ tests/ --include='*.py' && test ! -e streamlit_app.py && test ! -d app/pages && test ! -d app/components && test ! -e config/auth.yaml && ! grep -E "^streamlit\b|^streamlit-authenticator\b|^nest-asyncio\b|^bcrypt\b" requirements.txt && python -c "import app_v2.main" && pytest tests/ -q</automated>
  </verify>
  <done>
    - All seven grep / filesystem / import / pytest gates pass
    - Reported pytest test count for the record (used to update STATE.md "tests passing" line in a follow-up `/gsd-transition`)
    - No new commits in this task unless a prior task needed a fix; if so, those fixes commit separately as `fix(quick-260429-kn7): ...`
  </done>
</task>

</tasks>

<verification>
End-to-end checks for the whole plan (run after Task 5):

1. **Streamlit purge complete:**
   - `grep -rE "^\s*import streamlit|^\s*from streamlit" app/ app_v2/ tests/ --include='*.py'` returns zero
   - `grep -rE "@st\.(cache_data|cache_resource|dialog)" app/ app_v2/ --include='*.py'` returns zero
   - `streamlit_app.py`, `app/pages/`, `app/components/`, `tests/pages/`, `config/auth.yaml` all absent

2. **`ufs_service.py` is canonical:**
   - The four functions are `list_platforms`, `list_parameters`, `fetch_cells`, `pivot_to_wide` — no `_core` suffix
   - `app_v2/services/cache.py` and `app_v2/services/browse_service.py` import the canonical names
   - `tests/services/test_ufs_service.py` exists, has zero Streamlit dependency, covers the four functions
   - `tests/v2/test_cache.py` patch-target strings reference `_list_platforms_uncached` / `_list_parameters_uncached` / `_fetch_cells_uncached`; zero remaining `*_core` references in that file

3. **Dependencies trimmed:**
   - `requirements.txt` does not contain `streamlit`, `streamlit-authenticator`, `nest-asyncio`, or `bcrypt`
   - `python -c "import app_v2.main"` succeeds

4. **Phase 6 invariant flipped (in Task 1's commit, not Task 3's):**
   - `tests/v2/test_phase06_invariants.py::test_v1_streamlit_ask_deleted_per_d22` asserts `streamlit_app.py` does NOT exist (instead of reading it)
   - All other invariant tests still pass

5. **Documentation reflects reality:**
   - `.planning/PROJECT.md` "What This Is" + Constraints describe FastAPI + Bootstrap + HTMX
   - CLAUDE.md GSD-synced "Project" section mirrors the FastAPI framing
   - The v1.0 research stack table in CLAUDE.md is annotated with a v2.0 supersession note (not deleted)

6. **Test suite green at every commit boundary, not just at the end:**
   - Task 1 commit: streamlit_app.py deleted AND its invariant flipped → no FileNotFoundError in test_phase06_invariants.py at that SHA
   - Task 2 commit: ufs_service rewrite + cache.py imports + test_cache.py patch targets all renamed together → no AttributeError in test_cache.py at that SHA
   - Task 3 commit: deps dropped → no test consequences
   - Task 4 commit: docs only
   - Task 5: full pytest suite green
</verification>

<success_criteria>
- [ ] Task 1: Five paths deleted (streamlit_app.py, app/pages/, app/components/, tests/pages/, config/auth.yaml); .env.example AUTH_PATH line removed; `tests/v2/test_phase06_invariants.py::test_v1_streamlit_ask_deleted_per_d22` flipped to assert non-existence and passes; `tests/v2/test_phase04_invariants.py` still green
- [ ] Task 2: `ufs_service.py` rewritten; canonical names everywhere; `cache.py`, `browse_service.py`, `test_browse_service.py`, `test_browse_routes.py`, `test_cache.py` (patch targets) updated; `test_ufs_service.py` rewritten; `grep -cE "list_platforms_core|list_parameters_core|fetch_cells_core" tests/v2/test_cache.py` returns 0; the four pytest invocations in Task 2 verify all exit 0
- [ ] Task 3: requirements.txt drops 4 lines (invariant flip already happened in Task 1)
- [ ] Task 4: PROJECT.md and CLAUDE.md describe FastAPI + Bootstrap + HTMX as the canonical UI; v2.0 supersession annotation in the research stack table
- [ ] Task 5: Full grep audit + import smoke + `pytest tests/ -q` all green; final test count recorded
- [ ] Five atomic commits, each with the `quick-260429-kn7` prefix per the constraint
- [ ] Out of scope items NOT touched: app/core/agent/, app/core/config.py, app/services/{result_normalizer,sql_validator,sql_limiter,path_scrubber,ollama_fallback}.py, app/adapters/, all tests/v2/ except the 4 named files (test_browse_service.py, test_browse_routes.py, test_cache.py, test_phase06_invariants.py), all tests/agent/, all the framework-agnostic tests/services/*.py
</success_criteria>

<output>
After completion, create `.planning/quick/260429-kn7-remove-v1-0-streamlit-shell-app-v2-fasta/260429-kn7-SUMMARY.md` recording:

- Files deleted (count and list)
- ufs_service.py before/after API surface
- requirements.txt deps dropped
- Final pytest count (before: 506, after: ?)
- Doc sections updated in CLAUDE.md / PROJECT.md
- Verification gates passed (grep + filesystem + import + pytest)
- Suggested next user action: tag `v2.1` (Streamlit Sunset)
</output>
</content>
</invoke>