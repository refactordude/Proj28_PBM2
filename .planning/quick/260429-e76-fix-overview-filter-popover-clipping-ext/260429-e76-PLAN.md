---
phase: 260429-e76
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app_v2/static/css/app.css
autonomous: false
requirements:
  - QUICK-260429-e76
must_haves:
  truths:
    - "Overview Status picker popover renders all options without bottom clipping (the 4th 'On Hold' option is fully visible, not half-cut)"
    - "Overview Customer / AP Company / Device / Controller / Application picker popovers also escape the .panel { overflow: hidden } clipping when their option lists exceed the panel's bottom edge"
    - "Browse picker popovers (Platforms / Parameters) continue to escape clipping exactly as in Phase 4 — no behavior change for Browse"
    - "Phase 03 panels (which carry neither .browse-filter-bar nor .overview-filter-bar) still keep their rounded-corner overflow:hidden clipping unchanged"
  artifacts:
    - path: "app_v2/static/css/app.css"
      provides: "Extended :has() selector covering both .browse-filter-bar and .overview-filter-bar"
      contains: ".panel:has(.browse-filter-bar), .panel:has(.overview-filter-bar)"
  key_links:
    - from: "app_v2/static/css/app.css line ~110"
      to: ".overview-filter-bar (line 176) AND .browse-filter-bar (line 101)"
      via: ":has() compound selector listing both filter-bar classes"
      pattern: "\\.panel:has\\(\\.browse-filter-bar\\),\\s*\\.panel:has\\(\\.overview-filter-bar\\)"
---

<objective>
Fix Overview filter popover clipping by extending the existing Phase 4 `.panel { overflow: visible }` escape-hatch to also cover the Overview filter bar.

Purpose: Phase 5 introduced `.overview-filter-bar` (app.css line 176) but did not extend the Phase-4 selector at line 110 — so all 6 Overview pickers (Status, Customer, AP Company, Device, Controller, Application) suffer popover clipping when the option list exceeds the panel's bottom edge. The user observes the 4th "On Hold" option of the Status picker rendered half-visible.

Output: `app_v2/static/css/app.css` lines 106-110 region updated — the comment block reflects that BOTH filter bars are now covered, and the selector adds `, .panel:has(.overview-filter-bar)` to the existing rule. No other code changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@app_v2/static/css/app.css
@app_v2/templates/overview/_filter_bar.html
@app_v2/templates/browse/_picker_popover.html

<interfaces>
<!-- Current state of the relevant region in app_v2/static/css/app.css (lines 106-110): -->

```css
/* Allow Bootstrap dropdown popovers (Platforms / Parameters pickers) to escape
   the Phase 03 .panel { overflow: hidden } clipping when the panel is short.
   Scoped via :has() so only the Browse panel is affected — Phase 03 panels
   keep their rounded-corner clipping unchanged. */
.panel:has(.browse-filter-bar) { overflow: visible; }
```

<!-- Both filter bar classes exist and gate panel overflow: -->
<!-- - .browse-filter-bar (app.css line 101) — Phase 4 -->
<!-- - .overview-filter-bar (app.css line 176) — Phase 5 -->

<!-- The picker_popover macro (app_v2/templates/browse/_picker_popover.html) is shared by Browse,
     Overview, and Ask. It renders a Bootstrap .dropdown-menu inside the .panel.
     The .panel itself has overflow:hidden (Phase 03, line 20) for rounded-corner clipping.
     Without the :has() escape rule, dropdown menus that overflow the panel get clipped. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the :has() escape selector to also cover .overview-filter-bar</name>
  <files>app_v2/static/css/app.css</files>
  <action>
In `app_v2/static/css/app.css`, modify the rule at lines 106-110 to extend the existing :has() selector so it covers BOTH the Browse and Overview filter bars. Two edits inside this 5-line block:

(1) Update the leading comment to drop the "Browse-only" qualifier and reflect that the rule now covers both filter bars. Replace the current comment text:

```
/* Allow Bootstrap dropdown popovers (Platforms / Parameters pickers) to escape
   the Phase 03 .panel { overflow: hidden } clipping when the panel is short.
   Scoped via :has() so only the Browse panel is affected — Phase 03 panels
   keep their rounded-corner clipping unchanged. */
```

with:

```
/* Allow Bootstrap dropdown popovers (Browse Platforms/Parameters pickers AND
   Overview Status/Customer/AP Company/Device/Controller/Application pickers)
   to escape the Phase 03 .panel { overflow: hidden } clipping when the panel
   is short. Scoped via :has() so only panels containing a known filter bar
   are affected — Phase 03 panels (which carry neither class) keep their
   rounded-corner clipping unchanged. */
```

(2) Extend the selector itself by appending `, .panel:has(.overview-filter-bar)` to the existing rule. Replace:

```css
.panel:has(.browse-filter-bar) { overflow: visible; }
```

with:

```css
.panel:has(.browse-filter-bar), .panel:has(.overview-filter-bar) { overflow: visible; }
```

CRITICAL constraints:
- Do NOT touch any other line in app.css. Phase 4 byte-stability for everything outside this 5-line region must be preserved.
- Do NOT change `.browse-filter-bar` selector in the .panel:has() compound — keep it FIRST in the comma list, exactly as written above. This preserves Phase 4 selector identity for the Browse half.
- Do NOT touch HTML or Python — this is CSS only.
- Do NOT add any new CSS rules elsewhere — the fix is to the existing rule only.
- The comma in `.panel:has(.browse-filter-bar), .panel:has(.overview-filter-bar)` is followed by a single space (standard CSS list separator).

Why this works (for executor context): both Browse (Phase 4) and Overview (Phase 5) wrap their filter bars inside a `.panel` element that has `overflow: hidden` (Phase 03 line 20) for rounded-corner clipping. The shared `picker_popover` macro (`app_v2/templates/browse/_picker_popover.html`) renders a Bootstrap `.dropdown-menu` whose absolutely-positioned popover layer can extend below the panel — so the panel's overflow:hidden clips it. The Phase 4 fix used `:has(.browse-filter-bar)` to opt only the Browse panel into `overflow: visible`. Phase 5's Overview tab has the same picker macro inside a panel, but its filter-bar carries `.overview-filter-bar` (not `.browse-filter-bar`), so the existing rule does not match and clipping returns. Adding `.overview-filter-bar` to the same compound selector applies the identical escape rule to Overview without touching Browse and without affecting any other panel (Ask, content, summary).
  </action>
  <verify>
    <automated>grep -nE '\.panel:has\(\.browse-filter-bar\),\s*\.panel:has\(\.overview-filter-bar\)\s*\{\s*overflow:\s*visible' app_v2/static/css/app.css</automated>
  </verify>
  <done>
- The compound selector `.panel:has(.browse-filter-bar), .panel:has(.overview-filter-bar) { overflow: visible; }` is present in `app_v2/static/css/app.css` at the same location (around line 110) where the original single-class selector lived.
- The leading comment block above the rule no longer says "only the Browse panel is affected" / "Platforms / Parameters pickers" — the new comment explicitly names both filter bars and the 8 picker categories (Browse: Platforms, Parameters; Overview: Status, Customer, AP Company, Device, Controller, Application).
- `grep -c '\.panel:has(\.browse-filter-bar)' app_v2/static/css/app.css` returns 1 (selector still exists exactly once).
- `grep -c '\.panel:has(\.overview-filter-bar)' app_v2/static/css/app.css` returns 1 (selector now exists exactly once — the addition).
- `git diff --stat app_v2/static/css/app.css` shows a small additive diff (comment lines + 1 selector clause) with NO deletions outside the 5-line region 106-110.
- `git diff -- ':!app_v2/static/css/app.css'` is empty (no other file changed).
- The full v2 test suite remains green: `pytest tests/v2/ -q` passes (the fix is CSS-only — no Python or template assertions are affected).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual UAT — Overview picker popovers no longer clip</name>
  <files>app_v2/static/css/app.css (verified, not modified in this task)</files>
  <action>
Pause here for human visual verification of Task 1. No code changes in this task — the user runs the v2.0 app locally and confirms the Overview picker popovers render unclipped while Browse and Phase 03 panels are unchanged. The instructions for the human are in <how-to-verify> below.

What was built (from Task 1): Extended the `.panel:has(...) { overflow: visible }` escape-hatch in `app_v2/static/css/app.css` from a Browse-only rule (Phase 4) to also cover the Overview filter bar (Phase 5). All 6 Overview filter pickers should now render their full option lists without being clipped at the panel's bottom edge.
  </action>
  <how-to-verify>
1. Start the v2.0 app locally:
   ```
   uvicorn app_v2.main:app --reload --port 8001
   ```
2. Open http://localhost:8001/overview in a browser.
3. Click the **Status** picker trigger (the original repro).
   - Expected: ALL 4 status options (Active, In Progress, Completed, On Hold) are fully visible inside the dropdown — the 4th "On Hold" row is NOT cut off at the bottom edge.
   - Before the fix: the 4th option was rendered half-visible because `.panel { overflow: hidden }` clipped the dropdown layer at the panel boundary.
4. Repeat for the remaining 5 Overview pickers: **Customer**, **AP Company**, **Device**, **Controller**, **Application**.
   - Expected: Every option in each picker's full list is visible inside the dropdown. If a list is long, the inner `<ul>` scrolls (`max-height: 320px; overflow-y: auto;` from the macro) — but the dropdown container itself is not clipped by the panel.
5. Sanity-check the Browse tab (regression check for Phase 4):
   - Navigate to http://localhost:8001/browse.
   - Open the **Platforms** picker, then **Parameters** picker.
   - Expected: Both still render unclipped, exactly as in Phase 4. Identical visual behavior to before the fix.
6. Sanity-check Phase 03 panels still clip (i.e., the rule did NOT over-broaden):
   - Navigate to a content/summary panel (any platform detail page that shows a `.panel` wrapper without a filter bar).
   - Expected: rounded corners still clip overflowing content; nothing inside that panel "escapes" beyond the rounded border.

Type **approved** when all 6 Overview pickers render unclipped and Browse + Phase 03 panels are unchanged. Otherwise describe which picker / panel still misbehaves and we will diagnose.
  </how-to-verify>
  <verify>
    <automated>MISSING — visual UAT cannot be automated; human confirmation via "approved" resume signal is the only acceptance gate.</automated>
  </verify>
  <done>
- User has typed "approved" after performing the 6-step visual check above.
- All 6 Overview pickers (Status, Customer, AP Company, Device, Controller, Application) render their full option lists without bottom clipping.
- Browse Platforms / Parameters pickers behavior is byte-stable with Phase 4 (no regression).
- Phase 03 panels without a filter bar continue to clip overflowing content via `overflow: hidden` (no over-broadening of the escape rule).
  </done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- Automated grep (Task 1 verify command) confirms the exact compound selector landed in app.css with the correct property/value.
- `git diff app_v2/static/css/app.css` is small and confined to lines ~106-110 (comment update + 1 selector clause appended).
- No other file in `app_v2/` changed (`git diff -- ':!app_v2/static/css/app.css'` is empty).
- Full v2 test suite stays green: `pytest tests/v2/ -q` shows no regressions (CSS change does not affect any pytest assertions).
- Human UAT confirms all 6 Overview pickers render unclipped; Browse pickers and Phase 03 panels are visually unchanged.
</verification>

<success_criteria>
- The Overview Status picker shows all 4 options fully visible (the original repro is resolved).
- The other 5 Overview pickers (Customer, AP Company, Device, Controller, Application) also render unclipped when their option list exceeds the panel's bottom edge.
- Browse Platforms / Parameters pickers behavior is byte-stable from Phase 4 (no regression).
- Phase 03 panels without a filter bar continue to clip overflowing content via `overflow: hidden` (no over-broadening).
- The fix is a single-region edit in `app_v2/static/css/app.css` — no HTML, Python, JS, or template changes; no new CSS rules elsewhere.
- Full v2 pytest suite remains green.
- User approves the visual UAT.
</success_criteria>

<output>
After completion, create `.planning/quick/260429-e76-fix-overview-filter-popover-clipping-ext/260429-e76-SUMMARY.md` documenting:
- The exact diff applied (selector + comment).
- Confirmation that grep returns exactly 1 hit each for `.panel:has(.browse-filter-bar)` and `.panel:has(.overview-filter-bar)`.
- Human UAT outcome (all 6 Overview pickers unclipped; Browse + Phase 03 unchanged).
- Reference back to STATE.md decisions: 04-02 (Phase 4 panel-overflow escape rule) and 05-01/05-05 (Phase 5 Overview redesign and shared picker_popover macro).
</output>
