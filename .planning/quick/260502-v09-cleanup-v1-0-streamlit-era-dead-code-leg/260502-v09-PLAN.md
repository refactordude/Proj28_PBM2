---
phase: quick-260502-v09
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/adapters/llm/base.py
  - app/adapters/llm/openai_adapter.py
  - app/adapters/llm/ollama_adapter.py
  - app/adapters/llm/registry.py
  - requirements.txt
  - .streamlit/config.toml
  - config/overview.example.yaml
autonomous: true
requirements:
  - CONCERNS-QW-3   # Delete app/adapters/llm/base.py
  - CONCERNS-QW-4   # Delete app/adapters/llm/openai_adapter.py
  - CONCERNS-QW-5   # Delete app/adapters/llm/ollama_adapter.py
  - CONCERNS-QW-6   # Delete app/adapters/llm/registry.py
  - CONCERNS-QW-7   # Drop requests>=2.32 from requirements.txt
  - CONCERNS-QW-8   # Drop plotly>=5.22 from requirements.txt
  - CONCERNS-QW-9   # Drop altair>=5.3 from requirements.txt
  - CONCERNS-QW-10  # Drop bcrypt>=4.2 from requirements.txt
  - CONCERNS-QW-11  # Drop openpyxl>=3.1 from requirements.txt
  - CONCERNS-QW-12  # Delete config/overview.example.yaml
  - CONCERNS-QW-2   # Delete .streamlit/config.toml (and parent dir)
  - CONCERNS-MISC-1 # Wipe orphan __pycache__ entries (local-disk hygiene)

must_haves:
  truths:
    - "Legacy LLM adapter cluster (base.py, openai_adapter.py, ollama_adapter.py, registry.py) is deleted from app/adapters/llm/."
    - "app/adapters/llm/pydantic_model.py still exists and is the only remaining adapter file (besides empty __init__.py)."
    - "requirements.txt no longer pins plotly, altair, bcrypt, openpyxl, or requests."
    - ".streamlit/ directory and config/overview.example.yaml no longer exist."
    - "Repo-root __pycache__/streamlit_app.cpython-313.pyc and orphan overview_*.pyc bytecode are wiped from local disk."
    - "Existing v2 test suite (442 passed / 5 skipped baseline) still passes; zero new failures introduced by deletions."
  artifacts:
    - path: "app/adapters/llm/pydantic_model.py"
      provides: "v2.0 NL agent LLM path (the kept adapter)"
      contains: "build_pydantic_model"
    - path: "requirements.txt"
      provides: "Python dependencies — pruned of 5 stale pins"
      excludes: ["plotly", "altair", "bcrypt", "openpyxl", "requests"]
  key_links:
    - from: "app_v2/main.py"
      to: "app.adapters.db.registry"
      via: "build_adapter import"
      pattern: "from app.adapters.db.registry import build_adapter"
      note: "Confirms v2 entrypoint never imported the deleted app.adapters.llm.registry path."
    - from: "app/core/agent/nl_agent.py"
      to: "app/adapters/llm/pydantic_model.py"
      via: "build_pydantic_model import (or equivalent)"
      pattern: "pydantic_model"
      note: "v2.0 NL agent's only LLM-adapter consumer; survives the deletion."
---

<objective>
Execute the HIGH-confidence Quick Wins from `.planning/codebase/CONCERNS.md`: delete the closed-subgraph legacy LLM adapter cluster left behind by the v1.0 Streamlit shell sunset, drop 5 stale dependencies from `requirements.txt`, remove orphan config files (`.streamlit/`, `config/overview.example.yaml`), and wipe stale `__pycache__` bytecode from local disk.

Purpose: Reduce the codebase's dead-code surface area so future audits (and AI agents reading the tree) don't waste cycles on unreachable v1.0 plumbing. The v2.0 milestone shipped 2026-04-29 (tag `v2.0`); these artifacts have no consumer in the current FastAPI/Bootstrap/HTMX shell.
Output: 4 deleted Python files (~226 LOC), 5 dropped requirements lines, 2 deleted config artifacts (1 file + 1 dir), local `__pycache__` orphans wiped. Test suite stays green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/codebase/CONCERNS.md
@./CLAUDE.md
@requirements.txt
@app/adapters/llm/pydantic_model.py

<interfaces>
<!-- Evidence pre-extracted so the executor doesn't need to re-grep. All HIGH-confidence per CONCERNS.md. -->

Closed-subgraph proof (CONCERNS.md §"Legacy LLM adapter cluster"):
```
$ grep -rn "from app.adapters.llm" app app_v2 tests | grep -v pydantic_model
app/adapters/llm/registry.py:9:  from app.adapters.llm.base import LLMAdapter
app/adapters/llm/registry.py:10: from app.adapters.llm.ollama_adapter import OllamaAdapter
app/adapters/llm/registry.py:11: from app.adapters.llm.openai_adapter import OpenAIAdapter
app/adapters/llm/openai_adapter.py:15: from app.adapters.llm.base import LLMAdapter, SQL_SYSTEM_PROMPT
app/adapters/llm/ollama_adapter.py:13: from app.adapters.llm.base import LLMAdapter, SQL_SYSTEM_PROMPT
```
No external consumers exist — these 4 files only import each other. `app_v2/main.py:30` imports `build_adapter` from `app.adapters.db.registry`, NOT `app.adapters.llm.registry`.

`app/adapters/llm/__init__.py` is empty (0 bytes) — no re-exports to update; deletion is mechanical.

Stale-dep audit (CONCERNS.md §"Stale dependencies"):
- `requests` — only `app/adapters/llm/ollama_adapter.py:11` imports it (dies with cluster)
- `plotly` — zero importers in app/, app_v2/, tests/, scripts/
- `altair` — zero importers
- `bcrypt` — zero importers (was transitive of streamlit-authenticator, already gone)
- `openpyxl` — zero importers (v1.0 Excel export retired with Streamlit per PROJECT.md "Out of Scope")

Current repo-root requirements.txt (15 active lines + 1 comment + 9 v2.0 additions). The 5 lines to drop:
```
line  4: openpyxl>=3.1
line 11: requests>=2.32
line 12: plotly>=5.22
line 13: altair>=5.3
```
Note: `bcrypt>=4.2` is NOT in the current requirements.txt — already removed in `260429-kn7`. CONCERNS.md flagged it but the audit predates the latest sweep. **Action: verify with `grep -n bcrypt requirements.txt` before assuming a no-op; if absent, log as already-clean and move on.**

Orphan `__pycache__` files to wipe (CONCERNS.md §"Misc cleanup candidates"):
- `__pycache__/streamlit_app.cpython-313.pyc` (repo root; src deleted in 260429-kn7)
- `app_v2/services/__pycache__/overview_{store,filter,grid_service}.cpython-313.pyc` (srcs deleted per D-JV-06)
- `tests/v2/__pycache__/test_overview_*.pyc`
- All `app/adapters/llm/__pycache__/{base,openai_adapter,ollama_adapter,registry}.cpython-*.pyc` after Task 1 deletions

These are gitignored (`__pycache__/` matches `.gitignore`) so the wipe is a local-disk-hygiene step that produces NO tracked git change. Task 4 explicitly notes this.
</interfaces>

</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete legacy LLM adapter cluster (closed subgraph)</name>
  <files>
    app/adapters/llm/base.py (DELETE)
    app/adapters/llm/openai_adapter.py (DELETE)
    app/adapters/llm/ollama_adapter.py (DELETE)
    app/adapters/llm/registry.py (DELETE)
  </files>
  <action>
    Delete the four legacy v1.0 LLM adapter files in `app/adapters/llm/`. These form a closed subgraph — they only import each other; nothing in `app/` (besides themselves), `app_v2/`, `tests/`, or `scripts/` consumes them. The v2.0 NL agent uses `app/adapters/llm/pydantic_model.py::build_pydantic_model` exclusively (the docstring on that file even calls out the legacy cluster as a "parallel path" left over from Phase 1).

    Steps:
    1. `rm app/adapters/llm/base.py app/adapters/llm/openai_adapter.py app/adapters/llm/ollama_adapter.py app/adapters/llm/registry.py`
    2. Confirm `app/adapters/llm/__init__.py` is empty (`wc -c app/adapters/llm/__init__.py` should be 0). If non-empty, it would be re-exporting deleted classes — open it and remove any `from .base/openai_adapter/ollama_adapter/registry import …` lines, then save. Per audit: it is currently 0 bytes, so this is a safety check, not expected work.
    3. Confirm `app/adapters/llm/pydantic_model.py` still exists and is untouched.
    4. Re-run the closed-subgraph grep to prove no broken imports remain:
       `grep -rn "from app.adapters.llm" app app_v2 tests scripts | grep -v pydantic_model` — expected output: empty.
    5. Stage the deletions only (no other files): `git add -A app/adapters/llm/`.

    Do NOT touch `pydantic_model.py`. Do NOT touch `app/adapters/db/`. Do NOT update CLAUDE.md or README.md (out of scope per `<scope>`).

    Why these specific files: CONCERNS.md Quick Wins #3-#6 (HIGH confidence). 4-file closed cluster confirmed by grep above; ~226 LOC total; zero test coverage; v2.0 NL path uses `pydantic_model.py` only.
  </action>
  <verify>
    <automated>
      # All four files gone:
      test ! -e app/adapters/llm/base.py && \
      test ! -e app/adapters/llm/openai_adapter.py && \
      test ! -e app/adapters/llm/ollama_adapter.py && \
      test ! -e app/adapters/llm/registry.py && \
      # pydantic_model.py preserved:
      test -e app/adapters/llm/pydantic_model.py && \
      # No external importers broken (closed subgraph confirmed empty after deletion):
      [ -z "$(grep -rn 'from app.adapters.llm' app app_v2 tests scripts 2>/dev/null | grep -v pydantic_model)" ] && \
      # Full test suite still green:
      pytest -q tests/v2/ tests/agent/ tests/services/ tests/adapters/ 2>&1 | tail -5
    </automated>
  </verify>
  <done>
    4 files deleted, `pydantic_model.py` and empty `__init__.py` preserved, no `from app.adapters.llm.{base,openai_adapter,ollama_adapter,registry}` imports remain anywhere, full pytest suite passes (442 passed / 5 skipped baseline preserved — same number or better).
  </done>
</task>

<task type="auto">
  <name>Task 2: Drop 5 stale dependencies from requirements.txt</name>
  <files>requirements.txt</files>
  <action>
    Edit `requirements.txt` to remove the 5 lines flagged HIGH-confidence in CONCERNS.md §"Stale dependencies":

    Lines to delete (current line numbers in the file):
    - Line 4: `openpyxl>=3.1` — zero importers; v1.0 Excel export retired per PROJECT.md "Out of Scope"
    - Line 11: `requests>=2.32` — sole importer was `ollama_adapter.py` (deleted in Task 1)
    - Line 12: `plotly>=5.22` — zero importers; v1.0 chart feature retired
    - Line 13: `altair>=5.3` — zero importers; never used (carry-over from v1.0 STACK research)
    - Plus `bcrypt>=4.2` IF it appears anywhere — CONCERNS.md flagged it, but a fresh `grep -n bcrypt requirements.txt` shows it is NOT in the current file (already removed by `260429-kn7`). If grep returns no hit, log "bcrypt already clean — no edit needed" and skip; if it returns a hit, delete that line too.

    Steps:
    1. `grep -nE "^(openpyxl|requests|plotly|altair|bcrypt)" requirements.txt` — record every match.
    2. Use the Edit tool (NOT sed) to remove each matching line individually so the diff is reviewable. Preserve all other lines verbatim, including the `# --- v2.0 Bootstrap Shell additions (Phase 1, INFRA-09) ---` comment and the trailing v2.0 deps.
    3. After edits, re-run `grep -nE "^(openpyxl|requests|plotly|altair|bcrypt)" requirements.txt` — expected output: empty.
    4. Confirm the file still parses as a valid pip requirements file: `python -m pip install --dry-run -r requirements.txt --no-deps 2>&1 | head -5` (we expect no syntax errors; resolver will report what it would install).

    Do NOT touch `httpx>=0.27` (CONCERNS.md flagged as LOW-confidence and explicitly out-of-scope per `<scope>`). Do NOT add any new lines.

    Why these specific lines: CONCERNS.md Quick Wins #7-#11 (HIGH confidence). Each verified zero-importer via grep; `requests` becomes orphan once Task 1 lands.
  </action>
  <verify>
    <automated>
      # None of the 5 stale deps remain (header-anchored, so we don't false-match an inline comment):
      [ -z "$(grep -nE '^(openpyxl|requests|plotly|altair|bcrypt)([[:space:]]|>|=|<|!|~|$)' requirements.txt)" ] && \
      # File still has the live deps:
      grep -qE '^sqlalchemy>=2\.0' requirements.txt && \
      grep -qE '^pandas>=3\.0' requirements.txt && \
      grep -qE '^fastapi>=0\.136' requirements.txt && \
      grep -qE '^pydantic-ai>=1\.0' requirements.txt && \
      # Full test suite still green (no module imports the dropped libs):
      pytest -q tests/ 2>&1 | tail -3
    </automated>
  </verify>
  <done>
    `requirements.txt` no longer contains `openpyxl`, `requests`, `plotly`, `altair`, or `bcrypt` pins. All other lines preserved byte-stable. Full pytest suite still passes (442 passed / 5 skipped baseline preserved).
  </done>
</task>

<task type="auto">
  <name>Task 3: Delete .streamlit/ directory and config/overview.example.yaml</name>
  <files>
    .streamlit/config.toml (DELETE)
    .streamlit/ (DELETE — whole dir)
    config/overview.example.yaml (DELETE)
  </files>
  <action>
    Delete two orphan config artifacts left over from the v1.0 Streamlit shell and the retired Phase 5 curated-Platform watchlist:

    1. `.streamlit/` directory — contains only `config.toml` (Streamlit theme: `primaryColor = "#1f77b4"` etc.). Streamlit is gone (CONCERNS.md Quick Win #2; `grep -rn '\.streamlit\|streamlit/config'` in app code returns nothing).
       Steps: `rm -rf .streamlit/`

    2. `config/overview.example.yaml` — Phase 5 curated-Platform watchlist scaffold; the live `config/overview.yaml` and the entire feature (`overview_store.py`, `overview_filter.py`, `overview_grid_service.py`, `POST /overview/add`) were retired per D-JV-06 / D-JV-07 (CONCERNS.md Quick Win #12). The `.example.yaml` template is the last user-facing surface. Existing test files mention it only in deletion-confirmation comments (`tests/v2/test_phase03_invariants.py:23,209`, `test_summary_routes.py:92`, `test_summary_integration.py:59`, `test_content_routes.py:56`) — those comments are historical and stay.
       Steps: `rm config/overview.example.yaml`

    3. Stage the deletions: `git add -A .streamlit/ config/overview.example.yaml`. Note that since these are deletions, `git add -A` on the specific paths is the correct invocation (a tracked file's deletion needs to be staged; an untracked-now-deleted file produces a tracked-deletion).

    Do NOT touch `config/settings.example.yaml`, `config/starter_prompts.example.yaml`, or any other `config/` file. Do NOT touch `content/` (those JV fixtures are live per CONCERNS.md "Unused fixtures / test data" section).
  </action>
  <verify>
    <automated>
      # Both paths gone:
      test ! -e .streamlit && \
      test ! -e config/overview.example.yaml && \
      # Other config examples still present (sanity check we didn't over-delete):
      test -e config/settings.example.yaml && \
      test -e config/starter_prompts.example.yaml && \
      # No live code references remain (test-comment-only mentions are fine):
      [ -z "$(grep -rn '\.streamlit/config' app app_v2 tests scripts 2>/dev/null)" ] && \
      # Test suite still green:
      pytest -q tests/v2/ 2>&1 | tail -3
    </automated>
  </verify>
  <done>
    `.streamlit/` directory removed entirely; `config/overview.example.yaml` removed; sibling example yamls preserved; no live code references either deleted path; v2 tests still pass.
  </done>
</task>

<task type="auto">
  <name>Task 4: Wipe orphan __pycache__ bytecode (local-disk hygiene)</name>
  <files>
    __pycache__/ (LOCAL DELETE — gitignored, no tracked change)
    app/adapters/llm/__pycache__/ (LOCAL DELETE)
    app_v2/services/__pycache__/overview_*.pyc (LOCAL DELETE)
    tests/v2/__pycache__/test_overview_*.pyc (LOCAL DELETE)
  </files>
  <action>
    Wipe stale `__pycache__/` bytecode that references files deleted in Task 1, in `260429-kn7`, or in Phase 1 D-JV-06. This task produces NO tracked git change — `__pycache__/` is gitignored — so the work is purely local-disk hygiene that the user explicitly requested in `<scope>`.

    Steps:
    1. Verify nothing important is in repo-root `__pycache__/`:
       `ls -la __pycache__/` — expected: only `streamlit_app.cpython-313.pyc` (the smoking gun), maybe other v1.0-era artifacts.
    2. Wipe ALL `__pycache__` dirs in the project tree, EXCLUDING `.venv/` if a venv exists at that path:
       `find . -path ./.venv -prune -o -path ./node_modules -prune -o -name __pycache__ -print -exec rm -rf {} +`
       The `-prune` clauses skip `.venv/` and `node_modules/` (if present) so we don't trash interpreter-managed caches that pytest will need on the next run anyway.
    3. Confirm wipe succeeded:
       `find . -path ./.venv -prune -o -path ./node_modules -prune -o -name __pycache__ -print` — expected: empty (until pytest re-runs).
    4. Run `git status --short` to confirm no tracked files changed (because `__pycache__/` is gitignored). Expected: clean working tree on the cache front; only the deletions from Tasks 1-3 (if uncommitted) are listed.
    5. Re-run pytest once to repopulate caches and confirm the wipe didn't break anything: `pytest -q tests/ 2>&1 | tail -3`. Pytest will rebuild the caches it needs on import.

    Important caveat the executor MUST surface in their commit/summary message: "This task touches no tracked files. The work is local-disk cleanup; the commit (if any) for this task will be empty unless combined with a doc/SUMMARY update. Recommended: skip an empty commit and mention this task in the SUMMARY.md hash-record paragraph for the quick task instead."

    Do NOT touch `.venv/` or `node_modules/`. Do NOT delete any `*.py` source files (only `*.pyc` and the `__pycache__/` dirs containing them).
  </action>
  <verify>
    <automated>
      # No __pycache__ dirs remain outside .venv (run BEFORE pytest re-creates them):
      # Note: this check is intentionally run before the final pytest re-run in the action.
      [ -z "$(find . -path ./.venv -prune -o -path ./node_modules -prune -o -name __pycache__ -print 2>/dev/null | head -1)" ] && \
      # Smoking-gun .pyc gone specifically:
      test ! -e __pycache__/streamlit_app.cpython-313.pyc && \
      # Sanity: source files NOT touched (count the v2 service .py files, expect ≥ 5):
      [ "$(find app_v2/services -maxdepth 1 -name '*.py' | wc -l)" -ge 5 ] && \
      # Re-run pytest to confirm wipe didn't break anything (and to repopulate caches):
      pytest -q tests/ 2>&1 | tail -3
    </automated>
  </verify>
  <done>
    All `__pycache__/` dirs outside `.venv/` and `node_modules/` wiped. `streamlit_app.cpython-313.pyc` and the legacy LLM cluster's `.pyc` files specifically gone. No tracked files changed (gitignored). Pytest re-runs cleanly and caches repopulate. Executor flags this as a no-tracked-change task in the SUMMARY.
  </done>
</task>

</tasks>

<verification>
After all 4 tasks complete, the cumulative state must satisfy:

1. **Source tree** (`app/adapters/llm/`):
   - Only `__init__.py` (empty) and `pydantic_model.py` remain.
   - `grep -rn "from app.adapters.llm" app app_v2 tests scripts | grep -v pydantic_model` returns empty.

2. **requirements.txt**:
   - `grep -nE "^(openpyxl|requests|plotly|altair|bcrypt)" requirements.txt` returns empty.
   - All other deps preserved byte-stable.
   - `python -m pip install --dry-run -r requirements.txt --no-deps` parses without syntax error.

3. **Config artifacts**:
   - `.streamlit/` and `config/overview.example.yaml` no longer exist.
   - `config/settings.example.yaml` and `config/starter_prompts.example.yaml` still exist (sanity).

4. **Local cache hygiene**:
   - No `__pycache__/` anywhere outside `.venv/` (immediately after Task 4 step 3; pytest will repopulate them by step 5, which is fine).

5. **Test suite**:
   - `pytest -q tests/` still passes at the v2 baseline (442 passed / 5 skipped per STATE.md). Zero new failures introduced. CONCERNS.md guarantees "zero test changes required" for the legacy LLM cluster (it has no tests), and zero importers exist for the dropped deps.

6. **Git diff sanity**:
   - 4 deleted Python files (`base.py`, `openai_adapter.py`, `ollama_adapter.py`, `registry.py`).
   - 1 modified file (`requirements.txt` — 4 line removals; 5 if `bcrypt` was present).
   - 1 deleted file (`config/overview.example.yaml`).
   - 1 deleted dir (`.streamlit/` containing `config.toml`).
   - Task 4 contributes ZERO tracked changes (gitignored).

   Total: ~6 tracked file deletions + 1 tracked file modification.
</verification>

<success_criteria>
- All 4 tasks pass their `<verify>` automated checks.
- Full pytest suite green (442 passed / 5 skipped or better; CONCERNS.md guarantees zero test impact).
- `app_v2/main.py` still imports `build_adapter` from `app.adapters.db.registry` (NOT `app.adapters.llm.registry`) — confirmed by app starting / tests passing.
- v2.0 NL agent path through `app/adapters/llm/pydantic_model.py::build_pydantic_model` untouched.
- Each task is one git commit with a clear message tracing back to its CONCERNS.md Quick Win number(s).
- Quick task SUMMARY.md records the cumulative impact: "~226 LOC removed, 5 deps dropped, 2 config orphans deleted, local pyc wipe (no tracked change)".
- Out-of-scope items per `<scope>` (README rewrite, CLAUDE.md trim, milestone archiving, settings.yaml key rotation, .env.example AUTH_PATH, stale comments in ask.py/starter_prompts.py/nl_service.py, httpx pin question, sqlite adapter investigation) are NOT touched in this plan and remain as separate follow-ups.
</success_criteria>

<output>
After completion, create `.planning/quick/260502-v09-cleanup-v1-0-streamlit-era-dead-code-leg/260502-v09-SUMMARY.md` recording:
- Per-task commit hashes (short SHAs).
- Final test-suite tally (e.g., "442 passed / 5 skipped — baseline preserved").
- Net file count: 4 deleted Python files + 1 deleted yaml + 1 deleted dir + 1 modified requirements.txt.
- Net LOC delta: ~-226 Python LOC + ~-5 requirements lines + ~-7 config lines.
- Note that Task 4 produced no tracked git change (local-disk hygiene only).
- Explicit list of out-of-scope items deferred to future quick tasks (copy from `<scope>` "Explicitly out of scope").
</output>
