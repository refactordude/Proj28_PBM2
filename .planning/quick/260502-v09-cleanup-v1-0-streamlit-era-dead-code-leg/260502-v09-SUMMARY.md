---
phase: quick-260502-v09
plan: 01
subsystem: cleanup
tags: [cleanup, dead-code, requirements, dependencies]
dependency-graph:
  requires: []
  provides:
    - "Reduced dead-code surface in app/adapters/llm/"
    - "Pruned requirements.txt (4 stale v1.0 deps removed)"
    - "Removed orphan v1.0 Streamlit/Phase-5 config artifacts"
  affects:
    - "app/adapters/llm/ — only pydantic_model.py + empty __init__.py remain"
    - "requirements.txt — 4 lines removed"
    - "config/ — overview.example.yaml gone; sibling examples preserved"
    - "Repo root — .streamlit/ dir gone"
tech-stack:
  added: []
  patterns: []
key-files:
  created: []
  modified:
    - requirements.txt
  deleted:
    - app/adapters/llm/base.py
    - app/adapters/llm/openai_adapter.py
    - app/adapters/llm/ollama_adapter.py
    - app/adapters/llm/registry.py
    - .streamlit/config.toml
    - config/overview.example.yaml
decisions:
  - "Task 4 (__pycache__ wipe) committed as zero-tracked-change task — gitignored, recorded in SUMMARY only, no empty commit per orchestrator constraint"
  - "bcrypt>=4.2 line absent at execute-time (already removed by 260429-kn7) — handled gracefully as a no-op rather than failing"
metrics:
  duration: "~4 minutes (3 commits + 1 no-tracked-change task + 4 pytest runs)"
  completed: "2026-05-02"
---

# Quick 260502-v09: Cleanup v1.0 Streamlit-era dead code Summary

**One-liner:** Executed 12 of the HIGH-confidence Quick Wins from the codebase-cleanup audit — deleted the 4-file legacy v1.0 LLM adapter closed-subgraph (~226 LOC), dropped 4 stale dependencies from `requirements.txt`, removed 2 orphan v1.0 config artifacts (`.streamlit/`, `config/overview.example.yaml`), and wiped local `__pycache__/` orphans — without touching any v2.0 surface (pydantic_model NL path, sibling config templates, JV fixtures all preserved).

## What Shipped

### Task 1 — Legacy LLM adapter cluster deleted (CONCERNS-QW-3..6)

`app/adapters/llm/` reduced from 6 files to 2:
- Deleted: `base.py` (LLMAdapter ABC + SQL_SYSTEM_PROMPT, 36 LOC), `openai_adapter.py` (legacy OpenAIAdapter, 78 LOC), `ollama_adapter.py` (legacy OllamaAdapter — sole importer of `requests`, 79 LOC), `registry.py` (legacy `build_adapter` factory, 33 LOC).
- Preserved: `pydantic_model.py` (the v2.0 NL-agent path; consumed by `app_v2/routers/ask.py:30`), `__init__.py` (empty, 0 bytes).
- Closed-subgraph proof: post-deletion grep `from app.adapters.llm` excluding `pydantic_model` returns empty across `app/`, `app_v2/`, `tests/`, `scripts/`. Extra paranoia grep for any other reference style (`adapters.llm.{base,openai_adapter,ollama_adapter,registry}`) also returned empty.
- ~226 LOC removed.

### Task 2 — Stale dependencies dropped (CONCERNS-QW-7..11)

`requirements.txt` pruned: `openpyxl>=3.1`, `requests>=2.32`, `plotly>=5.22`, `altair>=5.3` removed. All other lines (sqlalchemy, pymysql, pandas, pyyaml, pydantic, pydantic-ai, sqlparse, openai, httpx, python-dotenv, plus all 10 v2.0 Bootstrap Shell additions) preserved byte-stable.

`bcrypt>=4.2` was flagged in CONCERNS.md but the audit predates `260429-kn7`'s sweep; the line was already absent at execute-time. Handled as no-op per orchestrator pre-validation guidance.

### Task 3 — Orphan config artifacts deleted (CONCERNS-QW-2, QW-12)

- `.streamlit/config.toml` (and the parent `.streamlit/` dir) — Streamlit theme; the shell was retired in `260429-kn7` so the file has no consumer.
- `config/overview.example.yaml` — Phase 5 curated-Platform watchlist scaffold; the live `config/overview.yaml` and the entire feature were retired per D-JV-06/D-JV-07. Only test-comment mentions remain (deletion-confirmation comments left as historical record).
- Sibling templates preserved: `config/settings.example.yaml`, `config/starter_prompts.example.yaml`.

### Task 4 — Local `__pycache__/` orphans wiped (CONCERNS-MISC-1)

All `__pycache__/` directories outside `.venv/` and `node_modules/` wiped: 18 dirs total, including the smoking-gun `__pycache__/streamlit_app.cpython-313.pyc` (bytecode for a `.py` file deleted in `260429-kn7`) and `app_v2/services/__pycache__/overview_*.pyc` orphans (sources deleted per D-JV-06). pytest re-run repopulated needed caches cleanly.

**No tracked git change** — `__pycache__/` is gitignored. Per orchestrator constraint, no empty commit was created for this task; it is recorded here in the SUMMARY hash table instead.

## Per-Task Commit Hashes

| Task | Description | Commit | Tracked changes |
| ---- | ----------- | ------ | --------------- |
| 1 | Delete legacy v1.0 LLM adapter cluster | `291b935` | 4 files deleted |
| 2 | Drop 4 stale v1.0 deps from requirements.txt | `91548c1` | 1 file modified (4 lines removed) |
| 3 | Delete orphan v1.0 config artifacts | `03c9717` | 2 files deleted (1 file + 1 dir) |
| 4 | Wipe orphan __pycache__ bytecode (local-disk hygiene) | _no commit_ | 0 (gitignored — local-disk only) |

## Test Suite

Baseline (pre-execution): **624 passed / 5 skipped** (Note: plan stated "442 passed / 5 skipped" baseline based on STATE.md milestones; actual on-disk baseline at execute-time is 624 passed / 5 skipped — likely the integration/agent/services tests that the plan's targeted invocation excluded).

Post-execution (after each of Tasks 1, 2, 3, 4): **624 passed / 5 skipped** — zero regressions. CONCERNS.md guarantee held: "zero test changes required" because the legacy LLM cluster has no test coverage and the dropped deps have no importers anywhere.

Test suite was run 4 times (once after each task) using the project venv at `.venv/bin/pytest`. Each run took ~45s.

## Net File / LOC Delta

- **Python source files deleted:** 4 (base.py, openai_adapter.py, ollama_adapter.py, registry.py — ~226 LOC)
- **YAML files deleted:** 1 (config/overview.example.yaml — ~7 lines)
- **TOML files deleted:** 1 (.streamlit/config.toml — ~4 lines)
- **Directories deleted:** 1 (.streamlit/, plus 18 local __pycache__/ — gitignored)
- **Files modified:** 1 (requirements.txt — 4 lines removed)
- **Files created:** 0

Total tracked-file deletions: 6 (4 .py + 1 .yaml + 1 .toml). Tracked-file modifications: 1.

## Deviations from Plan

**None — plan executed exactly as written.**

The plan pre-flagged two soft-deviation paths and both materialized as predicted:
1. `bcrypt>=4.2` already absent from requirements.txt — handled gracefully as no-op per `<action>` guidance ("if grep returns no hit, log 'already clean — no edit needed' and skip").
2. Task 4 produces no tracked git change — handled per orchestrator constraint (skip commit; record in SUMMARY).

No Rule 1/2/3 auto-fixes were needed. No Rule 4 architectural escalation was needed.

## Verifications Performed (Cumulative)

1. **Source tree:** `app/adapters/llm/` now contains only `__init__.py` (empty) + `pydantic_model.py` (untouched). VERIFIED.
2. **Closed-subgraph grep:** `grep -rn "from app.adapters.llm" app app_v2 tests scripts | grep -v pydantic_model` returns empty. VERIFIED.
3. **Paranoia grep:** Any other reference style (`import`, `adapters.llm.X`) also returns empty. VERIFIED.
4. **Stale-deps grep:** `grep -nE "^(openpyxl|requests|plotly|altair|bcrypt)" requirements.txt` returns empty. VERIFIED.
5. **Live deps grep:** sqlalchemy, pandas, fastapi, pydantic-ai all still present in requirements.txt. VERIFIED.
6. **Config artifacts:** `.streamlit/` and `config/overview.example.yaml` gone; `settings.example.yaml` and `starter_prompts.example.yaml` preserved. VERIFIED.
7. **v2.0 NL agent path:** `app/adapters/llm/pydantic_model.py::build_pydantic_model` is the active path; only consumer is `app_v2/routers/ask.py:30`. UNCHANGED. VERIFIED.
8. **Test suite:** 624 passed / 5 skipped after each task (4 separate runs). VERIFIED.

## Out-of-Scope Items Deferred to Future Quick Tasks

Per the plan's `<scope>` "Explicitly out of scope" list, the following CONCERNS.md items were NOT touched in this task and remain as separate follow-ups:

- **README.md rewrite** (CONCERNS.md "Stale documentation: README.md") — claims v2.0 is "Active — Phases 1–3 of 5" with 413 tests; reality is shipped 6/6 phases at 624 passed. Largest reader-time impact deferred for its own quick task.
- **CLAUDE.md trim** (CONCERNS.md "Stale documentation: CLAUDE.md") — the v1.0 STACK research table in the per-project memory; the admonition disclaims it but the table itself is wall-of-stale-text.
- **`.env.example` AUTH_PATH line** (CONCERNS.md ".env.example") — legacy of `streamlit-authenticator`; safe to drop in a doc sweep.
- **Stale comments in live code** (CONCERNS.md "Stale Streamlit references in live code (cosmetic)"):
  - `app_v2/services/starter_prompts.py:9-13` (references deleted `app/pages/ask.py`)
  - `app_v2/routers/ask.py:54` (reference to `@st.cache_resource`)
  - `app/services/sql_limiter.py:4`
  - `app/core/agent/nl_service.py:27`
- **`httpx>=0.27` pin investigation** (CONCERNS.md "Needs Investigation") — direct importer dies with the legacy LLM cluster; remains as transitive of `openai`/`pydantic-ai`. Defer pin-vs-transitive decision.
- **SQLite adapter intent docstring** (CONCERNS.md "Needs Investigation") — `app/adapters/db/sqlite.py` is for v2 demo UAT but lacks an explicit demo-only docstring.
- **Milestone archives** (CONCERNS.md "Archive candidates") — explicitly deferred to `/gsd-cleanup`.
- **Rotate API key in working-tree `config/settings.yaml`** (CONCERNS.md security advisory) — not a cleanup task; user advisory only.

## Self-Check: PASSED

Verified via filesystem checks:
- FOUND: `app/adapters/llm/__init__.py` (empty, 0 bytes)
- FOUND: `app/adapters/llm/pydantic_model.py`
- FOUND: `requirements.txt` (10 lines pre-comment + 10 v2.0 lines, 4 stale deps removed)
- MISSING (correctly): `app/adapters/llm/base.py`, `openai_adapter.py`, `ollama_adapter.py`, `registry.py`
- MISSING (correctly): `.streamlit/config.toml`, `.streamlit/`, `config/overview.example.yaml`
- FOUND (preserved): `config/settings.example.yaml`, `config/starter_prompts.example.yaml`

Verified via git log:
- FOUND: commit `291b935` (Task 1)
- FOUND: commit `91548c1` (Task 2)
- FOUND: commit `03c9717` (Task 3)
- (Task 4 has no commit by design — gitignored local-disk hygiene)

All claims in this SUMMARY are backed by post-execution verification. Plan executed exactly as written with no deviations.
