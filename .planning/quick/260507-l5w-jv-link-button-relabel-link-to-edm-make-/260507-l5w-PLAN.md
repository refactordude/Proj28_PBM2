---
quick_id: 260507-l5w
type: quick
wave: 1
depends_on: []
files_modified:
  - app_v2/templates/overview/_grid.html
autonomous: true
requirements:
  - QUICK-260507-l5w
must_haves:
  truths:
    - "The active (linkable) Report Link button in the JV grid renders the visible label `edm` (lowercase, no expansion) instead of `Link`."
    - "The disabled Report Link button in the JV grid renders the visible label `edm` (lowercase, no expansion) instead of `Link`."
    - "The active button is visually discernible from the disabled button: active uses near-black ink (`text-dark`); disabled keeps Bootstrap's default washed-out outline-secondary look."
    - "The aria-labels are unchanged (`Open report link for {title}` / `No report link available`) — screen readers still hear meaningful copy, not the `edm` acronym."
    - "The detail page (app_v2/templates/joint_validation/detail.html) is NOT modified — the `Report Link` <th> header and the inline anchor stay byte-stable."
    - "The full v2 test suite stays green (no test currently pins the JV-grid Link button label or class string — verified via grep)."
  artifacts:
    - path: "app_v2/templates/overview/_grid.html"
      provides: "Two-branch Report Link button with `edm` label; active branch carries `text-dark` for contrast"
      contains: "edm"
  key_links:
    - from: "app_v2/templates/overview/_grid.html (active branch, line ~57-63)"
      to: "Bootstrap text-dark utility"
      via: "class attribute on the <a> element"
      pattern: "btn btn-sm btn-outline-secondary text-dark"
---

<objective>
Two cosmetic fixes to the per-row "Report Link" button in the Joint Validation grid (`app_v2/templates/overview/_grid.html`, lines 54-71):

1. **Relabel** the visible button text from `Link` to `edm` (lowercase, exactly as the user wrote it — internal acronym; do not capitalize, do not expand). Both branches (`{% if row.link %}` enabled and `{% else %}` disabled).
2. **Improve discernibility** between active and disabled states. Today both states use the SAME `btn btn-sm btn-outline-secondary` class with the SAME chain icon and the SAME label, so the only visual difference is the browser-default `disabled` styling (slight opacity + not-allowed cursor). Add `text-dark` to the active branch ONLY so it reads as clearly black/active vs. the muted disabled state.

Purpose: Match the precedent set by quick task 260429-ek7 (the Overview AI button restyle the user already approved). The 260429-ek7 styling was inadvertently dropped when commit `52bddfc` rewrote `_grid.html` from scratch for the JV grid pivot (Phase 1 Plan 05). This restores the approved visual pattern and applies the user's new `edm` label.

Output: One template file edited (`app_v2/templates/overview/_grid.html`); zero test files modified (verified — no test pins the JV-grid Link button label or class).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/STATE.md
@.planning/PROJECT.md
@app_v2/templates/overview/_grid.html
@.planning/quick/260429-ek7-restyle-active-state-link-button-in-over/260429-ek7-SUMMARY.md

<scope_boundaries>
**IN SCOPE — modify only:**
- `app_v2/templates/overview/_grid.html` lines 54-71 (the JV-grid Report Link button block, both `{% if row.link %}` and `{% else %}` branches)

**OUT OF SCOPE — DO NOT TOUCH:**
- `app_v2/templates/joint_validation/detail.html` — the `<th>Report Link</th>` cell at line 35 and the inline anchor at line 41 are a different surface; the user explicitly targeted "the Link button" (the grid action button), not the detail-page properties row.
- The AI Summary button block in the same `_grid.html` cell (lines 73-86) — not in scope.
- Any other route, template, service, CSS file, or test.
- `app_v2/static/css/app.css` — verified: no project-specific override for `.text-dark` or `.btn-outline-secondary` exists. Bootstrap defaults are sufficient.

**Test landscape verified:**
- `grep -rn '> Link\|>Link<\|"Link"' tests/v2/` — zero matches for the JV-grid Link button literal.
- `grep -rn 'btn-outline-secondary' tests/v2/` — only matches in `tests/v2/test_content_routes.py` lines 385/409, both inside `@pytest.mark.skip` decorators on legacy curated-Platform AI-button tests (already skipped per `_PLAN_04_REMOVED_AI_BUTTON_REASON`). Not in scope.
- `tests/v2/test_joint_validation_invariants.py` only checks the `| safe` filter is absent and the URL sanitizer scheme list — neither affected by this change.
- Therefore: zero test edits required; v2 suite stays green by construction.
</scope_boundaries>

<interfaces>
<!-- Current state of the active and disabled branches in _grid.html (lines 57-71). -->
<!-- The change is a 2-line edit: bare `Link` → `edm` in both branches, plus `text-dark` added to the active branch's class attribute. -->

```jinja
{# CURRENT (lines 57-71) #}
{% if row.link %}
  <a href="{{ row.link | e }}"
     class="btn btn-sm btn-outline-secondary"
     target="_blank" rel="noopener noreferrer"
     aria-label="Open report link for {{ row.title | e }}">
    <i class="bi bi-link-45deg"></i> Link
  </a>
{% else %}
  <button type="button"
          class="btn btn-sm btn-outline-secondary"
          disabled
          aria-label="No report link available">
    <i class="bi bi-link-45deg"></i> Link
  </button>
{% endif %}
```

```jinja
{# TARGET — exactly two textual changes per branch:
   active branch: class adds ` text-dark`; visible label `Link` → `edm`
   disabled branch: visible label `Link` → `edm` (class unchanged) #}
{% if row.link %}
  <a href="{{ row.link | e }}"
     class="btn btn-sm btn-outline-secondary text-dark"
     target="_blank" rel="noopener noreferrer"
     aria-label="Open report link for {{ row.title | e }}">
    <i class="bi bi-link-45deg"></i> edm
  </a>
{% else %}
  <button type="button"
          class="btn btn-sm btn-outline-secondary"
          disabled
          aria-label="No report link available">
    <i class="bi bi-link-45deg"></i> edm
  </button>
{% endif %}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Relabel Link → edm and add text-dark to active branch in JV grid</name>
  <files>app_v2/templates/overview/_grid.html</files>
  <action>
Edit `app_v2/templates/overview/_grid.html` with EXACTLY two changes inside the Report Link button block (lines 54-71). Use the Edit tool — do NOT rewrite the file.

**Change 1 — active branch (`{% if row.link %}`):**
- On the `<a>` element's `class` attribute, change `class="btn btn-sm btn-outline-secondary"` to `class="btn btn-sm btn-outline-secondary text-dark"` (append ` text-dark`, single space separator, lowercase).
- Inside the `<a>...</a>` body, change the literal text `Link` to `edm` (lowercase, exactly three characters, no quotes around it). The `<i class="bi bi-link-45deg"></i>` icon stays unchanged — only the label after the icon changes.

**Change 2 — disabled branch (`{% else %}`):**
- The `<button>` element's `class` attribute stays `class="btn btn-sm btn-outline-secondary"` — DO NOT add `text-dark` here. The disabled state should remain visually washed out so it contrasts with the now-darker active state.
- Inside the `<button>...</button>` body, change the literal text `Link` to `edm` (lowercase). Icon unchanged.

**Do NOT modify:**
- The `aria-label` attributes ("Open report link for ..." / "No report link available") — screen readers should keep getting the descriptive copy, not the bare `edm` acronym.
- The `bi-link-45deg` icon class.
- The surrounding comment block at lines 55-56 (`{# Report Link button (D-JV-15) — disabled when row.link is None ... #}`).
- The AI Summary button block immediately below (lines 73-86).
- The detail-page template `app_v2/templates/joint_validation/detail.html` — leave that file untouched.

**Rationale captured for traceability:** This restores the visual pattern from quick task 260429-ek7 (Overview AI button precedent). The user explicitly approved `text-dark` on the active branch + Bootstrap's default disabled styling on the disabled branch in that task. The `edm` label is the user's chosen acronym (likely an internal Confluence/EDM tool reference); preserve verbatim, lowercase, no expansion.
  </action>
  <verify>
    <automated>
# 1. Active branch: text-dark present + label is `edm` (and `Link` literal gone from the cell)
grep -F 'class="btn btn-sm btn-outline-secondary text-dark"' app_v2/templates/overview/_grid.html | wc -l | grep -q '^1$' && \
grep -cE '<i class="bi bi-link-45deg"></i> edm' app_v2/templates/overview/_grid.html | grep -q '^2$' && \
! grep -E '<i class="bi bi-link-45deg"></i> Link' app_v2/templates/overview/_grid.html && \
# 2. Disabled branch: class stays bare outline-secondary (no text-dark on the button element)
grep -E 'class="btn btn-sm btn-outline-secondary"$' app_v2/templates/overview/_grid.html | wc -l | grep -q '^1$' && \
# 3. Detail page is byte-stable — `Report Link` <th> header still present and untouched
grep -F '<th>Report Link</th>' app_v2/templates/joint_validation/detail.html | wc -l | grep -q '^1$' && \
# 4. aria-labels preserved (still say "report link", not "edm")
grep -F 'aria-label="Open report link for' app_v2/templates/overview/_grid.html | wc -l | grep -q '^1$' && \
grep -F 'aria-label="No report link available"' app_v2/templates/overview/_grid.html | wc -l | grep -q '^1$' && \
# 5. v2 test suite stays green
python -m pytest tests/v2/ -x --tb=short -q 2>&1 | tail -5
    </automated>
  </verify>
  <done>
- `app_v2/templates/overview/_grid.html` has the active branch class `btn btn-sm btn-outline-secondary text-dark` (single occurrence).
- Both branches show `edm` after the chain icon (`<i class="bi bi-link-45deg"></i> edm` appears exactly 2 times in the file).
- Zero occurrences of the legacy `Link` label after the chain icon in `_grid.html`.
- Disabled branch's `<button>` class is exactly `btn btn-sm btn-outline-secondary` (no text-dark).
- `aria-label` attributes on both branches unchanged.
- `app_v2/templates/joint_validation/detail.html` is byte-identical to its pre-task state (`<th>Report Link</th>` still at line ~35, anchor at line ~41 untouched).
- Full `pytest tests/v2/` suite passes (currently 546 — must remain ≥546).
  </done>
</task>

</tasks>

<verification>
**Automated checks (all run in Task 1 verify block):**
1. Active branch class string contains `text-dark` (exactly 1 occurrence in file).
2. Both branches show `<i class="bi bi-link-45deg"></i> edm` (exactly 2 occurrences in file).
3. No occurrences of the legacy `<i class="bi bi-link-45deg"></i> Link` pattern in `_grid.html`.
4. Disabled-branch `<button>` class line ends in `btn-outline-secondary"` (no `text-dark` appended).
5. `app_v2/templates/joint_validation/detail.html` retains the byte-stable `<th>Report Link</th>` header (out-of-scope file confirmed unchanged).
6. `aria-label` attributes preserved on both branches in `_grid.html`.
7. `pytest tests/v2/` exits 0; test count ≥ baseline (546 passing per the planning context).

**Human verification (deferred — not blocking):**
- Open the JV listing page in the browser; confirm the active `edm` button reads as clearly black/dark while the disabled `edm` button (rows without a `link:` frontmatter value) reads as clearly washed out / muted. The contrast difference should be visually obvious without needing to hover.
- Confirm the detail page's `Report Link` properties row still renders unchanged.
</verification>

<success_criteria>
1. **Label change applied** — Both active and disabled branches of the JV-grid Report Link button render the literal `edm` (lowercase, three characters) where they previously rendered `Link`.
2. **Discernibility restored** — Active branch carries `text-dark`; disabled branch keeps Bootstrap default outline-secondary. The two states are visually distinct without relying solely on the `disabled` attribute's opacity tweak.
3. **Accessibility preserved** — `aria-label` text is unchanged on both branches; screen readers still hear "Open report link for ..." / "No report link available", not the `edm` acronym.
4. **Out-of-scope files untouched** — `app_v2/templates/joint_validation/detail.html` is byte-identical; no other template, route, service, CSS, or test file modified.
5. **Test suite green** — `pytest tests/v2/` passes with no regressions (baseline 546 passing).
</success_criteria>

<output>
After completion, create `.planning/quick/260507-l5w-jv-link-button-relabel-link-to-edm-make-/260507-l5w-SUMMARY.md` capturing:
- Files modified (just `app_v2/templates/overview/_grid.html`)
- The two-line edit (active branch class + label, disabled branch label)
- Verification command outputs (grep counts + pytest summary)
- HUMAN-UAT pending item: "Load JV listing in browser; confirm active `edm` buttons read near-black and disabled `edm` buttons read washed-out grey — clearly discernible at a glance."
- Reference to the 260429-ek7 precedent that this restores.
</output>
