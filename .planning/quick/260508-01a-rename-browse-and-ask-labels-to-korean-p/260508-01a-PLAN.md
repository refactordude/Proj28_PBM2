---
phase: quick-260508-01a
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app_v2/templates/browse/index.html
  - app_v2/templates/ask/index.html
autonomous: true
requirements:
  - QUICK-260508-01a-01
must_haves:
  truths:
    - "Browse page renders the heading 'Platform 브라우저' (not 'Browse') inside .ph#browse-ph"
    - "Ask page renders the heading 'AI 질문하기' (not 'Ask') inside the .ph > h1.panel-title"
    - "Korean strings on the page headings match topbar tab labels verbatim (single source of truth)"
  artifacts:
    - path: "app_v2/templates/browse/index.html"
      provides: "Browse page heading with Korean label 'Platform 브라우저'"
      contains: "<b>Platform 브라우저</b>"
    - path: "app_v2/templates/ask/index.html"
      provides: "Ask page heading with Korean label 'AI 질문하기'"
      contains: '<h1 class="panel-title">AI 질문하기</h1>'
  key_links:
    - from: "app_v2/templates/browse/index.html line 30"
      to: "app_v2/templates/_components/topbar.html line 25"
      via: "verbatim string match"
      pattern: "Platform 브라우저"
    - from: "app_v2/templates/ask/index.html line 76"
      to: "app_v2/templates/_components/topbar.html line 29"
      via: "verbatim string match"
      pattern: "AI 질문하기"
---

<objective>
Rename the visible page headings on the Browse page ("Browse" → "Platform 브라우저") and the Ask page ("Ask" → "AI 질문하기") to match the Korean labels already used in the topbar tabs (`app_v2/templates/_components/topbar.html` lines 25 and 29).

Purpose: Consistency between the topbar tab labels (which already render the Korean names) and the corresponding page headings. Today a user clicks the "Platform 브라우저" tab and lands on a page titled "Browse" — the tab label and the page heading disagree. After this change the page heading echoes the tab label verbatim.

Output: Two heading sites updated. Internal Python identifiers (`BrowseViewModel`, route function names, etc.), comments, `aria-label="Browse presets"`, and `page_title` strings passed from routers are intentionally untouched (per scoping_already_done from orchestrator).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@app_v2/templates/_components/topbar.html
@app_v2/templates/browse/index.html
@app_v2/templates/ask/index.html

<interfaces>
<!-- Canonical Korean strings — copy verbatim from topbar.html.
     The space inside "Platform 브라우저" is a regular ASCII space (U+0020). -->

From app_v2/templates/_components/topbar.html line 25:
    <i class="bi bi-table"></i> Platform 브라우저

From app_v2/templates/_components/topbar.html line 29:
    <i class="bi bi-chat-dots"></i> AI 질문하기

Current state to replace:

app_v2/templates/browse/index.html line 30:
      <b>Browse</b>

app_v2/templates/ask/index.html line 76:
      <h1 class="panel-title">Ask</h1>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename Browse page heading to "Platform 브라우저"</name>
  <files>app_v2/templates/browse/index.html</files>
  <action>
    On line 30 of `app_v2/templates/browse/index.html`, replace the literal `<b>Browse</b>` with `<b>Platform 브라우저</b>`.

    The space between "Platform" and "브라우저" must be a regular ASCII space (U+0020), matching topbar.html line 25 byte-for-byte. Do NOT use a non-breaking space, em space, or any other variant.

    Do NOT touch:
    - The surrounding `<div class="ph" id="browse-ph">` wrapper.
    - The `<span class="tag">Pivot grid</span>` sibling on line 31.
    - Any comment in the file (the docstring at lines 1-23 mentions "Browse" multiple times but those are file-purpose comments, not user-visible text).
    - The `aria-label="Browse presets"` on line 62 — that labels the preset row, not the page heading.

    Use the Edit tool with `old_string="      <b>Browse</b>"` (preserve the 6-space indent visible in the file) and `new_string="      <b>Platform 브라우저</b>"`.
  </action>
  <verify>
    <automated>grep -F '<b>Platform 브라우저</b>' app_v2/templates/browse/index.html &amp;&amp; ! grep -nE '^\s*&lt;b&gt;Browse&lt;/b&gt;\s*$' app_v2/templates/browse/index.html</automated>
  </verify>
  <done>
    - Line 30 of `app_v2/templates/browse/index.html` reads `      <b>Platform 브라우저</b>` exactly.
    - The standalone heading line `<b>Browse</b>` no longer appears anywhere in the file (a `grep -nE '^\s*&lt;b&gt;Browse&lt;/b&gt;\s*$'` returns no matches).
    - File still parses as valid Jinja2 (no broken `{% %}` or unbalanced tags introduced).
    - Other occurrences of the substring "Browse" inside file-purpose comments and `aria-label="Browse presets"` are intentionally preserved.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rename Ask page heading to "AI 질문하기"</name>
  <files>app_v2/templates/ask/index.html</files>
  <action>
    On line 76 of `app_v2/templates/ask/index.html`, replace the literal `<h1 class="panel-title">Ask</h1>` with `<h1 class="panel-title">AI 질문하기</h1>`.

    The Korean string must match topbar.html line 29 byte-for-byte (no surrounding whitespace inside the tag, no trailing punctuation).

    Do NOT touch:
    - The surrounding `<div class="ph">` wrapper on line 75.
    - The LLM dropdown block on lines 84-117 (the `aria-label="Select LLM backend"` is unrelated).
    - File-purpose comments at lines 1-27 that mention "Ask" — those describe the page's role in the architecture, not user-visible text.

    Use the Edit tool with `old_string='      <h1 class="panel-title">Ask</h1>'` (preserve the 6-space indent visible in the file) and `new_string='      <h1 class="panel-title">AI 질문하기</h1>'`.
  </action>
  <verify>
    <automated>grep -F '<h1 class="panel-title">AI 질문하기</h1>' app_v2/templates/ask/index.html &amp;&amp; ! grep -nE 'class="panel-title"&gt;Ask&lt;/h1&gt;' app_v2/templates/ask/index.html</automated>
  </verify>
  <done>
    - Line 76 of `app_v2/templates/ask/index.html` reads `      <h1 class="panel-title">AI 질문하기</h1>` exactly.
    - The exact heading literal `class="panel-title">Ask</h1>` no longer appears in the file (a `grep -nE 'class="panel-title">Ask</h1>'` returns no matches).
    - File still parses as valid Jinja2 (no broken `{% %}` blocks).
    - Other occurrences of the word "Ask" in file-purpose comments are intentionally preserved.
  </done>
</task>

</tasks>

<verification>
Combined post-task checks:

1. Both Korean strings present in their target files:
   ```bash
   grep -F 'Platform 브라우저' app_v2/templates/browse/index.html
   grep -F 'AI 질문하기' app_v2/templates/ask/index.html
   ```
   Both must return exactly one match (the heading line).

2. Old standalone heading literals are gone (precise patterns to avoid false-positives on comments / aria-labels):
   ```bash
   ! grep -nE '^\s*<b>Browse</b>\s*$' app_v2/templates/browse/index.html
   ! grep -nE 'class="panel-title">Ask</h1>' app_v2/templates/ask/index.html
   ```
   Both must produce no output.

3. Korean strings match topbar.html byte-for-byte (single source of truth check):
   ```bash
   grep -F 'Platform 브라우저' app_v2/templates/_components/topbar.html
   grep -F 'AI 질문하기' app_v2/templates/_components/topbar.html
   grep -F 'Platform 브라우저' app_v2/templates/browse/index.html
   grep -F 'AI 질문하기' app_v2/templates/ask/index.html
   ```
   The first two confirm the canonical strings still live in topbar.html; the second two confirm the same bytes were echoed into the page templates.

4. Existing v2 test suite still green (smoke-level — these tests pin a lot of structural HTML; the heading change is unlikely to break them, but worth a scan):
   ```bash
   python -m pytest tests/v2/ -x -q
   ```
   Suite should remain at the green count it was at before the edits. If any test pinned the literal "Browse" or "Ask" inside the .ph heading specifically, that test must be updated in the same commit (Rule 1 auto-fix per project pattern). The project precedent for this exact situation is the topbar rebrand quick task (260507-mmv) which updated the topbar literals and tests in lockstep.
</verification>

<success_criteria>
- `app_v2/templates/browse/index.html` heading reads "Platform 브라우저"; the literal `<b>Browse</b>` heading line is gone.
- `app_v2/templates/ask/index.html` heading reads "AI 질문하기"; the literal `<h1 class="panel-title">Ask</h1>` is gone.
- Both new strings match `app_v2/templates/_components/topbar.html` lines 25 and 29 verbatim (regular ASCII space inside "Platform 브라우저"; no extra punctuation around "AI 질문하기").
- Internal Python identifiers, route function names, `page_title` router strings, and `aria-label="Browse presets"` are unchanged (per scoping_already_done).
- v2 test suite still green; any test that pinned the literal heading text gets updated in the same commit.
- Live smoke (manual, optional): `GET /browse` shows "Platform 브라우저" in the panel header; `GET /ask` shows "AI 질문하기" in the panel header.
</success_criteria>

<output>
After completion, create `.planning/quick/260508-01a-rename-browse-and-ask-labels-to-korean-p/260508-01a-SUMMARY.md` documenting:
- Two heading literals changed (file + line + before/after).
- Confirmation that internal identifiers, route names, `page_title`, comments, and the preset-row `aria-label` were intentionally NOT touched.
- v2 test suite result (green count before vs after).
- Any tests that needed lockstep updates (with reason).
</output>
