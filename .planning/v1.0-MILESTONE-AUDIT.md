---
milestone: v1.0
audited: 2026-04-24
status: passed
scores:
  requirements: 47/49  # 47 satisfied; FOUND-01/03 deferred per D-04 (accepted)
  phases: 2/2
  integration: 18/18  # all cross-phase wiring verified
  flows: 4/4  # Settings→Browse, Browse→Export, Browse→URL-share, Ask→Agent→DB
deferred:  # Accepted deferrals (not gaps)
  requirements:
    - id: "FOUND-01"
      status: "deferred"
      phase: "01"
      reason: "Auth deferred to pre-deployment phase per CONTEXT D-04 (locked decision). ROADMAP SC#1 was revised to acknowledge this during Phase 1 planning."
    - id: "FOUND-03"
      status: "deferred"
      phase: "01"
      reason: "Cookie-key startup guard deferred alongside FOUND-01 per D-04. config/auth.yaml still gitignored (FOUND-02) so demo creds cannot leak when auth is later enabled."
tech_debt:
  - phase: 01
    items:
      - "IN-01: duplicate @st.cache_resource factory in streamlit_app.py and browse.py — 2 separate engine pools for same db_name; acceptable for single-user intranet but wastes pool overhead"
      - "IN-02: settings.py line 26 has unused import `build_llm_adapter` (comment says 'retained for Phase 2' but Phase 2 is complete and it remains unused)"
      - "IN-03: _sanitize_filename has no unit test despite being a security-relevant path-traversal defence"
      - "IN-04: pandas>=3.0 has no upper bound in requirements.txt"
      - "Human validation: 6 items pending live-browser testing (persists in 01-HUMAN-UAT.md)"
  - phase: 02
    items:
      - "IN-01 (phase 2): hardcoded '5-step limit', '30 seconds', row_cap=200 in ask.py error copy — enforcement is correct (uses settings.app.agent) but display messages are stale if user overrides those values"
      - "IN-02 (phase 2): test_sql_validator.py gained UNION and CTE tests during code-review-fix but could benefit from fuzz testing"
      - "IN-03 (phase 2): ask.py line 314 imports _execute_read_only but inlines the logic below instead of calling it — dead import"
      - "IN-04 (phase 2): AgentRunFailure.last_sql is always '' on step-cap abort; D-22 'Partial output' expander renders empty in that case"
      - "Integration warning: browse.py:41 docstring claims cross-function cache deduplication — this is incorrect (separate @st.cache_resource functions produce separate cache slots)"
      - "extract_json (NL-09) exists and is unit-tested but never invoked in production — PydanticAI handles structured output natively. NL-09 is satisfied by framework; extract_json is defensive-dead-code"
      - "Human validation: 6 items pending live-LLM testing (persists in 02-HUMAN-UAT.md)"
  - milestone:
    items:
      - "Deprecation warning from pydantic_ai 1.86 internals: `asyncio.get_event_loop()` — non-failing, benign, will resolve when pydantic_ai ships asyncio.new_event_loop()"
      - "ufs_service.py calls _db._get_engine() unconditionally while nl_agent and ask.py defensively fallback to run_query — inconsistent adapter contract; blocking for any future non-MySQL adapter"
integration:
  cross_phase_exports:
    - from: "streamlit_app.render_sidebar"
      to: "ask.py / browse.py / settings.py"
      via: "st.session_state['active_db'], st.session_state['active_llm']"
      status: "wired"
    - from: "ufs_service.list_parameters (Phase 1)"
      to: "ask.py NL-05 catalog add-from (Phase 2)"
      via: "list_parameters(db, db_name=db_name)"
      status: "wired"
    - from: "app.core.config.save_settings → st.cache_*.clear() (Phase 1)"
      to: "ask.get_nl_agent rebuilds (Phase 2)"
      via: "global cache_resource clear"
      status: "wired"
    - from: "build_pydantic_model factory (Phase 2 02-01)"
      to: "build_agent → run_agent (Phase 2 02-03)"
      via: "import from app.adapters.llm.pydantic_model"
      status: "wired"
    - from: "safety primitives validate_sql/inject_limit/scrub_paths (Phase 2 02-02)"
      to: "nl_agent.run_sql tool (Phase 2 02-03)"
      via: "direct imports, composed inside tool body"
      status: "wired"
  e2e_flows:
    - name: "Settings → Browse"
      steps: "User edits DB in Settings → Save → cache clear → Browse sees new DB"
      status: "verified"
    - name: "Browse → Export"
      steps: "User pivots → clicks Export → dialog with Excel/CSV download"
      status: "verified"
    - name: "Browse → Shareable URL"
      steps: "User copies link → paste in new tab → same state"
      status: "verified"
    - name: "Ask → NL Agent → DB"
      steps: "User types question → LLM proposes params → NL-05 confirm → Run Query → result + summary"
      status: "verified (automated via TestModel; human verification pending live LLM)"
---

# Milestone v1.0 — Audit Report

## Summary

**Status:** PASSED ✓
**Scope:** 2 phases (Foundation + Browsing, NL Agent Layer), 49 requirements
**Duration:** 2026-04-23 → 2026-04-24 (autonomous /gsd-autonomous run, single session)

All 47 active requirements satisfied; 2 intentionally deferred per locked decision D-04 (auth deferred to pre-deployment phase). Cross-phase integration verified — 18 wired exports, 4 end-to-end flows, 0 broken connections. 171-test suite passing, including regression tests for 2 critical SQL validator bypasses (UNION/CTE smuggle) found and fixed during Phase 2 code review.

## Requirements Coverage

| Phase | Requirements | Satisfied | Deferred | Orphaned |
|-------|--------------|-----------|----------|----------|
| 1: Foundation + Browsing | 32 | 30 | 2 (FOUND-01, FOUND-03) | 0 |
| 2: NL Agent Layer | 17 | 17 | 0 | 0 |
| **Total** | **49** | **47** | **2** | **0** |

All 47 satisfied requirements have evidence in plan SUMMARY.md frontmatter (`requirements-completed`) AND are checked `[x]` in REQUIREMENTS.md traceability table AND were verified PASSED in the corresponding phase VERIFICATION.md.

The 2 deferred requirements (FOUND-01 login, FOUND-03 cookie guard) are blocked by the locked decision D-04 in Phase 1 CONTEXT.md (auth deferred to pre-deployment). ROADMAP Phase 1 SC#1 was revised during planning to acknowledge this. FOUND-02 (gitignore config/auth.yaml) IS in scope and complete, so the scaffold does not leak demo creds when auth is enabled later.

## Phase Verification Rollup

### Phase 1: Foundation + Browsing

- **VERIFICATION.md status:** human_needed (16/16 automated must_haves verified; 6 human validation items deferred to HUMAN-UAT.md)
- **Plans completed:** 7/7
- **Tests:** 75 Phase 1 tests passing after initial implementation; 0 regressions after Phase 2 added
- **Code review:** 1 critical + 5 warnings + 4 info → all critical/warning fixed with atomic commits (CR-01 table allowlist, WR-01 multi-DB cache key, WR-02 logger.debug, WR-03 pd.isna guard, WR-04 sanitize_filename order, WR-05 browse.tab overwrite, plus WR-06 XSS escape uncovered in re-review) — all regression tests added

### Phase 2: NL Agent Layer

- **VERIFICATION.md status:** human_needed (17/17 automated must_haves verified; 6 human validation items deferred to HUMAN-UAT.md)
- **Plans completed:** 6/6
- **Tests:** 96 new Phase 2 tests (171 total) passing
- **Code review:** 2 critical + 4 warnings + 4 info → all critical/warning fixed
  - CR-01 SQL UNION bypass (admin table smuggled via UNION) — fixed with blanket `UNION/INTERSECT/EXCEPT` rejection + 5 regression tests
  - CR-02 SQL CTE bypass (WITH evil AS (SELECT FROM other_table)) — fixed with `Token.Keyword.CTE` rejection + 3 regression tests
  - WR-01 display-side SQL missing max_execution_time — fixed
  - WR-02 Regenerate discarded confirmed NL-05 params — fixed
  - WR-03 path scrubber not case-insensitive — fixed with `re.IGNORECASE` + uppercase-path tests
  - WR-04 run_sql exception leaked pymysql connection string to LLM — fixed (server-side log, only exception type surfaced to agent)

## Cross-Phase Integration

0 blocking integration issues. All 18 cross-phase exports wired and exercised. See frontmatter `integration:` for full map.

1 Warning: browse.py:41 docstring misleads maintainers into assuming cross-function cache deduplication. This is a comment-only bug — the actual runtime behavior (separate engine pools per cache slot) is acceptable for a single-user intranet but not what the docstring implies.

## Tech Debt

16 minor items across the 2 phases and milestone level. None block v1.0. See frontmatter `tech_debt:` for full list. Highest-impact items for a follow-up cleanup phase:

1. Consolidate the 3 `@st.cache_resource get_db_adapter` factories into a single shared import
2. Update hardcoded "5-step limit", "30 seconds", row_cap=200 in ask.py error copy to read from `settings.app.agent`
3. Either delete `extract_json` (Ollama fallback) or wire it into a PydanticAI `on_output_failed` hook
4. Normalize `DBAdapter` ABC to either require `_get_engine()` or consistently use defensive `hasattr`
5. Decide whether to clean up dead import `_execute_read_only` in ask.py or refactor SQLResult-branch duplicated logic

## Human Validation Pending

12 items across 2 phases persisted in HUMAN-UAT.md files for later manual testing via `/gsd-verify-work`. These could not be automated (live DB queries, live LLM backend, browser interactions, URL round-trip).

## Nyquist Compliance

`workflow.nyquist_validation` not enabled in config — step skipped. No VALIDATION.md files in any phase (expected).

## Security

Threat models declared in all 13 plan files. SAFE-01 through SAFE-06 all satisfied post-code-review-fix. No secrets logged or exposed. The /gsd-secure-phase command can be run for an extra retroactive audit per phase if desired.
